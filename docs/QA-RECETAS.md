# QA-RECETAS.md — Flujos de verificación por módulo

Recetas paso a paso para verificar cada módulo tras un cambio, sin redescubrir el flujo. La IA lee la receta del módulo tocado y la ejecuta en el navegador (localhost:8080).

## ⚡ Regla de alimentación

Cada flujo que se verifique de verdad deja aquí sus pasos exactos. Los ⬜ son huecos por confirmar en la primera ejecución real: **al ejecutarlos, se reemplazan por el paso confirmado en la misma sesión**. Si la UI cambia y una receta miente, se corrige al detectarlo. Nunca escribir pasos no ejecutados como si fueran hechos.

## Base común

- App: http://localhost:8080 (Docker arriba). Hotel semilla: **Los Cedros**.
- Usuarios QA: `claude_qa1` (general/nómina), `dueno_qa` (rol dueño remoto). ⬜ contraseñas: pedirlas a Maximiliano la primera vez y anotar aquí dónde viven (no anotar la contraseña).
- Verificación de dinero: SIEMPRE por script PDO en el contenedor, nunca mysql CLI (timezone). Patrón:
  `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php -r "...PDO..."`
- Estado previo: correr la radiografía (`tools/radiografia.php`) para descartar migraciones pendientes antes de culpar al cambio.

## Smoke global HTTP (ejecutado ✅ 2026-07-17, todo 200)

Barrido autenticado de módulos sin navegador — 1 minuto, detecta 5xx y rutas rotas:
1. Crear endpoint temporal de sesión en `src/public_html/tmp_sesion_smoke.php` (sembrar `$_SESSION` de gerente Los Cedros; nombre `tmp_*` porque `qa_*`/`__qa_*`/`debug_*` dan 403 por .htaccess). **Borrarlo al terminar y verificar 303.**
2. Con cookie jar de curl (⚠️ `-c` Y `-b` en CADA request: la app regenera el session id en la primera petición autenticada), recorrer: dashboard, habitaciones, mapa, areas, reservaciones, huespedes, caja, inventario, reportes, configuracion, configuracion/roles, tareas, nomina, trabajadores, camarista, lavanderia, lavanderia/lotes, lavanderia/pedidos, documentos, compras, proveedores, cuentas-por-cobrar, cuentas-por-pagar, facturacion, usuarios, auditoria, operacion/diaria, copiloto/valor, canales, whatsapp, lealtad, reputacion, forecast, mantenimientos/activos, checkin-digital. Esperado: todo 200; `/dueno` da 302 para gerente (falta `dueno.view`, correcto).
3. Comparar `wc -l` del log `storage/logs/app-YYYY-MM-DD.log` antes/después: no debe crecer.
4. ⬜ Repetir el barrido con rol recepcionista y dueno_qa cuando los gates de autorización (plan operativo) estén activos.

## Menú y aterrizaje por permiso del rol (sidebar filtrada) — verificado ✅ 2026-07-18

Objetivo: comprobar que un rol acotado ve en la sidebar SOLO sus pantallas y aterriza en su herramienta, sin dev-login. La sidebar/`config/navegacion.php` filtran por `módulo activo Y can(permiso)`.

1. **Sembrar un rol de prueba** por PDO en el contenedor (Database NO conecta en CLI → PDO crudo con `require config/database.php`; DB = `medisoft_hoteles_import`). Ej. rol `camarista_qa` en hotel 1 con `permisos_json=["camarista.view"]`, `es_sistema=0`, `activo=1`. Anotar el `id`.
2. **Endpoint temporal de sesión** en `src/public_html/tmp_<algo>.php` (nombre fuera de `qa_*/__qa_*/debug_*` que dan 403 por .htaccess): `session_set_cookie_params([...'path'=>'/'...])` + `session_start()` + sembrar `$_SESSION` (`user_id=43`, `hotel_id=1`, `hotel_slug='los-cedros'`, `hotel_usuario=['id'=>34,'rol'=>'recepcionista','role_id'=><ID>]`, `unset($_SESSION['user'])`), luego `header('Location: /dashboard')`. **Borrarlo al terminar** (verificar con `git status`) y **borrar el rol de prueba** (DELETE si `hotel_usuarios.role_id` no lo referencia). ⚠️ un rol personalizado guarda `hu.rol='recepcionista'`: para simularlo, ese es el ENUM correcto; `can()` resuelve por `role_id`.
3. Navegar al endpoint (`?to=/dashboard`) y **enumerar la sidebar por JS** (el `screenshot` del pane se cuelga; usar `javascript_tool`):
   `Array.from(document.querySelectorAll('.sidebar-nav .nav-item .nav-text')).map(t=>t.textContent.trim())`
   y `location.pathname`. Recargar con `?nc=N` si el bfcache del .htaccess sirve versión vieja.
4. **Asserts** (verificados jul-18): rol `camarista.view`-solo → sidebar = **["Limpieza"]**, y `/dashboard` REBOTA a **`/camarista`** (`home_route_for_current_user` → `primary_landing_route`). Recepción (role_id sembrado 29) → Habitaciones/Reservas/Huéspedes/Check-in/Caja/Tareas/Limpieza/Mantenimiento/Documentos, aterriza en dashboard. Gerente (15) → menú completo (35 ítems, 7 secciones). GOTCHA verificado: la cuenta QA (`usuarios.id=43`) tiene rol GLOBAL 'gerente' → sirve para cazar fugas por `is_gerente()/is_admin()` (deben gatear por rol del hotel, no global).
5. Es visibilidad de MENÚ; el enforcement de servidor YA existe desde 2026-07-19 (receta abajo).

## Gates de servidor RBAC (enforcement) — verificado ✅ 2026-07-19

Objetivo: comprobar que un gate `require_permission_or_403`/`can_any` devuelve 403 REAL a un rol acotado y NO rompe al rol operativo, sin dev-login.

1. **Endpoint temporal de sesión** en `src/public_html/tmp_<algo>.php` (fuera de `qa_*/__qa_*/debug_*`): clona los `session_set_cookie_params` de `index.php` (path '/', httponly, samesite Lax, secure=false en dev) + `session_start()`, siembra `$_SESSION` (`user_id=43`, `hotel_id=1`, `hotel_slug='los-cedros'`, `hotel_usuario=['id'=>34,'rol'=>'recepcionista','role_id'=>N]`, `unset($_SESSION['user'])`). **Borrarlo al terminar** (verificar `git status`).
2. ⚠️ El `role_id` es OBLIGATORIO para un test fiel: `can()` sin `role_id` cae a `can_legacy()`→`user_role()` (rol GLOBAL) y el user 43 es gerente global → NO restringe (ver CEMENTERIO). Usa un `role_id` de rol acotado REAL del hotel.
3. Con curl + cookie jar dentro del contenedor (`http://localhost`): sembrar por `?role_id=N` y pedir la pantalla gateada. Asserts verificados en Los Cedros: role_id=**33** "Limpieza" (sin `reservaciones.view`/`huespedes.view`) → `/reservaciones` y `/huespedes` = **403** (`<title>403 - Acceso denegado`); role_id=**29** recepción → **200** con contenido. Gerente (legacy, sin role_id) arranca sin fatales.
4. Escrituras (`/reservaciones/cancelar/{id}` etc.) son POST-only: un GET da 404 (NO es el gate). Para el 403 de escritura, POST con CSRF o confiar en la caracterización estática de `ReservacionGatesRbacTest` (50 asserts).

## Permisos por PERSONA dentro de su rol — verificado ✅ 2026-07-23

Objetivo: comprobar que `/configuracion/roles` lista a la gente de cada rol y que ajustarle permisos a UNA persona no toca a sus compañeros. Guarda diferencias (`hotel_usuarios.permisos_json` = `{"extra":[...],"quitados":[...]}`), no una copia del rol.

1. Endpoint temporal de sesión (mismo patrón de las dos recetas de arriba) con un `role_id` que traiga `roles.manage` (Los Cedros: 15 = Gerente). **Borrarlo al terminar.**
2. **Listado**: `/configuracion/roles` → cada tarjeta trae su bloque "Quién lo tiene". Assert por JS: `[...document.querySelectorAll('.rv-card')].map(c=>[c.querySelector('.rv-rolename').textContent.trim(), c.querySelectorAll('.rv-person').length])`. En Los Cedros: Gerente 4, Recepcionista 5, y franja ámbar `.rv-orphans` con los 3 `hotel_usuarios` sin `role_id`. Los roles de acceso total listan a su gente como `<div>`, no como enlace.
3. **Guardar**: POST a `/configuracion/roles/{rol}/persona/{usuario}` con el set COMPLETO de casillas marcadas (la vista manda todo lo que la persona puede; el servidor deriva la diferencia con `Rol::derivarAjustes`). Omitir uno que el rol sí da → `quitados`; mandar uno que no da → `extra`. Verificar la columna por PDO: debe quedar el JSON mínimo, no la lista entera.
4. **Ámbito** (el assert que importa): un permiso FUERA del catálogo de `config/permisos.php` — hoy `llaves.control`, que trae el preset Recepcionista — **no debe aparecer en `quitados`** aunque no venga marcado (no tiene casilla que marcar). Si aparece, el ámbito se rompió y toda persona ajustada pierde llaves en silencio.
5. **Candados** (los 4 dan 303 y NO escriben): rol/persona cruzados (`/roles/29/persona/24` con la 24 en otro rol), persona de otro hotel, rol de acceso total (`/roles/8/...`), y sobre uno mismo → quitarse `roles.manage` o darse un permiso que su rol no tiene. Los dos últimos redirigen al FORMULARIO con el mensaje; los otros al listado.
6. **Restablecer**: POST a `.../restablecer` → `permisos_json = NULL` y la persona vuelve a heredar su rol. Bitácora en `logs_acceso` con `tipo='permisos_persona'`.
7. **Interruptor "control total" del área** (aplica igual a `/configuracion/roles/{id}/editar`): el permiso `<modulo>.all` sale de la lista y va arriba en su propio bloque. Asserts por JS sobre un grupo con wildcard (ej. `documentos`): al ponerlo → grupo con clase `is-total`, hijos `checked` y `disabled`, botón "Todos" oculto, y `new FormData(form).getAll('permisos[]')` devuelve **solo** `documentos.all` para ese grupo; al quitarlo → hijos editables y siguen marcados. En la pantalla por persona, además: la marca verde/roja se pone en el interruptor y **los hijos no se pintan ni cuentan** en el contador.
8. **Guardar un rol es sin pérdida** (regresión del bug de `llaves.control`): respaldar `roles.permisos_json`, abrir el form, reenviar EXACTAMENTE las casillas marcadas y no deshabilitadas, y comparar conjuntos ordenados contra el respaldo. Verificado idéntico jul-23 en Gerente (33, con 13 interruptores puestos → no explota en hijos) y Recepcionista (19, conserva `llaves.control` pese a no tener casilla).
9. **Modo oscuro** (`.permp-view`, `.rolf-view` y lo nuevo de `.roles-view`): escaneo por luminancia = 0 elementos claros. ⚠️ el escáner debe convertir `color(srgb r g b)` a 0–255 antes de medir, o los `color-mix` pasan de largo y reporta 0 fugas falsas (así se coló el chip dorado "A la medida" en la primera pasada).

## Reservaciones (crear → anticipo → check-in → checkout)

1. `/reservaciones` → botón crear (form activo: `crear.php`, jQuery).
2. Crear reservación con huésped nuevo y ⬜ (datos mínimos exactos del form).
3. Registrar **anticipo** desde ⬜ (pantalla/modal) → debe crear fila en `reservacion_abonos` ligada a caja (AnticipoService).
4. **Check-in**: el cobro debe ser saldo-aware (total − anticipo, sin doble cobro). En móvil el modal es wizard de 2 pasos (≤640px); `ver.php` tiene su propio wizard distinto.
5. **Paso 2 del check-in de `ver.php` (cobro tipo POS, rediseñado jul-2026)** — verificado en navegador: (a) 1 toque a una ficha → autollena el total, barra 100%, "Cobro completo"; (b) split: teclear $800 en tarjeta → "Falta $1,200" → tocar efectivo → rellena $1,200 (efectivo es el relleno automático); (c) saldo pendiente + efectivo $500 → barra 25%, "Se cobra una parte hoy", `permitir_saldo_pendiente=1`. **Invariante**: nunca coexisten "Cobro completo" + "paga solo una parte" marcado — al cubrir el total el checkbox se apaga solo (`calcularTotales`), y activarlo estando cubierto parte de cero para capturar el parcial real (`toggleSaldoPendienteCheckIn`). IDs de inputs y validación (`validarPagoCheckInWizard`) intactos. GOTCHA de prueba: `SALDO_RESERVACION_CHECKIN` puede ser 0 en reservas ya saldadas (#32869 quedó en 0) → para probar la lógica, forzar `totalReservacion=2000` en la consola. ⬜ falta confirmar el mismo rediseño en el modal de **check-in tardío** (`#modalCheckInTardio`, aún patrón viejo de chips).
6. Checkout → verificar por PDO que los movimientos de caja cuadran con lo cobrado.
7. **Ticket térmico (verificado en navegador ✅ 2026-07-21)**: en `ver.php` → "Imprimir ticket termico". Debe reflejar la cuenta REAL (`resumenPagos` + `abonos` + `pagos`), jamás el total como pagado: (a) reserva con anticipo parcial → título "COMPROBANTE DE PAGO PARCIAL", anticipo listado con método+fecha, filas Total estancia/Pagado y renglón grande `SALDO PENDIENTE: $X MXN`; (b) sin pago → "COMPROBANTE DE RESERVACIÓN" + "Sin pago registrado" + saldo completo; (c) checked_out liquidada → "COMPROBANTE DE PAGO", desglose real de `reservacion_pagos` (funciona TRAS checkout: el controller ya carga pagos en todo estado) y `TOTAL PAGADO`. Tarjeta muestra crédito/débito. Truco de verificación sin abrir ventana: interceptar `window.open` con fake `document.write` desde javascript_tool y assertear el HTML.
8. **Cotización PDF (verificado por curl+asserts ✅ 2026-07-22)**: hay DOS PDFs — desde `ver.php` (`POST /reservaciones/cotizacion-reservacion-pdf`, `reservacion_id`) y desde el form de crear (`POST /reservaciones/cotizacion-pdf`, huesped+fechas+habitaciones[]+hora_llegada). Ambos deben mostrar: columna **LLEGADA ESTIMADA** en la caja de estancia (hora del huésped + subtexto "Hora indicada por el huésped"; sin hora → "Por confirmar"), columna **PERS.** (capacidad real, fallback por tipo), SIN bloque de vehículos/placas, y el cromado con primario↔secundario INTERCAMBIADOS (header/tabla/total = `color_primary`). Verificación sin ojo humano: inflar streams del PDF con `gzuncompress` y assertear texto + que el PRIMER `x.xxx x.xxx x.xxx rg` del stream sea el primario del hotel (RGB/255 a 3 decimales).
9. **Candado de pendientes en disponibilidad (verificado en navegador ✅ 2026-07-17)**: sembrar reservación `checked_in` con salida AYER (⚠️ fechas relativas al reloj de la APP, no al `CURDATE()` de PDO crudo que va en UTC) → en `/reservaciones/crear` con entrada HOY la habitación sale como tarjeta roja "Check-out vencido · Resolver primero" (link a la reservación, sin checkbox) y la API `disponibles` la excluye; con entrada MAÑANA vuelve a estar disponible. Habitación en `limpieza` + entrada HOY → chip "En limpieza ahora" y al seleccionarla msConfirm (aviso, no candado). Check-in desde `ver.php` con habitación en limpieza/mantenimiento → msConfirm "no está marcada como lista" antes de abrir el modal. Suite: `DisponibilidadPendientesTest.php` (27 asserts).

## Tarifas — recálculo de reservaciones existentes (impacto)

Verificado E2E en navegador ✅ 2026-07-22 (worktree servido en :8090 contra BD de prueba aislada; claro Y oscuro 0 fugas). Suite: `TarifaImpactoTest.php` (48 asserts).

1. `/configuracion/tarifas/crear` (o editar): card "Revisar reservaciones existentes al guardar" visible con clase incremento; al elegir **Descuento** se oculta y desmarca (los descuentos no recalculan en retro) — verificado.
2. Guardar con el checkbox → aterriza en `/configuracion/tarifas/impacto/{id}`: chips de la tarifa + tabla de afectadas (solo confirmada/checked_in con noches por venir y habitaciones en el alcance) con precio actual → nuevo, delta, pagado y saldo resultante — verificado (+10% global: 1000→1100; checked_in en curso solo repone noches dentro de vigencia: 1000→1050).
3. Banderas: "Detalle desincronizado" (caso prod #1867: header ≠ Σ detalle − descuento; al aplicar se corrige), "Sobrepago" (pagado > precio nuevo), "Descuento topado". Cortesías listadas en $0 y jamás tocadas — verificado.
4. Selección con checkboxes actualiza contador y delta total en vivo; "Aplicar recálculo" (msConfirm) POST a `/impacto/{id}/aplicar` → flash "Se recalcularon N (diferencia total ±$X)" y `logs_acceso` tipo `recalcular_reservaciones_tarifa` — verificado (subset: solo la seleccionada cambió, header=detalle por PDO).
5. `descuento_total` INTACTO tras aplicar (solo se topa al nuevo subtotal); reservación cancelada entre previsualizar y confirmar → omitida sin tocar (suite).
6. Tarifa clase descuento o inactiva → pantalla "no aplica" con motivo — verificado.

## Lavandería (blancos → ciclo de lavado → pedido de huésped con cobro)

Verificado E2E en navegador ✅ 2026-07-18 (Los Cedros, sesión gerente; claro Y oscuro con escaneo de luminancia = 0 fugas). Suite: `LavanderiaTest.php` (55 asserts, incluye dinero idempotente vía Reflection y aislamiento multi-hotel).

1. `/lavanderia` (sidebar OPERACIÓN → Lavandería, badge = pedidos activos): con cero blancos muestra onboarding "Registrar mi primer blanco". Alta "Sábana king" cama, 10 piezas, mín 4 → tarjeta con celdas 10/0/0 y bitácora "Alta de piezas × 10" con usuario — verificado.
2. "Registrar movimiento" → Se ensució × 6 → celdas 4/6/0 y fichas stat en vivo — verificado. Exceso de stock → rechazado con mensaje.
3. `/lavanderia/lotes/nuevo`: radio Externo exige proveedor; input capado al stock sucio (max=6); preview chips + contador y submit deshabilitado en 0 — verificado. Enviar 4 → lote #N "En lavado", stock 2 sucias / 4 en lavado.
4. Detalle del lote → Recibir con 3 de 4 + costo $250 efectivo → stats 3 de vuelta / 1 merma, stock limpio 4→7, y **gasto directo a Caja** (verificar por PDO: `movimientos_caja` tipo gasto, ref `LAVLOTE-N`, categoría "Lavanderia", corte abierto; `lavanderia_lotes.gasto_movimiento_id` ligado) — verificado. Con caja cerrada queda "Gasto POR REGISTRAR" + botón de reintento en el detalle (cubierto por la suite, no por navegador).
5. `/lavanderia/pedidos` → "Precios": alta "Camisa lavada y planchada $45.50". `/lavanderia/pedidos/nuevo`: radio Huésped en casa lista reservaciones `checked_in` (huésped — hab.); el datalist autollena el precio del catálogo; total en vivo — verificado ($256.50, 4 piezas).
6. Detalle del pedido: timeline Recibido→En proceso→Listo→Entregado; "Empezar a lavar" y **Cobrar $X** (msConfirm) → verificar por PDO: ingreso ref `LAV-N` en corte abierto **y el hospedaje INTACTO** (`reservacion_pagos`/`reservacion_abonos` de la reserva vinculada sin cambios, cero CxC) — verificado (invariante crítico: el vínculo a reserva es solo informativo).
7. Cancelar pedido cobrado → rechazado con mensaje de devolución por Caja (suite). Doble cobro / doble gasto → un solo movimiento (suite).
8. Rol recepcionista (view+operar+cobrar sin `lavanderia.all`) — verificado por curl jul-18: `/lavanderia` 200 con botones de operar visibles y detalle de pedido accesible (role_id 29 con backfill).
9. Repoblado tras rechazo del servidor — verificado E2E jul-18: partida inválida forzada (cantidad 0 saltando el min) → redirect a /pedidos/nuevo con cliente, vínculo y AMBAS filas dinámicas repobladas (cantidad normalizada a 1) y total en vivo recalculado; el éxito limpia el borrador (form virgen en la siguiente visita). Mismo patrón en /lotes/nuevo (tipo, proveedor, notas y cantidades topadas al stock).

## Huéspedes: campo nacionalidad + reporte de procedencia internacional

Verificado E2E jul-2026 (localhost:8080, sesión QA en Los Cedros).

1. **Activar el campo**: `huespedes.campos_registro` (policy por hotel) → `nacionalidad.visible=true`. En prod se hace desde Configuración → Registro de huéspedes; en QA por PDO crudo sobre `hotel_configuracion` (la clase Database no conecta en CLI). Revertir al terminar.
2. **Alta** `/huespedes/create`: la sección **Procedencia** muestra un toggle "¿huésped extranjero?" que revela el campo Nacionalidad (`extras[nacionalidad]`). Assert JS: `[data-foreign-block]` existe, arranca `is-open=false`, al marcar el toggle agrega `is-open` (la clase es la fuente de verdad; el `max-height` computado miente por el renderer congelado del pane), al desmarcar LIMPIA el input. La nacionalidad NO debe duplicarse en "Datos adicionales" (`nacionalidadInDatosAdicionales=false`). Si el hotel la marca obligatoria, se muestra SIN toggle.
3. **Persistencia**: guardar → cae en `datos_extra_json.nacionalidad` por el colector genérico (sin tocar controller). `ver.php` la muestra por el loop de extras; `editar.php` la muestra en Procedencia como campo simple (sin toggle).
4. **Reporte** `/reportes/procedencia`: panel "Procedencia internacional" con KPIs (extranjeros, % del total, nacionalidades, ingresos) + ranking por nacionalidad. Con dato de prueba (huésped con reserva en rango + nacionalidad) el panel se puebla; sin extranjeros muestra estado vacío que invita a activar el campo. Query = `Reporte::obtenerProcedenciaExtranjeros()` (`JSON_EXTRACT`), agrupa correcto (verificado: 1 extranjero, 2 reservaciones, $14,400).
5. GOTCHA: la ruta del alta es `/huespedes/create` (no `/crear`). El screenshot del pane se cuelga → verificar por `javascript_tool` + asserts.

## Mensajes / canal WhatsApp — enlaces de "lo que falta" en la cola

Verificado en navegador jul-25 (localhost:8080, Los Cedros). El bloque `canal_whatsapp` NO está activo en la BD local: sin fila `hotel_modulos` (modulo_id 6749, `es_core=0`) toda la sección da 403 → activarla para la prueba y **borrar la fila al terminar** (si no existía antes, se borra; no se deja en `activo=0`).

1. **Sembrar la cola** (PDO crudo, anotar ids para el rollback): huésped con `telefono=NULL` + reservación `confirmada` sin filas en `reservacion_habitaciones` → tarjeta con DOS faltas; segundo huésped con teléfono válido y `fecha_entrada` = mañana → recordatorio (usa `{link_maps}`, que el hotel no tiene capturado → falta de configuración). GOTCHA de fechas: `CURDATE()` del PDO va en UTC y de noche adelanta un día respecto a la app → sembrar fechas LITERALES (`'2026-07-26'`) o el recordatorio nace como confirmación.
2. **Asserts de la cola** (`javascript_tool`, el screenshot del pane se cuelga): por cada `[data-msj-card]`, `[...card.querySelectorAll('.msj-falta')].map(f => ({tag: f.tagName, href: f.getAttribute('href')}))`. Esperado — teléfono → `/huespedes/{id}/edit#telefono`; sin habitación → `/reservaciones/editar-habitaciones/{id}` (solo en confirmación/recordatorio); configuración → `/mensajes/configuracion` (solo en el recordatorio, que es la plantilla que pide Maps). En la tarjeta con configuración incompleta **no debe existir** `[data-msj-enviar]`, y `#msjVacio.hidden === true` mientras haya tarjetas.
3. **Los destinos abren de verdad**: `/huespedes/{id}/edit#telefono` → `document.activeElement.id === 'telefono'` (el foco por hash vive al final de `huespedes/editar.php`); `/reservaciones/editar-habitaciones/{id}` → título "Modificar Habitaciones" sin aviso de permiso.
4. **Degradado por permiso**: entrar como **recepcionista** (`hu.rol='recepcionista'` con `role_id` NO nulo, hoy 29). Esperado: teléfono y habitación siguen siendo `A` (trae `huespedes.edit` y `reservaciones.edit`), la falta de configuración pasa a `SPAN.msj-falta.is-plain` ("Pídelo a gerencia") y el banner ámbar de arriba dice lo mismo sin enlace — recepción no tiene `mensajes.configurar` y enlazarla sería mandarla a un 403.
5. **Oscuro y móvil**: cargar ya en dark (`localStorage['medisoft:theme']='dark'` + `?nc=N`) → `.msj-falta` con fondo luminancia ~51 y texto ~177 (los tokens `--msj-*` los remapea cupertino.css). A 375px: `scrollWidth === clientWidth` y las faltas envuelven a ~55px de alto.

## Caja (cortes y movimientos)

1. `/caja` → abrir corte (solo puede existir UN corte abierto por caja — probar doble apertura debe fallar).
2. Registrar un gasto y un ingreso → verificar que caen en el corte abierto.
3. Cerrar corte → un movimiento posterior NO debe caer en el corte cerrado.
4. Cifras del corte: validar por PDO contra `Caja/Reportes` (deben cuadrar exacto).
5. **Tarjeta "Movimientos del turno"** (feed con cancelaciones, verificado jul-25 por asserts JS): sembrar en el corte abierto un par cobro↔`Devoluciones` (mismo monto/método/reservación) y un par `Pago laboral`↔`Reversion Pago laboral` (mismo `proveedor`), marcados con `comprobante='QA-FEED'` para poder borrarlos (sembrar por PDO desde el contenedor app: por `mysql` CLI los acentos salen mojibake y las horas se corren 6h — ver CEMENTERIO). Esperado: el turno abierto sale expandido y COMPLETO (nada de "últimos 10"), los turnos anteriores plegados (`<details>`) y anunciados como "N movimientos recientes"; el original va tachado con "Se canceló a las HH:MM", la cancelación dice "Devuelve/Recupera el movimiento de las HH:MM (Título)" y "Ver cuál" destaca la contraparte (abriendo el turno o el bloque plegado si hace falta); el reverso de nómina se pinta **Cancelación ámbar**, no Entrada verde. Asserts útiles: `.cj-turno` (2), `.cj-turno.is-current[open]`, `getComputedStyle('.cj-mov.is-anulado .cj-mov-amount').textDecorationLine === 'line-through'`, color de `.cj-mov-amount.is-cancelacion` = `rgb(180,83,9)` (claro) / `rgb(232,163,61)` (oscuro), y en dark 0 fugas claras escaneando luminancia dentro de `.cj-mov-list`. Con >20 movimientos en el turno aparece el botón "Ver los N anteriores" (baja `$cjVisiblesPorTurno` en la vista para probarlo con pocos datos y restaurar a 20). Limpieza: `DELETE FROM movimientos_caja WHERE comprobante='QA-FEED'` y confirmar que el conteo del corte vuelve a su valor previo.
6. **Libro `/caja/movimientos`** (verificado jul-25): sembrar un anticipo y un cobro CxC (`categoria_id = NULL`) y un par cobro↔devolución **en cortes distintos** (el cobro en un corte cerrado, la devolución en el abierto: así lo genera `Reservacion::cancelar`). Esperado: (a) **cero** filas con "Sin categoria" — el anticipo dice "Anticipo de reservación" y el cobro "Cobro de cuenta pendiente"; (b) la devolución dice "Devuelve el movimiento de las HH:MM (Pago de hospedaje)" aunque su original esté fuera del filtro; (c) al filtrar por la fecha del corte viejo, ese cobro sale **tachado** (`.cash-move-row.is-anulado`, `textDecorationLine: line-through`) con "Se canceló a las HH:MM" aunque la devolución quede fuera del rango; (d) el filtro de concepto trae el grupo "Movimientos del sistema" y `?categoria=txt:Devoluciones` deja solo devoluciones; (e) con un corte abierto, `?fecha_inicio=&fecha_fin=` YA cambia el listado (antes se forzaba el corte actual y los campos no hacían nada).
7. La suite pura del feed corre sin BD: `docker exec medisoft_hoteles_app php /var/www/html/tests/run.php --keep-db CajaFeed` (78 asserts).

## Nómina (v2)

1. Login `claude_qa1` en Los Cedros → `/nomina` (fachada: subnav fija, ficha con 4 tabs, toggles Pagos/Pre-nómina).
2. ⬜ receta completa de pre-nómina → período → revisión (reconstruir de la receta original de la fase NP al primer uso).
3. **PAGAR se hace en la pantalla VIEJA** (trabajadores/nomina), no en /nomina.
4. Invariante a verificar si se tocó crédito: crédito = bruto − ledger absorbido; anular/reabrir no debe permitir doble pago.

## Modo dueño

1. Login `dueno_qa` → `/dueno`.
2. Debe mostrar score determinista del hotel y ⬜ (secciones exactas del tablero).
3. Si se tocó push: verificar que las notificaciones ruteen por clave de rol `dueno_remoto`.

## Notificaciones push (activación automática por dispositivo)

1. Local (jul-25 en adelante): `notificaciones` es **paquete base** → ya NO hay que tocar `activo_global` (antes había que flipearlo y restaurarlo; hacerlo ahora deja el catálogo inconsistente y tumba el verificador). Sembrar sesión con endpoint temporal (incluir `csrf_token`+`csrf_token_time`). El usuario de QA necesita `notificaciones.view` o `/notificaciones` da 403 — es el gate nuevo, no una regresión. Si el hotel de QA tiene `notificaciones.pwa_push_activo='0'` (push opt-in), ponerlo en `1` para probar y devolverlo a `0`.
2. `/api/pwa-push/public-key` con `X-Requested-With` debe dar `enabled:true` y llave.
3. El pane no muestra prompts nativos de permiso (queda `denied`): stubear `window.Notification` (getter `permission` + `requestPermission`) y `Object.defineProperty(ServiceWorkerRegistration.prototype,'pushManager',...)` con subscribe/getSubscription falsos, y llamar `window.PWA.autoActivarPush('arranque'|'appinstalled')`. Verificado jul-25 (12 asserts): suscripción silenciosa con permiso dado (POST real 200, fila con hotel/usuario correctos), fast-path sin red con marcador, opt-out respetado, no pide permiso fuera de la app instalada, `appinstalled` pide y suscribe, guard por sesión, iOS (requestPermission lanza sin gesto) → `pointerup` dispara y el listener es de un solo uso.
4. Limpiar: borrar filas `pwa_push_subscriptions` con endpoint de QA, devolver `notificaciones.pwa_push_activo` a su valor previo, borrar el endpoint temporal (`activo_global` ya no se toca).
5. ⬜ prueba real en teléfono (Android instalado y iPhone agregado a inicio): abrir la app → aceptar el permiso → mandar prueba desde el panel de /notificaciones y ver la notificación llegar.
6. **Opt-in por hotel (verificado jul-25)** — sin navegador, 30 s. Endpoint temporal que acepte `?h=<hotel_id>` (setea `$_SESSION['hotel_id']`), y por cada hotel: `curl -s -c jar -b jar "…/tmp_sesion_push.php?h=N"` y luego `curl -s -c jar -b jar -H "X-Requested-With: XMLHttpRequest" …/api/pwa-push/public-key | grep -oE '"enabled":(true|false)'`. Esperado: `enabled` espeja `notificaciones.pwa_push_activo` del hotel. **Caso que importa: BORRAR la fila del hotel y repetir** → debe dar `enabled:false` (si da `true`, alguien devolvió el fallback de `PwaPushService::estaActivoParaHotel` a `true` y el push se enciende solo en cada hotel nuevo). Restaurar la fila re-corriendo la migración `20260725_001` (⚠️ re-correrla APAGA el push de cualquier hotel que lo tuviera encendido: su upsert fuerza `'0'` en todos). ✅ jul-25 tras el apagado total: los 5 hoteles → `enabled:false`, y con la fila borrada también `false`. Antes del apagado se comprobó el positivo: hotel con `'1'` → `enabled:true`.

## Copiloto (acciones por chat)

1. Abrir widget del Copiloto (exento del difuminador de sidebar).
2. Pedir una acción: "registra un gasto de 450 de plomería en Mantenimiento" (exige caja abierta) / "bloquea la 204 por pintura" / "desbloquea la 204" / "crea un cupón de 10% para agosto" / "págale 500 al proveedor X".
3. Debe responder con **propuesta** de campos fijos → confirmar → la ejecuta el motor de pantalla estándar (verificar el registro resultante igual que en la receta del módulo destino).
4. Rechazar una propuesta también es caso de prueba: no debe ejecutar nada.
5. Reservación campo a campo: "quiero hacer una reservación" → debe ir pidiendo fechas → habitación (rechaza ocupadas) → nombre → teléfono si es nuevo → confirmar registra al huésped y da botón al formulario prellenado. **Verificado en navegador (jul-2026)**: wizard completo con "mañana por 2 noches" → "cualquiera" → "sin nombre" → botón abre `reservaciones/crear` con fechas puestas en los inputs. ⬜ confirmar prellenado de habitación/huésped (mismo mecanismo de query) y "cancelar" a media captura desde el widget.
6. Multi-turno IA: pregunta abierta ("dame un consejo para temporada baja") y luego "¿y de esas ideas cuál primero?" → debe retomar el hilo (verificado por CLI contra el API; ⬜ confirmar desde el widget).
6b. Tool use IA: pregunta de rango arbitrario que NO case con intents — ojo: "caja"/"ganancias del mes" las atrapa el router determinista; usar algo como "del 1 al 10 de mayo, ¿el total que entró y el neto?" → fuente=ia con cifras EXACTAS de la BD (verificado por CLI jul-2026: llamó dinero_entre_fechas y respondió $5,500/$4,700 sembrados). También: "¿tengo lugar del X al Y?" (disponibilidad) y "¿quién es <huésped>?" (búsqueda).
7. Pregunta de datos a media captura ("¿cuánto tengo en caja?") → responde normal y el wizard sigue donde iba.
8. **Eliminar pregunta (✕ en la burbuja, hover en PC)** — verificado en navegador (jul-2026): borra pregunta+respuesta del hilo y, si era la última, restaura el contexto anterior — probado con el wizard: eliminar la respuesta de fechas regresa al paso de fechas y la siguiente frase se interpreta como fechas. Borrar el mensaje NO deshace acciones ya ejecutadas.
9. GOTCHA de QA por widget: los params JSON (`historial`, `flujo`) viajan por POST y el `sanitize()` global los rompía — si el wizard "cae a IA sin razón", revisar el `html_entity_decode` del controller antes de culpar al servicio.
10. **Catálogo y sugerencias** — verificado en navegador (jul-2026): el saludo muestra el catálogo paginado (flechas ‹ › ciclan páginas, dots, "Tus frecuentes" primero en Los Cedros, "Acciones rápidas"/"Dinero"/"Tus bloques" según bloques); un chip plantilla ("💸 Registrar un gasto") RELLENA el input con la orden a medias y enfoca (no envía); tras responder "¿cómo voy de caja?" aparecen 3 sugerencias del tema dinero. Caso negativo por test: propuestas de acción no traen sugerencias.
11. **Respuestas posibles del wizard (chips)** — verificado en navegador (jul-2026): cada paso muestra sus opciones tocables — fechas: Hoy/Mañana/Del…al…/Cancelar; habitación: los TIPOS reales del hotel (Sencilla/Doble/Cuádruple en Los Cedros, aparta una libre del tipo) + Cualquiera; **paso "¿registrado o nuevo?"**: "✅ Ya registrado" (→ pide el nombre y lo busca; si NO aparece ofrece "🆕 Registrarlo como nuevo" con ese nombre o corregirlo) / "🆕 Huésped nuevo" (→ el nombre va directo al alta) / "📝 Capturarlo en el formulario"; escribir el nombre directo en esa pregunta también funciona (delegación); teléfono: "Sin teléfono". Cancelar por chip corta el flujo. GOTCHA: el widget recorta sugerencias con slice — el tope debe ser ≥5 o el chip Cancelar desaparece.
12. **Feedback 👍/👎** — verificado en navegador (jul-2026): cada respuesta terminal trae los botones (fantasma, aparecen al hover); votar 👎 marca la fila del log (`util=0`, cotejado por PDO), re-votar 👍 la actualiza (`util=1`), la UI marca el activo y atenúa el otro. Propuestas de acción y pasos del wizard NO traen botones. % útil visible en `/copiloto/valor` (los 👎 = backlog de enseñanza). GOTCHA: la ruta `/copiloto/feedback` debió registrarse en `src/config/routes.php` (404 sin eso).
11b. **Panel móvil a buen tamaño** — verificado en navegador (jul-2026, viewport 390×844): al abrir el copiloto en ≤640px el panel mide ~358×658 (78dvh centrado con aire alrededor — el dueño rechazó el 92dvh casi pantalla completa por exagerado), mensajes .92rem e input 16px exactos (sin zoom iOS); en escritorio sigue siendo widget de esquina de 384px. Si vuelve a verse diminuto, revisar el choque de especificidad con `body.has-hotel-bottom-nav #cop-panel` (comentario en el bloque ≤640px del widget). Revisar también que la fila del footer (input+mic+Enviar) no desborde: el input lleva `min-width:0` — sin él, con fuente 16px su ancho intrínseco empuja al botón Enviar fuera del panel (overflow hidden lo corta, visto en teléfono real jul-2026).

## Áreas del hotel (habitaciones y áreas)

1. `/areas` (subnav Habitaciones · Áreas; sidebar dice "Habitaciones y áreas"). **Verificado en navegador (jul-2026)**: alta inline "Nueva área" (nombre+tipo catálogo+piso+descripción) → tarjeta con chip Disponible y KPIs por estado; Editar rellena el mismo form (hidden id); Pausar/Reactivar togglea `activa` (tarjeta opaca + chip "Pausada"). Nombre duplicado en el hotel debe rechazarse con mensaje digno.
2. Gates: ver = módulo `habitaciones`; gestionar = `can('habitaciones.edit')` (gerente/admin vía `habitaciones.all`; recepcionista NO ve botones de gestión — ⬜ confirmar con sesión recepcionista).
3. **Acciones por área (`/areas/{id}`, "Ver y operar")** — verificado en navegador (jul-2026): (a) Mandar a limpieza → chip "En limpieza" + tarea Pendiente en historial; (b) Marcar limpia SIN seleccionar personal ni "Sin registrar" → RECHAZADO (estado no cambia); (c) con trabajador → Disponible + tarea Completada + nota "Limpieza realizada por: X." + fila en `tarea_trabajadores`; (d) Reportar mantenimiento (motivo+tipo+prioridad) → "En mantenimiento" + registro `en_proceso` con `area_id` y `habitacion_id NULL`; (e) Finalizar → Disponible + completado con fecha_fin; (f) Cerrar → "Cerrada" (única acción Reabrir) → Reabrir → Disponible. Verificar filas por PDO en `tareas_operativas`/`mantenimientos_habitaciones` con `area_id`.
4. **Mapa digital (`/mapa`, pestaña Mapa del subnav)** — verificado en navegador (jul-2026, rediseño calco de /habitaciones): header glass con la subnav DENTRO (mismo cromado en las 4 vistas de la sección, incluida /habitaciones), fichas stat estilo `.hb-stat` que FILTRAN al tocar (atenúan lo que no es de ese estado, segundo toque limpia — probado con 4 ocupadas resaltadas/46 atenuadas), separadores de piso serif + hairline + conteo; ficha de habitación enlaza a `habitaciones/{id}` y la de área a `areas/{id}`; dots con pulso SOLO en estados vivos; en 390px sin overflow horizontal. Pestañas con `data-prefetch` (instant-nav) y `/areas`+`/mapa` excluidas de prerender (estado vivo, igual que habitaciones). El index de áreas tiene el mismo filtro por fichas (`arsFiltrar`).
5. **Copiloto: cerrar/reabrir área por chat** — verificado en navegador (jul-2026): "cierra la alberca" → propuesta con msConfirm "¿Cerrar el area?" → confirmar → "Listo ✅ … quedo cerrada" + deep-link "Ver el area" + `accion:cerrar_area_ok` en `copiloto_mensajes`; "reabre la alberca" revierte. Solo reclama la frase si un área real del hotel aparece en el texto (sin área → la cadena sigue normal). Casos negativos por test (AreasTest): cerrar dos veces avisa, área de otro hotel rechazada.
6. **Badge sidebar**: área en `limpieza`/`mantenimiento` suma a la burbuja de "Habitaciones y áreas" (verificado: 1 área en mantenimiento → badge "1"; cache de sesión 60s).
7. Suite dedicada: `AreasTest.php` (36 asserts: modelo, limpieza con personal, mantenimiento, copiloto, aislamiento multi-hotel).
8. GOTCHA de QA en el pane: si `screenshot` se cuelga (pasó incluso en portrait) verificar por `get_page_text` + asserts JS; si un clic físico no dispara `onclick`, usar `dispatchEvent(new MouseEvent('click'))` o `requestSubmit()` vía javascript_tool; para confirmar un msConfirm, forzar `classList.add('open')` en `.ms-cf-ov` antes del click (el pane throttlea rAF y sin la clase `open` el promise no resuelve).

## Habitaciones — fichas de estado del index (partición excluyente)

Verificado E2E en prod ✅ 2026-07-22 (Los Cedros vivo, con datos cambiando entre chequeos). Suite: `HabitacionLlegadasStatsTest.php`.

1. `/habitaciones`: las 5 fichas (Libres hoy · Ocupada · Por llegar · Limpieza · Mantenimiento) deben **sumar exactamente el Total** y coincidir 1:1 con el conteo de tarjetas del grid por `data-estado` (verificar con JS: `querySelectorAll('.flip-card').data-estado` vs números de ficha) — verificado (49 = 7+7+33+0+2, grid idéntico).
2. Clic en cada ficha/chip filtra EXACTAMENTE el número prometido (mismo criterio ficha=filtro=grid).
3. Franja `.hb-today-strip` arriba de las fichas: "Hoy: N reservas llegan · M cuartos por llegar (K aún en limpieza) · S salidas"; se oculta sin movimiento del día; el "(K aún en limpieza)" desaparece con K=0 — verificado.
4. Ficha Limpieza con llegadas pendientes muestra sub-etiqueta "K para llegadas de hoy" — cubierto por suite (en vivo quedó 0 al terminar las camaristas).
5. `/reservaciones`: metric "Llegadas hoy" agrega "· M cuartos" solo cuando M > reservas (reserva grupal) — verificado ("Llegadas hoy · 33 cuartos = 10").

## Habitaciones — index en MODO FECHA (proyección a otro día)

Verificado en local ✅ 2026-07-27 (Los Cedros, proyección al 01/08). Suite: `HabitacionFechaStatsTest.php`.

1. `/habitaciones?fecha_consulta=<futuro>&mostrar_disponibilidad=1`: la reservación que **entra** ese día sale con `data-estado="por_llegar"` (violeta, `--sheet-c: #8039D0`, badge "Por llegar"), NO como "Ocupada" — verificado (hab AMBAR, 01/08).
2. Las 5 fichas suman el Total y cada chip filtra exactamente su número (los 6: Todas 49 / Libres 48 / Ocupada 0 / Por llegar 1 / Limpieza 0 / Mantenimiento 0 → 49/48/0/1/0/0 tarjetas visibles) — verificado.
3. Ningún texto dice "hoy" al proyectar: fichas "Libres" + subs ("vendibles el 01/08", "estancias que vienen de antes", "entran el 01/08", "no aplica en otra fecha") y paneles "Check-ins 01/08/2026" sin el prefijo "Hoy," — verificado.
4. Reverso de la tarjeta de una llegada: nombre del huésped + "Llega ese día · falta recibirla" + rango de fechas + botón "Ver Reservación" — verificado.
5. Elegir **hoy** en el selector NO entra a la proyección: título "Habitaciones - …" (no "Disponibilidad …"), labels vuelven a "Libres hoy", tarjetas con estado físico (`disponible`/`ocupada`, sin sufijo `_fecha`), input con la fecha y en `is-active`; los números deben ser **idénticos** a `/habitaciones` sin parámetros — verificado (49/48/1/0/0/0 en ambas).
6. ✅ Check-outs en modo fecha (Ola 3, 2026-07-27): el panel ya consulta la fecha en vez de mentir "No hay check-outs programados". Verificado en 3 fechas de Los Cedros — futura con salida `confirmada` (02/08, hab AMBAR → "Aún no llega"), futura con `checked_in` (17/09, CORAL → "Hospedado") y **pasada** con `checked_out` (11/09, VINO → "Ya salió"). En las tres: `0` botones de acción (no se cierra hoy una salida de otro día).
7. Hoy sin regresión: `/habitaciones` sigue rotulando "Check-ins/outs **Hoy**, dd/mm/aaaa", fichas sin sub-etiquetas y labels "Libres hoy" — verificado. ⬜ El botón "Check-out" del panel de hoy NO quedó ejercitado en vivo (Los Cedros no tiene salidas hoy en local); su condición es `!$es_filtro_fecha && estado=='checked_in'`, y en la vista de hoy la bandera es false, así que se reduce a la condición original.
8. "Hasta HH:MM" del panel sale de `operacion.checkout_hora` (ya no fijo "12:00 PM") — verificado con el default del hotel.
9. ✅ Alertas "vencidas" no contaminan la proyección (2026-07-27). A/B con el mismo cuarto (MOKA, check-in atrasado 2 días hoy): en `/habitaciones` sigue con clase `has-checkin-vencido`, `--sheet-c: #D64539` y cara "SIN CHECK-IN · 2 días"; proyectado a 17/09 la MISMA tarjeta va limpia (`--sheet-c: #1E9E63`, badge "Disponible", "Sin huésped") y la vista entera reporta **0 tarjetas** con `has-checkin-vencido`/`has-checkout-vencido`. El panel de alertas se conserva en ambas, con `<small>` = "Toca para revisar el detalle" hoy vs "De hoy, no del 17/09" al proyectar, y el chip "1 sin check-in" intacto.
12. ✅ Fecha PASADA con eje "resultado de la noche" (2026-07-28, rediseño). En `?fecha_consulta=2026-07-25`: **4 fichas** en orden visual `Total 49 · Se rentaron 0 (0% de ocupación) · Sin ocupar 48 (esa noche) · Sin check-in 1 (quedó sin registrar)`; rejilla en 4 columnas; chips y leyenda móvil sin Limpieza ni Mantenimiento. Partición cuadra con el grid (48 `disponible_fecha` + 1 `por_llegar` = 49). Tarjeta: badge "Sin check-in", cara "Sin check-in · 25/07 – 26/07", **`.rc-price` vacío**. Sin toast. La ficha y el panel ya NO se contradicen (antes "Entraron 1" arriba vs "Sin check-in" abajo). **Sin regresión**: hoy conserva 6 fichas + precio + `hbReservarDesdeHabitacion(event, 19)`; futuro conserva 6 fichas, "vendibles el 01/08" y precarga `'2026-08-01'`. Suite `HabitacionFechaStatsTest` en 29 asserts.
11. ✅ Consulta de fecha PASADA (2026-07-27, probado en `?fecha_consulta=2026-07-25`, día con una reserva que entró). Todo en pasado: fichas "Sin ocupar 48 · esa noche" / "Entraron 1 · entraron el 25/07" / Limpieza "no queda registro hacia atrás" / Mantenimiento "estado de hoy, no el de ese día"; tarjeta badge "Entró", cara "Entró ese día", reverso del cuarto libre "No se ocupó esa noche". Chips y leyenda móvil en sintonía. **El botón Reservar manda `''`** (`hbReservarDesdeHabitacion(event, 19, '')`) → alta normal sin fecha pasada; en futuro sigue mandando la fecha (`'2026-08-01'`). Panel de check-ins con chip por renglón ("Sin check-in" para la que nunca llegó) y sin viñeta colgando cuando no hay hora. Contraste sin cambios. **Sin regresión en HOY**: labels "Libres hoy", sin barra de contexto, alertas desplegables.
10. ✅ Panel de alertas ESTÁTICO al proyectar (2026-07-27). En `/habitaciones?fecha_consulta=2026-09-17&mostrar_disponibilidad=1`: cabecera `DIV` (no `BUTTON`), sin `onclick`, `cursor:default`, **0** nodos `.hb-alerts-collapse`, **0** chevron, **0** filas `.hb-alerts-item[data-href]` — no se pueden resolver pendientes de hoy desde otra fecha. Conserva chips y suma el enlace "Volver a hoy" → `/habitaciones`. Contraste del enlace: 19.3 oscuro / 5.0 claro. En HOY sin regresión: `BUTTON`, chevron, desplegable presente, 1 fila accionable, abre al clic con `aria-expanded=true` y sin enlace "Volver a hoy". Medir con el pane en un ancho real (`resize_window` a 1440): a su ancho nativo (~638px) las media queries de móvil ocultan los `<em>` de los chips y el texto del enlace, y el `innerText` parece incompleto.

## Habitaciones — alta masiva (lote)

1. `/habitaciones/lote` (botón "En lote" en el listado y en el estado vacío de onboarding; gate `habitaciones.create`). **Verificado en navegador + PDO (jul-2026)**: define piso/tipo/precio comunes + rango `numero_desde`–`numero_hasta` (prefijo y relleno de ceros opcionales). El preview lateral en vivo lista los números en chips, muestra el conteo y el botón refleja el total ("Crear 20 habitaciones").
2. **Creación real**: enviar 901–905 (tipo sencilla, piso 3, $750) → 5 filas con capacidad/camas HEREDADAS del tipo (2 pers, 1 matrimonial), `activa=1`, y `control_llaves`=1 por cuarto (verificado por PDO scope hotel 1).
3. **Idempotencia**: reenviar 903–907 → crea solo 906–907, OMITE 903–905 activas (sin duplicados: `COUNT(*)` = 1 por número); un número dado de BAJA se REACTIVA (misma lógica que el alta individual). Resumen del toast: "Lote procesado: N creadas · M reactivadas · K omitidas (lista)".
4. **Validaciones** (client + server, mismo mensaje): rango invertido, tope 500 por lote, número >10 caracteres (prefijo+relleno), negativos → el botón se deshabilita en el preview y el server las rechaza al formulario. Helper puro `HabitacionController::generarNumerosLote` con suite `HabitacionLoteTest.php` (12 asserts).
5. Sin fotos ni características custom en el lote (eso es edición individual). ⬜ confirmar visual del preview en navegador real (el pane congelado no anima chips, se validó por `get_page_text`).

## Habitaciones — servicios incluidos en TODAS las habitaciones (configurables)

1. `/configuracion` → pestaña **Habitaciones** → caja "Servicios incluidos en todas las habitaciones" (arriba de Amenidades especiales). **Verificado E2E en navegador + PDO (jul-2026, Los Cedros)**: un hotel sin configurar muestra los 6 de fábrica (Wi-Fi, Cablevisión, Baño privado, Estacionamiento, Agua caliente, Ventilador) con su ícono; "Agregar servicio" clona la fila con índice nuevo, activo=sí y la muestra del ícono en gris (`fa-circle-plus`) hasta elegir uno.
2. Escribir un servicio nuevo dejando el ícono en **Automático** y guardar → se guarda con el ícono adivinado por el nombre ("Aire acondicionado" → `snowflake`, "Alberca climatizada" → `swimming-pool`, no `snowflake`). Apagar el switch de uno existente lo conserva en la config pero deja de mostrarse en la habitación. Borrar el nombre y guardar = quitarlo.
3. `/habitaciones/create` refleja exactamente los activos, en orden, con ícono y color; el placeholder del campo "Descripción completa de características" también los enumera. Sin ninguno configurado el bloque no se pinta (solo un aviso con enlace a Configuración, y ese enlace solo si el rol trae `configuracion.edit`).
4. **Descripción automática**: al guardar la habitación (individual y lote) la descripción termina con los servicios del catálogo, no con la lista fija vieja — verificado por Reflection sobre `HabitacionController::generarDescripcionCaracteristicas`: `"2 camas matrimoniales, Pantalla, Wi-Fi, Baño privado, Estacionamiento, Agua caliente, Ventilador, Aire acondicionado"`.
5. Contratos que vigila `HabitacionIncluidosTest.php` (46 asserts): ícono fuera del catálogo o con inyección (`wifi" onload=...`) se descarta; lista vacía GUARDADA se respeta (no revive la de fábrica); un POST de `room_catalog` sin la llave `included` no borra los servicios del hotel; duplicados por nombre avisan y no se guardan.
6. ⬜ confirmar visual (pane congelado: se validó por asserts JS y `get_page_text`).

## Limpieza / camaristas

1. Marcar habitación como limpia → debe EXIGIR quién limpió (personal obligatorio, contrato `personal_confirmado`/`trabajador_ids`/`sin_personal`).
9bis. ✅ Barra de contexto temporal `.hb-datebar` (2026-07-27). Solo existe con fecha ≠ hoy (verificado ausente en `/habitaciones`). Es `position:sticky; top:0; z-index:31` — **el scroller real es `main.main-content`, NO el window** (`window.scrollY` se queda en 0 y las mediciones salen falsas: usar `main.scrollTop`). Verificado: en reposo va en el flujo (top 74) y a partir de cualquier scroll queda clavada en top 0 (probado a 700 / 2000 / fondo). La topbar `.ms-vtb` NO llega a fijarse en este layout (se va con el scroll, `bottom` negativo), por eso la barra ancla en 0 y no descuenta su alto. Contraste en oscuro: título 10.0, botón 12.5 (AA de sobra). Móvil 375px: fecha corta, botón solo ícono, sin desborde. Candado `body.ms-modal-abierto` presente igual que el del header.
2. Programar limpieza con fecha+personal → verificar fila en `tareas_operativas`.
2b. **Áreas en el tablero (`/camarista`)** — verificado E2E en navegador + PDO (jul-2026): el tablero muestra secciones "Áreas por limpiar" (estado limpieza, tarjeta como cuarto) y "Áreas listas" (disponible, `<details>` colapsado) además de los cuartos; sólo áreas disponible/limpieza (mantenimiento/cerrada son de recepción). "Ya quedó limpia" en un área abre el MISMO modal de personal (subtítulo con el nombre del área, no "Hab") y postea a `/camarista/area/marcar/{id}` con estado=disponible → área a `disponible` + tarea `completada` con `area_id` y nota "Limpieza realizada por: X" + fila en `tarea_trabajadores` (reusa `completarLimpiezaConPersonalAreaParaHotel`). "Marcar por limpiar" en un área lista (msConfirm) → estado=limpieza + tarea pendiente (`iniciarLimpiezaAreaParaHotel`). GOTCHA del modal: `abrir()` usa `datos.actionBase` (del botón, sólo áreas) o el `data-action-base` del form (cuartos); el prefijo "Hab " ahora va en el `data-hab-numero` del botón, no hardcodeado en el modal.
3. **Modal sobre header en /habitaciones (≥768px)** — al abrir cualquier modal de la vista (limpieza, vista rápida…) el header candy sticky debe quedar DEBAJO del backdrop (difuminado como el resto), nunca flotando encima del modal. Verificado por asserts JS (jul-2026): `body.ms-modal-abierto` baja `#mainHeader` a z-index 0 + pointer-events none y restaura al cerrar. ⬜ confirmar visual en navegador real (el pane congelado no corre el fade del modal).
4. **Tareas manuales vinculadas (/tareas ↔ estado de la unidad)** — verificado E2E en navegador + suite (jul-2026): crear en `/tareas` una tarea de LIMPIEZA ligada a habitación o área disponible y "para ahora" (sin fecha futura) → la unidad pasa a `limpieza` al instante (evento "puesta en limpieza" en historial); completar/cancelar desde `/tareas` la libera SOLO si no queda otra tarea activa de limpieza sobre ella; programada a futuro no toca estado hasta "Iniciar"; ocupada jamás cambia; MANTENIMIENTO solo vincula y muestra (no bloquea — el bloqueo real sigue siendo el flujo de Mantenimiento); habitación y área son excluyentes en el form (JS limpia la otra + validación servidor). Suite: `TareasVinculadasTest.php` (24 asserts).
5. **Conexión /tareas ↔ /camarista ↔ /habitaciones** — corroborada E2E en navegador (jul-2026, tarea #113): las 3 superficies comparten `tareas_operativas`. Crear en /tareas limpieza + habitación + trabajador → el cuarto aparece "Por limpiar" en /camarista CON el nombre del asignado, y el modal "Ya quedó limpia" lo trae PRESELECCIONADO (`data-asignados` sale del pivote de la tarea); confirmar desde camarista → cuarto disponible Y la tarea de /tareas queda Completada con "Limpieza realizada por: X" (`completarLimpiezaConPersonalParaHotel` cierra cualquier tarea activa, origen manual incluido); "Programar" de camarista REUTILIZA la tarea manual activa (no duplica, `programarLimpiezaParaHotel`); el chip "N tareas" del index de habitaciones sale de `resumenActivoPorHabitacionesHotel` (misma tabla). Asimetría de diseño conocida: mandar un CUARTO a limpieza desde camarista/habitaciones/checkout NO crea tarea (el registro nace al marcarlo limpio); las ÁREAS sí la crean (`iniciarLimpiezaAreaParaHotel`).

6. **Check-out asigna responsable → /habitaciones lo trae preseleccionado** — verificado E2E en navegador + PDO (jul-2026): asignar responsable de limpieza al hacer check-out (selector opcional `CheckoutLimpieza`) SÍ persiste en `tareas_operativas`+`tarea_trabajadores` (confirmado por PDO: 4 cuartos de Los Cedros con `trabajador_id` y fila en el pivote). `liberarHabitacion()` en habitaciones/index.php ahora detecta esa preselección (`HB_LIMPIEZA_ASIGNADAS[id]`) y muestra confirmación de 1 clic ("¿Confirmas que X hizo la limpieza?" + botón "Cambiar personal" que abre el picker completo pre-marcado) en vez de repetir el selector obligatorio desde cero — probado con habitación UVA asignada a "oscar puto". ⬜ pendiente: badge de "asignada a" visible en el card sin abrir modal, y precarga per-cuarto en el modal masivo `.lm-staff` (hoy solo permite 1 persona para todo el lote).

## Mantenimiento Plus

1. Crear mantenimiento con fotos y costo → el costo debe generar gasto en caja.
2. Gotcha del validador en campos `*_costo`; mantenimiento sin habitación (área/activo) es válido (`habitacion_id` NULL).

## Modo oscuro (por vista)

1. Verificar SIEMPRE cargando la página ya en dark: `localStorage.setItem('medisoft:theme','dark')` + recargar `?nc=N` (togglear `data-theme` en vivo da valores falsos por el renderer congelado del pane — ver CLAUDE.md). Auditar elementos claros colados: escanear `querySelectorAll('*')` por luminancia `0.2126r+0.7152g+0.0722b>180` con alpha≥.5 dentro del wrapper de la vista.
2. **habitaciones/index** — verificado (jul-2026): las dos tarjetas de estado vacío (`#hbNoResults` "Sin resultados" con `#FBF8F2` inline, y `#hbEmptyState` "No se encontraron habitaciones" con `bg-white`/`bg-gray-*`) traían fondo claro fijo; remapeadas a `var(--brand-surface)` vía `<style>` scoped a `html[data-theme="dark"]` (no `-soft` que es negro en Cupertino). Quedan en #1C1C1E igual que el resto de paneles.
3. **usuarios/index** — verificado (jul-2026): los tokens propios `--user-*` son claros fijos → badge de conteo (`--user-accent-soft` 86% blanco) y botones de acción (`.act-btn{background:var(--user-surface-warm)!important}` que le ganaba a cupertino §35 sin `!important`) salían BLANCOS. Fix: bloque `html[data-theme="dark"] .usuarios-view{ --user-*: var(--brand-*) }` que remapea neutros de un golpe + ícono del header aclarado + badges rol/estado tintados oscuro conservando matiz + avatares al degradado de marca. Escaneo final = 0 elementos claros; modo claro intacto (reglas scoped a dark).
4. **habitaciones/create + habitaciones/lote** — verificado (jul-2026): ambas comparten el sistema visual `--room-*` (shell 1040px, hero acento, tarjetas cabecera-ícono). Modo oscuro **theme-agnóstico** (bloque inline `html[data-theme="dark"] .create-room-page/.lote-page`, aplica en Deleite Y Cupertino — la §30 de cupertino.css solo cubría /create en Cupertino): remapea tokens + oscurece superficies con color fijo (heads #161617, inputs/paper #1C1C1E, bordes #38383A, labels #D6D8D6, chips y purple-box tintados). GOTCHA que motivó el fix: textos con `color:#3f4743` INLINE no voltean (los 2 subheads de características y el span "Activar" → cambiados a `var(--room-ink)`). Escaneo por luminancia = 0 fugas claras en ambas (Los Cedros = Cupertino dark); modo claro intacto (card blanca, input flatten #F5F5F7). Form de /create sigue guardando E2E (habitación creada→detalle, verificada+borrada por PDO).
5. **inventario/nuevo** — verificado (jul-2026): reescrita al MISMO sistema `lote-*` (shell 1040px + hero + `.lote-card`/`__head`/`__body` + `.lote-field`/`.lote-row cols-2/3` + side sticky con Ayuda/Categorías + modo oscuro inline theme-agnóstico). Reusa clases `.lote-*` verbatim (CSS duplicado, patrón self-contained del repo) + extras `.inv-note`/`.inv-price`/`.inv-cat-item`; dejó MUERTA la §(~4003) de cupertino.css `.nuevo-producto-view` (sin riesgo, ya no matchea). Alta E2E OK (QATEST01 creado→/inventario, verificado+borrado por PDO); dark = 0 fugas; claro intacto. GOTCH​A al limpiar: el FK de movimientos es `movimientos_inventario.producto_id` (NO `inventario_movimientos`) — borrar movimientos antes que el producto.
6. **inventario/entrada + inventario/salida** — verificado (jul-2026): reescritas al sistema `lote-*` (form main + side con Vista previa dinámica + botón). Preview reescrito a clases tokenizadas (`.inv-pv-*`) en vez de utilidades tailwind para que voltee en dark; entrada tiene chips +10/+25/+50 (`.inv-qchip`), salida conserva validación de stock insuficiente (`.ms-form-invalid`, `#stock_error`, `validarStockSalida`) + preview `is-neg`/`is-down`. E2E OK: +3/−3 sobre Papel Higiénico (id 1) neto cero, movimientos ENTRADA/SALIDA registrados y luego borrados; dark = 0 fugas en ambas.
7. **inventario (índice)** — es LISTADO ancho boutique (NO se toca su layout ni se mete al shell 1040 de lote; su canvas ancho es deliberado). Ya tenía dark (dark-theme.css:538 remapea `--inv-*` + cupertino.css §2251-2299 literales). HUECO hallado y arreglado (jul-2026): el layout MÓVIL (≤639px, `.producto-card-mobile`) fija blanco con `!important` (@≤767px .78 y base .86) que el remap de tokens NO alcanza → 23 tarjetas blancas en dark. Fix theme-agnóstico en dark-theme.css:538 `html[data-theme="dark"] .inv-page .producto-card-mobile{ background:var(--inv-surface)!important; border-color:var(--inv-line)!important }` (specificity 0,3,1 gana). Reescaneo = 0 fugas; claro intacto. GOTCHA general: al auditar dark de un listado, escanear con el pane en ANCHO MÓVIL (el pane se queda ~638px) revela las reglas @móvil con blancos hardcodeados que el desktop oculta.

## Candado de un bloque a la venta (¿se puede cobrar?)

Recorrido de 2 minutos por bloque, ANTES de ponerlo `activo_global=1`. Un bloque sin candado se cobra pero no restringe nada (pasó con `pwa`, `tarifas_dinamicas` y `descuentos`).

1. `grep -rn "require_hotel_module('<clave>')" src/` → si da **0 coincidencias**, no es vendible: solo lo esconde el menú. Ojo: `hotel_menu_module_enabled` / `$menuModuloActivo` / la entrada de `navegacion.php` son visibilidad, NO enforcement.
2. Confirmar que TODAS las rutas del bloque (`src/config/routes.php`) las sirve un controller cuyo `before()` tenga el gate; si se reparten entre varios controllers, el gate va por acción.
3. Revisar el mapa `moduleForCurrentApiAction` de `ApiController` — la API es superficie aparte.
4. **Prueba real** con endpoint temporal de sesión que acepte `?h=<hotel_id>`: elegir un hotel que NO contrate el bloque y pedir su ruta principal por curl. Esperado **302/403**; un **200** = no vendible. ✅ jul-25: `/configuracion/tarifas` → hotel 1 (contrata) **200**, hotel 3 y hotel 8 (no contratan) **302**. Antes del fix los tres daban 200.
5. Bloque sin ruta propia (feature-flag dentro del paquete base, tipo `descuentos`): NO se prueba por HTTP, se caracteriza en suite — ver `GatesBloquesVentaTest` (16 asserts). GOTCHA del test: `hotel_active_module_keys` memoiza en un `static` por proceso, así que hay que usar DOS hoteles en vez de togglear uno.
6. **Fuga SATÉLITE (jul-29): que la PANTALLA dé 403 no significa que el bloque esté cerrado.** El candado de la pantalla es el primer paso; después hay que barrer los 4 tipos de superficie que quedan fuera y que ya dieron fugas reales: (a) **productores** en pantallas del paquete base — un flujo base que ESCRIBE datos del bloque (el check-in creaba `solicitudes_factura`, descontaba inventario y guardaba `vehiculos[]` sin mirar el módulo); (b) **exports** — `exportarPDF`/`exportarExcel` de Reservaciones imprimen marca/modelo/color del vehículo; (c) **el MOTOR** — las reglas guardadas se siguen aplicando desde ~20 consumidores aunque la pantalla de tarifas dé 403 (candado en `IncrementoTarifa::getIncrementosAplicables`, por CLASE); (d) **el mapa de `ApiController`** — la proyección de estacionamiento colgaba del módulo `dashboard`, que es base. Receta: `grep -rn "<tabla_del_bloque>" src/app` y clasificar cada hit en lectura/escritura/salida; los que vivan en pantallas base se GATEAN (dato no se pinta, campo se ignora), nunca 403. Suites de referencia: `GatesFacturacionInventarioTest` (22), `GatesEstacionamientoTest` (34), `GatesMotorTarifasTest` (17).
7. **GOTCHA que invalida cualquier QA local de un gate**: el catálogo de módulos de la BD de dev está ATRASADO respecto a prod (es dato editable desde el panel, no migración): en local `vehiculos`, `exportaciones` y `tablero_ejecutivo` tienen `activo_global=0` y en prod están abiertos y contratados. Consecuencia práctica: en local la función se ve APAGADA aunque el código esté bien, y al revés. Antes de concluir "esto se rompió", verificar el estado real: `SELECT clave,tipo_comercial,activo_global,precio_mensual FROM modulos WHERE clave='<clave>'` en local, y contra prod con la receta read-only por SSH (§Consultar BD de prod de CLAUDE.md). Para QA local de un bloque cerrado, abrirlo a mano (`UPDATE modulos SET activo_global=1 WHERE clave='<clave>'` + fila en `hotel_modulos`) y recordar que `hotel_active_module_keys` cachea 60s por APCu.

## Clasificación comercial de módulos / Panel SaaS (base·opcional·interno·bloqueado)

1. Catálogo real: `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tools/saas/verificar_clasificacion_comercial.php` → TODO PASS (25 checks: base exacto, internos, motivos, reportes individuales, planes, preset Básico). Ejecutado ✅ 2026-07-24.
2. `/admin/saas/hoteles/1#modulos` (sesión saas admin — en Surface `admin` user_id=1 es owner): bloque "Paquete básico" = 8 chips exactos (con "Roles y permisos"), sección "Funciones internas Medisoft" (internos con "· en pausa" los retirados), tabla de opcionales con checkbox `disabled` + badge "No disponible aún" + motivo en ámbar; cobro estimado ignora disabled (verificado ✅ 2026-07-24: $999 base + $299 solo Inventario). `/admin/saas/modulos`: mismas 3 secciones + estados Disponible/No disponible aún con motivo. ✅
3. Gates de reportes: `/reportes` con hotel sin reportes contratados muestra SOLO tarjeta(s) de Mantenimiento (base); URL directa `/reportes/procedencia` rebota a home; con un `reporte_*` activado global+contratado debe abrir SOLO su pantalla (probado en suite `ClasificacionComercialModulosTest`, "procedencia no abre ocupación"). ✅ 2026-07-24 (rebote en navegador; positivo por suite)
4. Configuración solo-Medisoft: `/configuracion` con saas admin ENTRA; con gerente del hotel (user 15 Rafael en Surface, no está en `saas_admins`) REBOTA a home y la sidebar NO muestra Configuración (Tarifas/Roles siguen siendo del hotel, controllers propios). ✅ 2026-07-24
5. `/api/sync` es POST-only: `fetch('/api/sync', {method:'POST', headers:{'X-Requested-With':'XMLHttpRequest'}})` con sesión → **423** `sync_temporarily_disabled` (GET da 404 de router y sin sesión 401 — no confundir con regresión). ✅ 2026-07-24
6. Smoke post-bloqueo masivo: dashboard, /habitaciones y /caja cargan sin 500 con gerente y el log del día queda limpio. ✅ 2026-07-24
7. ⬜ POST real de "Guardar bloques y cobro" en el panel (no se ejecutó para no alterar contrataciones del hotel QA; la lógica está cubierta por suite) y revisión visual humana del panel (los checks fueron por asserts JS).
8. **Reportes individuales a la venta (jul-28, migración `20260728_001`)** — los pasos 1-3 de arriba cambian de estado esperado: el verificador ahora exige los 5 `reporte_*` con `activo_global=1` + precio 49.00 + sin motivo (bloque 5 invertido), y en `/admin/saas/modulos` la sección "Reportes individuales" debe salir en **(0)** con el texto "No hay reportes individuales pendientes: los desbloqueados aparecen arriba, entre los disponibles", los 5 listados entre los Disponibles (categoría `analitica`, orden 801-805, precio editable) y el contador de bloqueados en **26**. Verificado ✅ 2026-07-28 por BD + `verificar_clasificacion_comercial.php` (TODO PASS) + `ClasificacionComercialModulosTest` (60) y `ReportesIndividualesGatesTest` (32); **cobro sin cambios en los 5 hoteles** (Los Cedros sigue $1,472: cero filas de `hotel_modulos` apuntaban a reportes). ⬜ FALTA en navegador: ver el panel con sesión saas admin y contratar un reporte a un hotel para comprobar que `/reportes/<pantalla>` abre solo esa (hoy el positivo está cubierto por suite, no por HTTP).

## Configuración de un hotel desde el Panel SaaS (`/admin/saas/hoteles/{id}/configuracion`)

Lo que se cuida: que la vista del hotel renderizada contra OTRO hotel no se equivoque de hotel al leer ni al guardar. Todo por curl con `-c jar -b jar` en cada request (la app regenera el session id).

1. Suite: `php src/tests/run.php SaasConfiguracionHotel` → **29 PASS** (jul-29: +5 por la barra inferior). Cubre normalización, tenancy del guardado, que la tabla legacy `configuracion` no se escriba, y que el catálogo de atajos del footer responda por el HOTEL OBJETIVO. ✅ 2026-07-29
1b. **Barra inferior desde el panel (jul-29, arreglado)**: abrir `/admin/saas/hoteles/{id}/configuracion` de un hotel que NO tenga un bloque con atajo propio (p.ej. `documentos` o `compras`) → en la sección de accesos rápidos ese atajo NO debe aparecer como opción. Antes aparecía y se GUARDABA, dejando en la barra del hotel un botón que su propio controlador rebota con 403. Assert por BD: `SELECT valor FROM hotel_configuracion WHERE clave='pwa.footer_shortcuts' AND hotel_id=<id>` (JSON) no debe contener claves de bloques sin contratar. ⬜ falta verlo en navegador (hoy cubierto por suite).
2. Render apuntado a otro hotel: GET `/admin/saas/hoteles/{otro}/configuracion` con sesión saas admin → 200 y `<title>` con el nombre del **hotel objetivo**, no el de la sesión. Las 3 secciones que el modo SaaS oculta NO deben existir en el HTML: `grep -c 'id="hc-appearance"'` / `hc-brand` / `hc-system` = 0, y `hc-catalogs`/`hc-rooms` = 1. ✅ 2026-07-29 (hotel 4 desde sesión del hotel 1)
3. Guardado real sin armar el POST a mano: el form tiene ~19 KB de campos y `hotel_config_normalize_editable_payload` valida TODOS los obligatorios juntos (un payload parcial devuelve 13 errores). Receta: guardar el HTML del GET y extraer los campos con un PHP temporal que lo lea con `DOMDocument` + XPath `//form[@id="configForm"]//input|select|textarea` (saltando checkbox/radio sin `checked` y los `type=file`), imprimirlos como querystring, cambiar UN valor distintivo y postear con `--data @archivo`. Esperado: **303** al mismo panel. ✅ 2026-07-29
4. Tenancy del guardado (el assert que importa): tras el POST, `SELECT hotel_id,clave,valor FROM hotel_configuracion WHERE clave='contacto.telefono' AND hotel_id IN (1,4)` → el valor nuevo SOLO en el hotel objetivo y el `updated_at` del otro hotel intacto; `SELECT COUNT(*) FROM configuracion` igual antes y después (esa tabla es GLOBAL, sin `hotel_id`). ✅ 2026-07-29
5. Gates con usuario del hotel (no `saas_admins`): GET `/configuracion` → 303 a dashboard; GET y **POST** `/admin/saas/hoteles/{id}/configuracion` → 302 a dashboard (probar el POST aparte: el GET gateado no prueba la escritura); y en cualquier vista del hotel `grep -c 'href="[^"]*/configuracion"'` = 0. ✅ 2026-07-29 (recepcionista user 16)
6. Degradación de los enlaces (gerente NO Medisoft, p.ej. user 15): `/dashboard` carga 200 con 0 enlaces a `/configuracion` (las 7 menciones de estacionamiento salen como `<span>`) y `/configuracion/roles` muestra "Volver al inicio". ✅ 2026-07-29
7. Sabido, NO es regresión de esta pantalla: la flecha y la miga de `view_topbar.php` en `/configuracion/roles` siguen apuntando a `/configuracion` (las deriva de la sección, `back_url($vtbRoot)`) → para el gerente rebotan al dashboard. ⬜ pendiente si se decide arreglarlo en el topbar genérico.

## Hotel SIN un bloque opcional: check-in y tablero — verificado ✅ 2026-07-29

Receta ejecutada tras el deploy de las 5 tandas, con el hotel **Demo de producción** (no tiene `facturacion` ni `vehiculos`) y reproducida en local. Montaje: contenedor hermano sirviendo el worktree contra la BD real (`docker run -d --name tmp_qafug --network medisoft-hoteles_default -p 127.0.0.1:8090:80 -e DB_HOST=db -e DB_NAME=medisoft_hoteles_import … -v <worktree>/src:/var/www/html`) + endpoint temporal de sesión con `?h=<hotel_id>` (borrado al terminar).

1. **Quitar/poner el bloque en local**: `UPDATE hotel_modulos SET activo=0 …` y **reiniciar el contenedor** (`docker restart`). GOTCHA que cuesta 10 min: `hotel_active_module_keys` cachea 60 s en APCu y `apcu_clear_cache()` por CLI **no** limpia el segmento de Apache → el bloque "no vuelve" y parece bug del código. ✅
2. **Check-in sin facturación** (`/reservaciones/ver/{id}` → modal): 0 radios `requiere_factura` en los 4 flujos, `.rv-final-invoice` ausente, y el paso 3 del stepper debe decir **"Confirmar"**, no "Factura" (regresión encontrada en este QA y corregida; el `aria-labelledby` apuntaba a un `<h4>` inexistente). ✅
2b. **LEER LA ETIQUETA DESPUÉS DE ABRIR EL MODAL, no en el HTML inicial** — `setCheckInWizardStep` la reescribe en cada cambio de etapa, así que un fix de PHP puede estar desplegado y la pantalla seguir diciendo "Factura" (pasó: lo reportó el owner con captura de prod cuando el commit ya estaba arriba). El assert correcto lee `#modalCheckIn [data-step-indicator] strong` en **tres** momentos: al cargar, tras `abrirModalCheckIn()` y ya en la etapa 3 — y hay que hacer el A/B con el bloque activo, donde debe seguir diciendo "Factura". El **segundo** stepper (wizard de anticipos, `[data-rda-ind]`) también tiene paso de factura: verificar los dos. ✅
3. **Trampa del efectivo (PREEXISTENTE, verificada A/B con el bloque activo)**: con `monto_efectivo` capturado y `recibido_efectivo` VACÍO, `calcularCambio` (ver.php ~§8100) deshabilita "Continuar" a propósito y el wizard se queda en el paso 2; el atajo **"Pagó exacto"** (`marcarEfectivoExacto`) lo destraba y avanza. Antes el aviso decía "El efectivo recibido no cubre el monto a cobrar" (suena a que falta dinero, con el resumen mostrando cobro completo y cambio $0.00) → ahora distingue vacío de insuficiente y nombra el atajo. Si se vuelve a reportar como "el check-in no avanza", es ESTE campo. ✅
4. **Tablero sin `vehiculos`**: 0 `<article … dashboard-parking-card>`, 0 `<div class="dm-card dm-park">`, 0 `#parkingForecast`, sin encabezado "Estacionamiento" huérfano y la sección `.dashboard-main-flow` NO queda vacía (conserva el grid3). GOTCHA de medición: `grep -c data-pf-strip` cuenta también la línea del **JS inline** (`querySelectorAll('[data-pf-strip]')`) → contar etiquetas (`<article[^>]*dashboard-parking-card`), no la cadena suelta, o parece que quedó una tira huérfana. ✅
5. **Tablero CON `vehiculos`** (el caso de Los Cedros y San Nicolás, que es donde importa no romper nada): tarjeta y bloque móvil presentes, 2 tiras `[data-pf-strip]` en el DOM y la API pinta **7 días**. ✅
6. **API de proyección**: hotel CON el bloque → **200** con `proyeccion[]`; hotel SIN el bloque → **403** `{"module":"vehiculos"}`. GOTCHA de datos: en local los 4 hoteles tienen fila `hotel_modulos.activo=1` histórica para `vehiculos`, así que abrir el bloque globalmente lo activa para TODOS — para probar el 403 hay que desactivar el contrato de ese hotel, no basta con elegir "otro hotel". ✅
7. ⬜ Falta: confirmar en el navegador de producción (hotel Demo) que el paso 3 ya dice "Confirmar" tras el deploy de estos dos arreglos.

## Panel SaaS → configuración de un hotel cliente — verificado ✅ 2026-07-29

Receta de la tanda 4 (avisos de bloque no contratado + hotel objetivo). Montaje: contenedor hermano sobre el worktree + endpoint temporal de sesión con `?h=` y el usuario que está en `saas_admins` (sin eso `/configuracion` y el panel rebotan).

1. **La prueba que importa es cruzada**: entrar a `/admin/saas/hoteles/2/configuracion` con la sesión apuntando al hotel **1**. Si aparece el chip de un bloque que el hotel 1 SÍ tiene y el 2 no (p.ej. Facturación), el predicado responde por el hotel objetivo. Si NO aparece, está leyendo `$_SESSION` y la fuga sigue viva. ✅
2. **A/B de chips** (local): hotel 2 → 17 chips (3 en campos de ajuste, 10 en filas de huésped, 2 en encabezados de grupo, 2 en catálogos) sobre 7 bloques; hotel 1 → 11 chips, desaparecen Facturación/Inventario/Documental y quedan los 4 bloqueados globalmente. ✅
3. **El POST no debe cambiar**: `document.querySelectorAll('#configForm [disabled]').length === 0` y el conteo de `[name^="hotel_config["]` igual en los dos hoteles (69). ✅
4. **Round-trip real** (lo único que prueba tenancy): apuntar un valor reconocible, `form.submit()` (nativo, salta el guard de app.js) y comparar en BD con marcas de tiempo, **no** con `MD5(GROUP_CONCAT(...))` — MySQL trunca GROUP_CONCAT a 1024 bytes y la huella sale idéntica aunque el contenido cambie. Con timestamps: hotel 2 = 60 filas tocadas + 8 creadas, hotel 1 = **0 y 0**, y `SELECT COUNT(*) FROM configuracion` (tabla legacy sin `hotel_id`) igual antes y después. ✅
5. Los vacíos que aparecen tras guardar (`contacto.email`, textos de políticas, destinatarios de reportes) son campos opcionales que el formulario envió vacíos — comportamiento normal, no efecto de los chips. Se distinguen por `updated_at`. ✅
6. ⬜ Falta: mirarlo en producción (panel de un hotel sin Facturación) para confirmar el chip en pantalla.

## Reputación — encuesta pública sin review gating (jul-26)

El módulo no tenía receta. Lo que se verifica es que la invitación a Google se ofrezca a TODO el que responde: filtrarla por calificación viola la política de Google Maps y arriesga el perfil del hotel.

1. Suite: `run.php ReputacionTest` → 28/28. Cubre las 5 calificaciones, sin URL configurada, alerta interna, independencia de umbrales, tenancy y respuesta repetida. ✅ 2026-07-26
2. **Prueba pública real, sin sesión** (la encuesta es anónima por token, no necesita login ni CSRF): en dev hay dos encuestas sembradas en Los Cedros — token `aabbccddeeff00112233445566778899` con **calificación 2** y `11223344556677889900aabbccddeeff` con 5.
   `curl -s "http://localhost:8080/h/los-cedros/encuesta/<token>" | grep -oE "(Escribir mi rese[^<]*|href=\"https://g\.page[^\"]*)"`
   Esperado en AMBAS: aparece el enlace y el botón. Si la de calificación 2 NO lo muestra, el gating volvió. ✅ 2026-07-26 (la de 2 estrellas sí lo muestra)
3. Requisito del paso 2: el hotel debe tener `reputacion.google_review_url` en `hotel_configuracion` (hotel 1 ya la tiene). Sin URL, lo correcto es que NO aparezca.
4. GOTCHA: el gating vivía en DOS lugares (servicio + vista). Tras el POST la vista relee la encuesta y evaluaba el umbral por su cuenta ⇒ verificar SIEMPRE por HTTP, no solo con la suite del servicio.
5. ⬜ Envío por correo: NO se prueba porque no funciona (no hay MTA en la imagen); la encuesta se comparte copiando el enlace.

## PWA / Offline — arranque sin internet (SW v25) — verificado ✅ 2026-07-30 (18 asserts, falta prueba en teléfono)

**Cómo probar un service worker SIN navegador** (el pane no corta la red ni reproduce el arranque de una PWA instalada): cargar el `service-worker.js` REAL con `new Function('self','caches','fetch','console', src)` y pasarle mocks — `self` (registration.scope, location.origin, addEventListener, clients), un CacheStorage falso con `open/match/keys/put/delete` (respetar `ignoreSearch`, que el SW usa) y un `fetch` que LANZA para simular sin red. Luego se dispara `listeners.fetch[0]({request, respondWith, waitUntil})` con un `Request` que lleve `accept: text/html` (el SW acepta navegación por `mode` o por ese header) y se leen las respuestas. Node 24 ya trae `Request`/`Response`/`URL` globales. Mismo truco para `offline.html`: extraer su 2º `<script>` con regex y ejecutarlo con un `document` mínimo (getElementById/createElement/appendChild) — evita jsdom, que no está instalado. Arneses versionados en `tools/tests_js/`: `node tools/tests_js/sw_arranque.test.js` (8) y `node tools/tests_js/offline_puente.test.js` (10), desde la RAÍZ del repo (leen `src/...` por ruta relativa). No hay PHP ni npm de por medio: corren con el `node` del host.

1. Ruteo del SW: extraer `OFFLINE_PAGE_PATHS`/`OFFLINE_ARRANQUE_PATHS` del archivo con regex y evaluarlas (no reescribirlas a mano, o pruebas una copia). Contrato: `''`/`login`/`h/{slug}/login` → arranque; `dashboard`, `caja/movimientos`, `offline/pendientes` → operativa; `reportes`, `loginfalso`, `h/{slug}/reservar` → ninguna. El handler evalúa **operativa primero**.
2. Sin red + arranque + dashboard guardado → devuelve el dashboard, NO `offline.html`. Este es el bug original.
3. Sin red + arranque + caché vacío → muro. Sin red + `/habitaciones` con su copia → SU copia, no el dashboard. Sin red + `/reportes` → muro (no suplantar: mentiría sobre la URL).
4. **Seguridad**: tras `CLEAR_PAGES_CACHE` (logout / cambio de hotel) el arranque sin red debe dar el muro, jamás la sesión anterior.
5. Con red: `/dashboard` se guarda en `loscedros-pages`; el login **nunca** se guarda (son credenciales y casi siempre un redirect).
6. `offline.html`: con pantallas guardadas revela los accesos con la URL real del caché y cambia el copy; sin nada guardado no inventa accesos; cuenta la cola (`operaciones_offline`, `estado !== 'sincronizado'`) en singular/plural; sin IndexedDB no truena. **Asertar el LENGUAJE, no solo que aparezca**: el offline es de solo lectura (ver la sección siguiente) → ningún texto puede decir "se enviará al volver el internet"; debe mandar a registrarlo a mano. Es la regresión fácil de reintroducir sin darse cuenta — pasó en esta misma sesión y lo cazó el assert.
7. Manifest: `curl <host>/h/<slug>/manifest.webmanifest` → `start_url` debe ser `/dashboard` y el `id` seguir siendo `/h/{slug}` (si el `id` cambia, la PWA se reinstala como app nueva y el hotelero pierde el ícono).
8. ⬜ prueba real en iPhone: instalar, abrir con red (para que guarde copia), modo avión, abrir desde el ícono → debe entrar al dashboard, no al muro. **Ojo**: una PWA ya instalada conserva su `start_url` viejo hasta reinstalarse — ese caso lo cubre el fallback del SW y es justo lo que hay que ver en el teléfono.

## Captura offline apagada (escrituras que no llegaban al servidor)

Desde jul-26 la app NO acepta capturas sin conexión (ver CLAUDE.md). Verificar que el candado sigue puesto:

1. Flag en el navegador: entrar con sesión y `curl … | grep -oE "MEDISOFT_OFFLINE_(ENABLED|ESCRITURAS) = [a-z]+"` → esperado `ENABLED = true` y **`ESCRITURAS = false`**. ✅ 2026-07-26. GOTCHA: `/login` es standalone y NO incluye header.php → ahí el grep sale vacío y parece que el flag no existe; probar en `/dashboard` con sesión.
2. Candado duro: `window.OfflineData.encolarOperacion('x',{})` desde la consola debe **rechazar** con `OFFLINE_ESCRITURAS_DESHABILITADAS`.
3. Simular corte de red (DevTools → Offline) e intentar un movimiento de caja: debe salir "Sin conexión … no quedó guardado", NUNCA "se enviará al volver internet". Idem check-in/out en Habitaciones y alta de huésped.
4. En Reservaciones offline no deben pintarse los botones de check-in/check-out en las tarjetas.
5. La cola vieja NO se borra: si un dispositivo tiene pendientes, al sincronizar recibe 423 y un aviso para capturarlas a mano. `/offline/pendientes` debe seguir listándolas.
6. ⬜ Prueba en dispositivo real de Los Cedros (revisar si hay capturas atoradas de antes del apagado — son la única copia de esos datos).

## Dinero E2E (regresión global)

- Suite: `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tests/run.php` (recrea la BD de prueba; filtrar por nombre para iterar rápido).
- Referencia histórica: la verificación E2E de dinero del ciclo jul-2026 cerró con 22 PASS — si algo de dinero baja de ahí, es regresión.

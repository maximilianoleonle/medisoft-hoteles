# CLAUDE.md — Mapa vivo del sistema (LEER SIEMPRE, se inyecta solo)

Medisoft Hoteles: SaaS hotelero white-label multihotel. PHP 8.2 puro (MVC propio) + MySQL 8 + Tailwind precompilado + jQuery. Sin framework. Idioma de trabajo: español.

## ⚡ REGLA DE ALIMENTACIÓN (obligatoria)

Este archivo es un **documento vivo**. Al cerrar cualquier tarea, si descubriste algo que le hubiera ahorrado tokens/tiempo a la siguiente sesión (una ruta no obvia, un gotcha, un comando, un atajo, un contrato), **agrégalo aquí en la sección que corresponda, en la misma sesión**, como UNA línea densa. Si una entrada resultó falsa u obsoleta, corrígela o bórrala. No agregues prosa ni entradas derivables del código en 10 segundos. Este archivo viaja por git a las 2 PCs (principal + Surface).

Gatillo automático: un hook de Stop (`.claude/hooks/alimentar-docs.sh`) recuerda esta regla UNA vez por sesión cuando el repo tiene cambios — al recibirlo, alimenta lo que aplique o declara que no hubo nada.

Documentos hermanos (misma regla de alimentación, se leen **bajo demanda**):
- `CEMENTERIO.md` — enfoques ya intentados que fallaron. Leerlo ANTES de atacar un problema conocido; enterrar ahí cada callejón sin salida nuevo.
- `docs/QA-RECETAS.md` — flujos de verificación por módulo. Leer la receta del módulo tocado tras cada cambio; completar los ⬜ al ejecutarlos.

## Entorno local

- App: Docker → http://localhost:8080 · phpMyAdmin: 8081 · MySQL expuesto: 3307 (user `medisoft_user`/`medisoft_pass`).
- **BD real de trabajo: `medisoft_hoteles_import`** (no `medisoft_hoteles` que dice el README).
- **GOTCHA jul-25 — Docker Desktop no arranca** con "An unexpected error occurred … initializing Inference manager … remove `…\Docker\run\dockerInference`: The file cannot be accessed by the system": sockets AF_UNIX HUÉRFANOS de un cierre sucio en `%LOCALAPPDATA%\Docker\run\` (`dockerInference`, `dockerEthernetVfkit`, `userAnalyticsOtlpHttp.sock` — length 0, reparse points, fecha vieja). No es el stack del proyecto: es Docker AI/Model Runner (`EnableDockerAI` en `%APPDATA%\Docker\settings-store.json`), que el proyecto NO usa y aun así tumba TODO el arranque. **NO PIERDAS TIEMPO BORRÁNDOLOS**: `Remove-Item`, prefijo `\\?\`, `del` de cmd y rename fallan los 4 con "El sistema no tiene acceso al archivo", incluso tras `wsl --shutdown` y con cero procesos Docker vivos — el kernel los retiene hasta el próximo boot (probado jul-25). Fix REAL sin reiniciar: matar procesos (`Get-Process '*docker*' | Stop-Process -Force`) + poner `"EnableDockerAI":false` por reemplazo TEXTUAL en settings-store.json (no re-serializar el JSON) + relanzar → daemon listo en 10s CON los sockets huérfanos aún presentes. **MATIZ jul-31: el remedio funciona pero TARDA — no lo declares muerto.** Si el huérfano que bloquea es el del **Secrets Engine** (`docker-secrets-engine\engine.sock`), Docker Desktop puede morirse solo a los segundos del primer lanzamiento (procesos en cero) y aun así levantar **~20 min después sin intervención**. Aplicar el remedio UNA vez y esperar en segundo plano (`until docker ps >/dev/null 2>&1; do sleep 5; done`), NO repetir el ciclo ni reiniciar la PC de entrada (detalle y cómo leer cada estado por la respuesta de `docker ps`, en CEMENTERIO §Entorno). La causa real SIEMPRE está en `%LOCALAPPDATA%\Docker\log\host\com.docker.backend.exe.log`, no en el diálogo. **JAMÁS el botón "Reset to factory defaults"** de ese diálogo: borra los volúmenes = adiós BD local `medisoft_hoteles_import`.
- Docker Desktop está instalado **por usuario** en `%LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe` (v4.83), NO en `C:\Program Files\Docker\` — un `Start-Process` a la ruta clásica falla silencioso (argumento vacío) y parece que "no levanta". Para arrancarlo desde CLI: el exe NO está en `Program Files` sino en `%LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe` — en esa misma carpeta vive `Docker Desktop Installer.exe`, que abre el instalador (no lo lances por error); tras `Start-Process` el daemon tarda ~1-2 min en aceptar `docker ps`.
- Complemento jul-25 al gotcha de arriba: el mismo cierre sucio deja un SEGUNDO socket huérfano, `%LOCALAPPDATA%\docker-secrets-engine\engine.sock` ("starting services: initializing Secrets Engine"), que sale apenas se destraba el de Inference — y aunque los ARCHIVOS socket no se dejan borrar ni renombrar, la CARPETA padre SÍ (`Rename-Item %LOCALAPPDATA%\Docker\run run_stale_N`, idem `docker-secrets-engine`) y Docker recrea la suya limpia. Con las dos carpetas renombradas + `EnableDockerAI:false` el daemon arrancó; no quedó claro cuál de los dos destrabó, así que aplicar ambos y no gastar tiempo en discriminar.
- Cada PC tiene su BD local independiente: tras merge, correr migraciones a mano (`php src/tools/migrate.php`).
- Hotel semilla/QA: **Los Cedros** (`hotel_id=1`, slug `los-cedros`). Usuarios QA: `claude_qa1` (nómina/general, `usuarios.id=43`), `dueno_qa` (modo dueño). GOTCHA jul-18: esos ids son de la PC principal — en la **Surface** NO existen; ahí usar `admin` (`user_id=1`, gerente, `hotel_usuario=['id'=>1,'rol'=>'gerente','role_id'=>15]`); verificar ids reales con un SELECT antes de armar el endpoint de sesión. GOTCHA jul-27: en la PC principal `usuarios.id=43` YA NO EXISTE (deriva de datos locales — SELECT vacío); fallback probado que sí funciona en cualquier PC: `usuarios.id=1` ("Administrador Principal", `hotel_usuarios.rol='gerente'`, `role_id=15`) con `hotel_usuario=['id'=>1,'rol'=>'gerente','role_id'=>15]`. No asumir el id de memoria: `SELECT hu.id,hu.usuario_id,hu.rol,hu.role_id,u.nombre_completo FROM hotel_usuarios hu JOIN usuarios u ON u.id=hu.usuario_id WHERE hu.hotel_id=1 AND u.activo=1 LIMIT 5` primero.
- Verificar cifras de dinero SIEMPRE vía PDO (script PHP), nunca mysql CLI directo: el timezone de sesión difiere y no cuadra con la app.
- **QA en navegador (Browser pane)**: no hay dev-login. Para entrar con sesión, crear endpoint temporal en `src/public_html/` que haga `session_start()` + setee `$_SESSION` (`user_id=43`, `hotel_id=1`, `hotel_slug='los-cedros'`, `hotel_usuario=['id'=>34,'rol'=>'gerente']`), navegar a él y **borrarlo al terminar**. GOTCHA jul-17: `.htaccess` ahora bloquea con 403 los patrones `qa_*`, `__qa_*`, `debug_*` (y el CI falla si se versionan en public_html) → nombrar el endpoint temporal fuera de esos patrones (ej. `tmp_sesion_smoke.php`); los 4 endpoints QA viejos viven en `_local_quarantine_sensitive/qa_endpoints_2026-07-17/`. GOTCHA visor: el app tapa viewport horizontal con overlay "Modo vertical requerido" y el screenshot del pane se cuelga en landscape → usar viewport **portrait** (390×844) o desktop alto (1280×900); si aun así `screenshot` se cuelga, verificar por `get_page_text`+asserts JS, y si el clic físico no dispara `onclick`, usar `dispatchEvent`/`requestSubmit` vía javascript_tool. GOTCHA jul-26 (corrige lo anterior): en formularios con guard de app.js —`#configForm` de /configuracion es el caso probado— `requestSubmit()` NO envía y NO avisa: la página se queda igual, `checkValidity()` da true y el handler hace preventDefault en silencio (se ve como "no pasó nada"); usar `form.submit()` (nativo, salta handlers) para QA de guardado. GOTCHA msConfirm en el pane: el visor throttlea `requestAnimationFrame` → el overlay nunca recibe la clase `open` y `settle()` no resuelve el promise → antes del click sintético forzar `document.querySelector('.ms-cf-ov').classList.add('open')`. Con el renderer congelado las transiciones CSS/rAF NO avanzan: los modales con fade quedan en `opacity:0` y los detectores por visibilidad (modal-sidebar-fix.js → `ms-modal-abierto`) nunca disparan — verificar CSS dependiente de esas clases toggleándolas a mano con JS; además `resize_window` puede no aplicar (innerWidth se queda en el ancho físico del pane, ~638px = layout móvil). El `.htaccess` cachea la vista (bfcache): recargar con query `?nc=N` para ver cambios. GOTCHA curl con sesión QA: la app REGENERA el session id en la primera petición autenticada → todo curl debe llevar `-c jar -b jar` en CADA request (solo `-b` = la cookie vieja muere y la sesión "desaparece"); el navegador no sufre esto. GOTCHA jul-27 — un `background` que "no aplica" en el pane y SÍ aplica en un navegador real: si la regla incluye `transition: … background …`, el renderer congelado del visor deja la transición a MEDIAS y `getComputedStyle` reporta el fondo INICIAL (`rgba(0,0,0,0)`) para siempre; el `color` de la misma regla sí aplica, así que parece "solo el fondo está roto" y se va media sesión buscando un `!important` que no existe (escaneé 7,279 reglas: cero). Señal inequívoca: ni un `element.style.background` INLINE lo cambia, pero un elemento NUEVO con el mismo inline sí. Antes de declarar bug de CSS, medir con `el.style.transition='none'` y releer el computado. GOTCHA verificar MODO OSCURO: togglear `data-theme="dark"` por JS EN VIVO da valores computados FALSOS (renderer congelado → `.usr-tr` reporta `rgb(255,255,255)` aunque la regla ganadora sea oscura); los custom properties sí se recalculan pero los `background`/paint no → medir SIEMPRE cargando la página ya en dark: `localStorage.setItem('medisoft:theme','dark')` y recargar `?nc=N`. Para auditar qué elementos quedan claros, escanear `view.querySelectorAll('*')` por luminancia `0.2126r+0.7152g+0.0722b>180` con alpha≥.5. GOTCHA jul-23: un `color-mix()` computa como `color(srgb 0.93 0.92 0.89)` con componentes **0–1**, no 0–255 → si el escáner no multiplica por 255 en ese formato, TODO color-mix claro pasa como oscuro y el reporte da "0 fugas" falso (así se coló un chip dorado casi blanco). Reserva útil para check-in: **#32869** (confirmada, $2,000, Los Cedros). **PLAN B jul-31 cuando NO hay navegador (Docker caído + pane colgado + extensión de Chrome desconectada, los tres a la vez): Chrome headless del host** — `"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu --virtual-time-budget=3000 --window-size=W,H --screenshot=X.png file:///...` (el PNG se LEE con la tool Read) y `--dump-dom` para leer asserts numéricos que un `<script>` del propio banco escribe en el DOM (delimitar con marcas tipo `@@RES@@` y `sed -n '/@@RES@@/,/@@FIN@@/p'`). El banco se arma sin PHP: `sed -n '<ini>,<fin>p' vista.php` extrae el `<style>` REAL (verificar después con `diff` que el CSS medido es idéntico al desplegado) + marcado copiado a mano + shim mínimo (`*{box-sizing:border-box}` y `.fas{display:inline-block;width:1em}`, que sin el bundle FA los íconos miden 0). Sirve para todo bug de layout/desborde y da números, no impresiones. GOTCHA que da FALSO VERDE: hay que reproducir el ancho de la app (viewport − sidebar ≈ 373px) **y** el layout de DOS columnas — con `--window-size` chico el `@media` colapsa `.guest-layout` a 1 columna, sobra espacio y el bug DESAPARECE de la captura (pasó: la primera captura salió "sin bug" con el defecto vivo).

## Mapa del código

- `src/app/controllers/` → `XController.php`; ruta `/seccion/accion` → `SeccionController::accion()`; vistas en `src/app/views/<seccion>/`.
- `src/app/models/` modelos · `src/app/services/` servicios (AnticipoService, etc.) · `src/app/helpers/functions.php` = helpers globales (archivo gigante: leer quirúrgico con Grep, jamás completo).
- Config global (permisos.php del RBAC, etc.) vive en `src/config/`, NO en `src/app/config/`.
- `src/app/views/partials/` componentes compartidos: `filtros.php` (barras filtro boutique), `confirm.php` (msConfirm), `back_arrow.php`, `view_topbar.php` (inyectado desde header.php a TODAS las vistas), `section_subnav.php`.
- **Flechas de regreso = retroceso REAL de historial** (jul-22): interceptor global al final de header.php — si el href de la flecha (`back_url()` PHP) coincide con `document.referrer`, hace `history.back()` en vez de navegar adelante (navegar al referer crea otra entrada y las flechas quedan en ping-pong A↔B). Con fallback (forms/misma página/sin historial) navega normal al href. Una flecha de regreso NUEVA debe llevar una de las 3 clases que cubre el selector: `ms-vtb-back`, `ms-back` o `ms-back-legacy`, o queda fuera del fix.
- Layout: `src/app/views/layout/` (header.php, footer.php, sidebar.php).
- CSS: `src/public_html/css/` (tailwind.css generado, dark-theme.css, cupertino.css) · JS: `src/public_html/js/` (app.js, sidebar-rail.js, instant-nav.js, modal-sidebar-fix.js).
- Copiloto IA: `CopilotoController` + `CopilotoIaController` + widget `views/layout/copiloto_widget.php`. Acción nueva por chat = 3 puntos: `detectarAccion*`+`ejecutar*` en CopilotoService, campos en `ejecutarAccion()` del widget, whitelist en `accionAction` del controller. Parsers de frases = estáticos puros públicos con test propio (`parsearGasto/Cupon/PagoProveedor/FechasReserva`, `sanearHistorial/Flujo`). Wizard de reservación: campo/flag nuevo del estado = 2 puntos o se PIERDE en el round-trip (`flujoReservaVacio()` + whitelist de `sanearFlujo`); las respuestas posibles de cada paso viajan por `sugerencias` desde `preguntaFlujoReserva()` y el `slice()` del widget debe ser ≥5 (o corta el chip Cancelar). Tool use IA: herramienta nueva = `herramientasIa()` (schema) + case en `ejecutarHerramientaIa()` (whitelist primero; solo lectura, hotel amarrado en servidor); el loop agéntico vive en `llamarClaude()` (máx 4 rondas, thinking intacto en el turno assistant). Al probarlo, esquivar keywords del router determinista ("caja") o nunca llega a la IA. GOTCHA: endpoint nuevo del copiloto exige registrar la ruta en `src/config/routes.php` (el router NO resuelve por convención: 404 silencioso). Feedback 👍/👎: `mid` viaja en respuestas terminales (via `conSugerencias`), `marcarFeedback()` con scope de hotel; % útil en /copiloto/valor.
- **Mensajes / canal WhatsApp** (`/mensajes`, jul-25): la cola no solo se queja, ENLAZA — el helper `$msjFaltasDe()` de `views/mensajes/index.php` arma por tarjeta las faltas con el link que las arregla: teléfono → `/huespedes/{id}/edit#telefono` (gate `huespedes.edit`, si no `huespedes.view` → ficha), sin habitación → `/reservaciones/editar-habitaciones/{id}` SOLO en confirmacion/recordatorio (esa pantalla exige estado `confirmada`; gate `reservaciones.edit`), datos del hotel → `/mensajes/configuracion` (gate `mensajes.configurar`). Sin el permiso el aviso se degrada a `<span class="msj-falta is-plain">` en vez de enlazar a un 403 (recepción NO trae `mensajes.configurar`) — mismo criterio en el banner ámbar de arriba. El deep link exige `huesped_id` en el item (`CanalWhatsAppService::armarItem`, aserto en `CanalWhatsAppTest`) y el foco automático en el campo lo hace un bloque al final de `huespedes/editar.php` que lee `location.hash` (el input necesita `id` + clase `.ge-control`). Los tokens `--msj-*` ya voltean en oscuro desde cupertino.css: una falta nueva que use `--msj-warning*` no agrega deuda de dark (fuga clara restante en la vista: `.msj-tabs`, blanco fijo).
- Áreas del hotel (alberca, lobby…): `AreaController` + modelo `Area` + vistas `views/areas/` (index, ver = acciones+historial, mapa = `/mapa` habitaciones+áreas por piso); estilos compartidos en `views/areas/_estilos.php` (tokens `--ax-*` CALCO del boutique `--hb-*` de habitaciones: header SIN franja (transparente, sin border/shadow/glass — se pidió quitar el recuadro blanco jul-2026), fichas stat filtrables, separadores de piso); viven DENTRO del módulo `habitaciones` (mismos gates, jamás claves `areas.*` nuevas por el gotcha can_legacy); subnav compartida (`section_subnav.php` sección 'habitaciones', Mapa·Habitaciones·Áreas) vive DENTRO del header glass de las 4 vistas y sus tabs llevan `data-prefetch` (instant-nav las prefetchea; /areas y /mapa excluidas de prerender por estado vivo); `area_id` nullable en tareas_operativas/mantenimientos_habitaciones/activos_hotel; limpieza de área = métodos `*AreaParaHotel` de TareaOperativa, mantenimiento = `iniciar/finalizarParaAreaHotel` de MantenimientoService. El tablero `/camarista` también lista áreas (secciones "Áreas por limpiar"/"Áreas listas", sólo disponible/limpieza) reusando el modal de personal; `CamaristaController::marcarArea` → `/camarista/area/marcar/{id}`; el modal elige endpoint por `data-action-base` del botón (áreas) o del form (cuartos). Tareas manuales (/tareas) vinculan habitación O área (excluyentes) y las de LIMPIEZA sincronizan estado: crear "para ahora"/iniciar → unidad disponible pasa a `limpieza` (`reflejarLimpiezaAlActivar`), completar/cancelar la libera si no queda otra tarea activa (`liberarLimpiezaAlCerrar`); mantenimiento solo vincula, no bloquea; suite `TareasVinculadasTest.php`.
- **Lavandería** (bloque `lavanderia`, jul-18): `LavanderiaController` + modelos `LavanderiaBlanco/Lote/Pedido/Servicio` + vistas `views/lavanderia/` (tokens `--lvx-*` calco de `--ax-*` PERO con dark inline de fábrica; forms boutique `lote-*` con lienzo neutro). 3 pestañas subnav propia (rama 'lavanderia' en section_subnav): Blancos (stock limpio/sucio/en lavado + ledger `lavanderia_movimientos`), Ciclos de lavado (lote FOR UPDATE mueve sucio→proceso→limpio con merma; locks de blancos SIEMPRE ordenados por id), Pedidos de huésped (partidas con precio congelado, catálogo `lavanderia_servicios` con datalist). DINERO: cobro pedido = `MovimientoCaja` ingreso ref `LAV-{id}` / gasto lote = ref `LAVLOTE-{id}`, ambos con categoría lazy 'Lavanderia' e idempotencia `cobro/gasto_movimiento_id` (calco mantenimiento; caja cerrada = "por registrar", jamás truena); JAMÁS abonos/CxC ligados a la reserva NI `reservacion_id` en el movimiento de caja (`reservacion_id` del pedido vive SOLO en la tabla del módulo — AnticipoService rechaza cargos si saldo=0, una CxC con reservacion_id contamina resumenPagos, y en `movimientos_caja` ese campo es CONTABLE: `Reservacion::cancelar` DEVUELVE todos los ingresos ligados, Facturación los lista como pago del hospedaje y el corte los clasifica como hospedaje — hallazgo ALTA de la revisión adversarial jul-18, con assert de regresión en la suite). RBAC `lavanderia.view/operar/cobrar/all` (backfill 20260718_001 a roles existentes, presets al día). Suite `LavanderiaTest.php` (55 asserts, cobra vía Reflection de los métodos privados del controller). Copiloto: herramienta IA read-only `estado_lavanderia` (stock+lotes+pedidos; gateada por módulo, cubierta en `CopilotoIaHerramientasTest`).
- **Tarifas → recálculo de reservaciones existentes** (jul-22): checkbox "Revisar reservaciones existentes" en tarifas crear/editar (solo clase incremento; JS lo oculta con descuento) → `procesarCreacion/Edicion` redirige a `/configuracion/tarifas/impacto/{id}` (GET previsualiza, POST `/aplicar` confirma; rutas en routes.php, gate `tarifas.edit`). Motor en `TarifaImpactoService`: candidatas = confirmada/checked_in con noches por venir (`GREATEST(salida, entrada+1) > hoy`, fechas PHP no CURDATE), estancia solapada a vigencia y ≥1 habitación cobrable en alcance; re-deriva precio noche×motor (`calcularPrecioConIncremento`) desde `precio_base` actual, cortesías intactas, `descuento_total` se CONSERVA (solo topado), guard #1867 = flag `cuadra_detalle` (header vs Σ detalle−descuento; aplicar sincroniza ambos re-derivando, jamás desde el detalle viejo); aplicar = 1 transacción, FOR UPDATE por reservación orden asc + re-check estado, log `recalcular_reservaciones_tarifa`. Saldos vía `resumenPagosLote`. Suite `TarifaImpactoTest.php` (48).
- Alta MASIVA de habitaciones (`/habitaciones/lote` → `loteAction`/`guardarLoteAction`, rutas en routes.php): genera un bloque por atributo común + rango (piso/tipo/precio comunes, `numero_desde`–`numero_hasta` + `prefijo` + `ancho` de relleno). Numeración = helper PURO estático `HabitacionController::generarNumerosLote(prefijo,desde,hasta,ancho=0,max=500)` con suite `HabitacionLoteTest.php` (lanza InvalidArgumentException; el controller la traduce a aviso). Reusa validador/defaults del alta individual (capacidad/camas se heredan del tipo vía `defaultsCapacidadCamas`), TODO en UNA transacción; número activo se OMITE, dado de baja se REACTIVA (como el alta individual), `crearControlLlave` por cada uno; resumen "N creadas · M reactivadas · K omitidas". Sin fotos (eso es individual). Vista `views/habitaciones/lote.php` con preview en vivo (chips+conteo). Accesos en index (botón "En lote"/"Crear en lote") y estado vacío de onboarding.
- **Formulario boutique `lote-*`** (patrón de "haz lo mismo que /habitaciones/lote"): sistema visual con tokens `--room-*` — shell centrado `max-width:1040px`, hero con acento lateral (`::before` border-left gold), tarjetas `.lote-card`>`.lote-card__head`(ícono+h2)+`.lote-card__body`, campos `.lote-field`+`.lote-row.cols-2/3`, side sticky. Incluye **modo oscuro theme-agnóstico** inline (`html[data-theme="dark"] .lote-page{...}`, paleta #1C1C1E/#161617/#38383A/#F5F5F7 — cubre Deleite Y Cupertino, la §30 de cupertino.css solo cubre Cupertino). Vive HOY en `habitaciones/lote.php`, `habitaciones/crear.php` (clases `.create-room-*`, mismo sistema), `inventario/nuevo.php` (clases `.lote-*` verbatim). Al replicarlo: copiar el `<style>` (patrón self-contained del repo, se duplica), y GOTCHA textos con `color:#xxx` INLINE no voltean en dark → usar `var(--room-ink)`/`var(--room-muted)`. Verificar dark cargando ya en dark + escaneo por luminancia (0 fugas claras).
- **Catálogos de habitación por hotel** (`helpers/hotel_config.php`, UI en `/configuracion#hc-rooms`): tipos (`catalogos.habitacion_tipos`), pisos, amenidades especiales y —jul-25— **servicios incluidos en TODAS las habitaciones** (`catalogos.habitacion_incluidos`, antes 6 ítems hardcodeados en `habitaciones/crear.php`). Contratos del nuevo: el hotelero solo escribe NOMBRE + ícono + activo (el `codigo` sale de un slug del nombre, `hotel_room_catalog_included_slug`, y dedupea); el ícono se adivina del nombre (`hotel_room_included_icon_guess`, gana la palabra que aparece ANTES en el texto — "Alberca climatizada" es alberca, no clima) y el override sale de la lista CERRADA `hotel_room_included_icon_catalog()` (icono FA6 + color; `hotel_room_included_icon_normalize` rechaza cualquier otra cosa: se pinta dentro de `class="fas fa-X"`); `[]` GUARDADO = el hotel no incluye nada (solo la ausencia de la llave cae a `..._default_included_rows()`), y `normalize_payload`/`save_values` tocan `included` SOLO si la llave viaja en el POST (un form viejo no borra la lista). Consumidores a mantener en sintonía: `habitaciones/crear.php` (bloque + placeholder del textarea) y `HabitacionController::generarDescripcionCaracteristicas` (ya no anexa lista fija ni el hack "TV normal"; alimenta store, lote y update vía `construirDescripcionCaracteristicas`). Suite `HabitacionIncluidosTest.php` (46). Las filas nuevas del panel usan la clase genérica de 3 columnas `hc-catalog-row is-simple` (`is-amenity`/`is-floor` comparten regla en 6 bloques CSS: no inventar clase nueva).
- Campos de huésped activables: catálogo `hotel_guest_field_catalog()` (helpers/hotel_config.php) + policy por hotel `huespedes.campos_registro` (visible/obligatorio). Storage `column` = columna real, HARDCODEADO en `HuespedController` store/update (agregar un campo columna = tocar controller + migración); storage `extra` = va a `datos_extra_json`, capturado GENÉRICAMENTE por `hotel_guest_collect_extra_values` → **un campo extra nuevo NO toca el controller ni el schema**. El campo `nacionalidad` (extra, etiqueta UI "País de origen") se captura en la sección **Procedencia** de crear.php con un toggle client-only "¿huésped extranjero?" (revela+limpia el select de países de `ms_select_paises()`; al cerrar dispara `change` para repintar el combo y al abrir enfoca `.ms-combo__trigger`, no el select oculto; si el campo es obligatorio se muestra sin toggle) y se EXCLUYE del loop genérico 'Datos adicionales' (crear+editar, filtro `ARRAY_FILTER_USE_BOTH`); ver.php lo muestra por el loop de extras. El reporte `/reportes/procedencia` lo lee vía `Reporte::obtenerProcedenciaExtranjeros()` (`JSON_EXTRACT(datos_extra_json,'$.nacionalidad')`) → panel "Procedencia internacional" (KPIs nacional/extranjero + top nacionalidades; extranjeros ⊆ totalHuespedes de porEstado). GOTCHA scripts CLI: la clase `Database` NO conecta fuera de web (lee env del front controller) → los one-off usan PDO crudo con `$cfg = require config/database.php`.
- **Caja → tarjeta "Movimientos del turno"** (jul-25, antes "Últimos movimientos"): la arma el presentador PURO `services/CajaMovimientosFeed.php` (suite `CajaFeedTest.php`, 70 asserts, sin BD) — `clasificar()` traduce categoría cruda→título humano + contexto (refs estructuradas; concepto libre del hotel ⇒ manda la descripción del usuario; `detalle_extra` = texto original solo si aporta, lo usa el libro), `emparejar()` liga cada cancelación con lo que anula y `agrupar()` devuelve grupos por corte listos para pintar. TRES estados, no dos: entrada/salida/**cancelación** (ámbar `--cj-amber`, incluye los reversos que son INGRESO — `Reversion Pago laboral/proveedor` — que antes se leían como venta). Emparejado por evidencia (no hay FK): referencia `REV-`+la del original (CxC/CxP) > misma reservación/proveedor + monto + método > misma reservación; solo contra movimientos ANTERIORES, sin reusar original, y JAMÁS toca totales (el dinero lo sigue derivando `Caja::obtenerResumenCaja`; el aviso de cabecera se alimenta de `$cash_reversos_total`, el mismo del KPI "Devuelto/cancelado"). Alcance: `MovimientoCaja::obtenerFeedDelTurno($corteId, $previos=12, $tope=400)` trae el corte abierto COMPLETO (el viejo `obtenerUltimosMovimientos(10)` escondía media jornada) + cola de turnos previos (necesaria para emparejar devoluciones de un corte cerrado); la vista pinta 20 y pliega el resto (`$cjVisiblesPorTurno`, botón "Ver los N anteriores"), turnos anteriores en `<details>` plegado y anunciados como "N movimientos recientes" (es una cola, no su total) con enlace a `/caja/corte/{id}`. `cj_finance_label/sentence` de la vista ahora delegan en el presentador (una sola fuente). El libro `/caja/movimientos` usa la misma lectura (título humano + "Cancelación" + fila ámbar + tachado) y SÍ empareja: como viene filtrado, `CajaController::cancelacionesParaEmparejar` trae las dos mitades invisibles — cancelaciones posteriores (`obtenerCancelacionesEntre`, +60 días) y originales anteriores de las cancelaciones visibles (`obtenerCandidatosOriginales`, mismo monto, −90 días) — marcadas `solo_para_emparejar` (marcan, no se pintan ni se suman). Ese libro también dejó de forzar el corte abierto cuando hay filtro de fecha (antes los campos desde/hasta no hacían NADA con caja abierta) y su filtro de concepto ahora incluye los del sistema como `txt:<nombre>` (`conceptosDelSistema()` + filtro `categoria_texto`), que antes eran infiltrables. GOTCHA de datos: hay ~22 categorías `Devoluciones` duplicadas con `tipo` VACÍO en Los Cedros (secuela del bug de enum 'egreso'), inofensivas para el feed pero ensucian el catálogo de conceptos.
- **"Sin categoría" en Caja = movimiento de un SERVICIO** (jul-25): anticipos, cobros CxC, pagos a personal/proveedor y TODOS los reversos se insertan con `categoria_id = NULL` y el nombre SOLO en el texto `movimientos_caja.categoria` (los servicios no tocan `categorias_movimientos`); cualquier vista que leyera nada más el JOIN `cm.nombre` los pintaba "Sin categoria". Arreglado en dos capas: `obtenerMovimientosDetallados` devuelve `COALESCE(cm.nombre, NULLIF(TRIM(mc.categoria),''))` (igual que ya hacía `Caja::obtenerMovimientosPorCategoria`) y toda pantalla resuelve el nombre con `CajaMovimientosFeed::concepto($mov)` (catálogo → texto del sistema → "Sin concepto"), ya aplicado en movimientos/corte/ver_corte y el modal de detalle. Al agregar una vista que muestre conceptos de caja: usar ese helper, nunca `categoria_nombre` a secas.
- **`categorias_movimientos` NO tiene `updated_at` → `$timestamps = false` (jul-31, 500 en prod ref #c0b81680)**: crear un concepto desde `/caja/categorias` y desactivar uno CON movimientos daban 500 (`SQLSTATE[42S22] 1054 Unknown column 'updated_at'`) porque `Model::create()/update()` agregan `created_at`+`updated_at` cuando `$timestamps` (default **true** en el Model base) y esa tabla solo tiene `created_at` (con `DEFAULT CURRENT_TIMESTAMP`, así que quitarlo del INSERT no pierde nada). Fix = `protected $timestamps = false` en `CategoriaMovimiento`; NO se agregó la columna (nada la lee). Auditado el resto: de 39 tablas sin `updated_at`, ésta era la ÚNICA cuyo modelo llama `create()/update()` del base — antes de culpar al Model base, correr esa comparación (tabla del `$table` vs `CREATE TABLE` de schema.sql). **El error era INVISIBLE y por eso llegó como "no me erró el modal"**: `Swal.fire(…)` sale en z-index 1060 y `.cash-category-modal` está en **13000**, así que el aviso quedaba TAPADO y solo aparecía al cerrar el modal a mano. Arreglar SOLO el camino de error no alcanzó y el owner lo reportó otra vez: el Swal de **éxito** seguía tapado, guardaba bien y el botón se quedaba en "Guardando..." para siempre. Fix definitivo en TRES capas: (a) la vista sube `.swal2-container` a **13500** (SweetAlert2 v11 lo declara con `div:where(.swal2-container)` = especificidad CERO, así que una regla de clase le gana sin `!important` siquiera), (b) al guardar bien se llama `cerrarModalCategoria()` ANTES del Swal, (c) los errores van INLINE en `#categoriaFormError`, que además conserva lo capturado. Regla general: **antes de meter un Swal en una pantalla, comparar su 1060 contra el z-index del contenedor**; y para verificar que un aviso se ve, `document.elementFromPoint()` sobre su centro — medir `z-index`/`classList` da verde con el elemento tapado. El submit además desactiva el botón (sin eso el usuario reclickea y salen dos 500 seguidos en el log). `crearCategoria` ya devuelve `message` además de `errores` (antes un duplicado mostraba un genérico). Suite `CajaConceptosTest.php` (28). **EN PRODUCCIÓN desde 2026-07-31** (2 archivos; respaldo `backups/pre_deploy_conceptos_20260731_210131.tar`). GOTCHA de método que valió la sesión: la vista real es `views/caja/ConfiguracióndeCategorías.php` (con acentos) y `categorias.php` es un stub de 2 líneas que la incluye — diffear `categorias.php` contra origin da "idéntico" y esconde que la vista de verdad estaba **280 líneas atrás**; comparar SIEMPRE el archivo que se va a desplegar.
- **El autoloader WEB no cubre `app/services/`** (solo `core/`, `app/controllers/`, `app/models/` — ver `public_html/index.php`), pero `tests/bootstrap.php` SÍ lo cubre: un servicio nuevo usado desde una vista pasa la suite en verde y truena con 500 `Class "X" not found` en el navegador. Cargarlo con `require_once __DIR__ . '/../services/X.php'` en el controlador que renderiza la vista (patrón ya usado en CajaController con ReporteEntregaService/NotificacionService).
- **Cotizaciones PDF** (jul-22): DOS acciones en `ReservacionController` — `cotizacionReservacionPdfAction` (desde ver.php) y `cotizacionPdfAction` (form crear, ~línea 4700), comparten los helpers `cotizacionPdf*`; `cotizacionPdfBranding()` INTERCAMBIA primario↔secundario a propósito (pedido del owner: header/tabla/caja total = `color_primary`, franja = `color_secondary`) y expone tokens ADAPTATIVOS por luminancia — `header_text_rgb` (texto sobre el fondo de marca: tabla y cajas de total lo usan, NUNCA blanco fijo), `header_accent_rgb` (dorado solo si el fondo es oscuro) y `accent_ink_rgb` (acento para montos/noches/términos sobre claro; si AMBOS colores de marca son claros cae a #374151) — hallazgo de revisión adversarial: con paletas claras tipo Los Cedros (#84B3D7/#F9FFF0) el blanco/dorado fijo quedaba ilegible. Ambas pintan la columna "LLEGADA ESTIMADA" en la caja de estancia (reservación: `hora_llegada_estimada` TIME; crear: POST `hora_llegada`; sin hora → "Por confirmar") y columna PERS. GOTCHA QA por curl: el CSRF global de `core/Router` bloquea TODO POST → el endpoint temporal de sesión debe sembrar también `$_SESSION['csrf_token']`+`csrf_token_time` (y `last_activity`) y el curl mandar `csrf_token` en el body; verificar el PDF inflando sus streams con `gzuncompress` (texto + colores como `x.xxx x.xxx x.xxx rg`, RGB/255 a 3 decimales; el primer `rg` es el header).
- **Cotizaciones PDF — bug PRECIO/NOCHE con tarifa dinámica (jul-27, corregido)**: en `cotizacionReservacionPdfAction`, la columna PRECIO/NOCHE pintaba `$hab['precio_base']` CRUDO (sin el incremento de `IncrementoTarifa::calcularPrecioConIncremento`) mientras TOTAL sí sumaba el precio ya incrementado noche por noche → con 1 noche el owner veía p.ej. $800/noche pero TOTAL $1,000 (25% de tarifa dinámica activa), inconsistencia real reportada por captura de pantalla. Fix: `$precioPorNoche = $precioTotalHab / $noches` (promedio ya incrementado), igual a como YA lo hacía `cotizacionPdfAction` (línea `precio_por_noche` derivado en `ReservacionController::calcularPrecioMultiple` ~línea 2717) — un solo cálculo se había quedado atrás. Verificado insertando una fila temporal en `incrementos_tarifas` (25%, `alcance='tipo_habitacion'`) y regenerando el PDF real vía curl+gzuncompress: PRECIO/NOCHE y TOTAL ahora coinciden ($1,250 = $1,250). El bloque de vehículos/placas (retirado jul-22) se REINTRODUJO el mismo jul-27 a pedido del owner: helper `cotizacionPdfVehiculos($huespedId,$hotelId)` (usa `HuespedVehiculo::porHuespedHotel`) arma una línea por vehículo SOLO con los campos que el huésped llenó (marca+modelo, placas, color, tipo si existe en `datos_extra_json['vehiculo_tipo']`) — nunca placeholders vacíos. La caja "DATOS DEL HUÉSPED" ahora es de alto DINÁMICO (`$huespedBoxH = 18 + 5*N`) y todo lo de abajo (estancia, tabla, términos) ya usaba ese offset como variable en `cotizacionReservacionPdfAction`, así que cascadeó solo; en `cotizacionPdfAction` el offset estaba HARDCODEADO (`$boxY+24`) y hubo que volverlo variable también — si se agrega una 3ª acción de cotización algún día, mismo patrón.
- **Ticket térmico de reservación** (jul-21): vive en `views/reservaciones/ver.php` (`imprimirTicketTermico` + `ticketData`, ~§9150). Fuente de verdad del dinero = `resumenPagos` (total/pagado/saldo/cxc_cobros) + `abonos` (anticipos) + `pagos` (el controller carga `reservacion_pagos` en TODO estado, no solo checked_in — antes el ticket post-checkout perdía el desglose); JAMÁS inventar un pago con `reservaciones.metodo_pago` (único fallback: checked_out legacy sin filas de pago). Título dinámico (COMPROBANTE DE RESERVACIÓN / PAGO PARCIAL / PAGO) y renglón grande = `SALDO PENDIENTE` o `TOTAL PAGADO`. Los datos se interpolan en HTML del ticket → escapar con `tEsc` (huésped/teléfono/método). Verificación: interceptar `window.open` con un fake `document.write` desde javascript_tool y assertear sobre el HTML capturado.
- **Proyección de estacionamiento** (jul-22): la tarjeta del dashboard proyecta vehículos a días futuros. `EstacionamientoProyeccionService` (estáticos puros `proyectarDia`/`clasificarNivel` con suite propia; regla: `reservaciones.vehiculos_estimados` NOT NULL manda (0 = "no trae" y silencia registrados), NULL cae a `huesped_vehiculos` activos por área, excedente del estimado va al área sintética `por_confirmar`; dedup por huésped/día) + endpoint `/api/dashboard/estacionamiento-proyeccion` (ruta en routes.php Y mapa módulo en `moduleForCurrentApiAction` — ambos o 404/403) + UI `.parking-forecast` en dashboard/index.php (tira 7 días con clic→detalle + date input fecha específica; el JS pinta TODOS los `[data-pf-strip]` — desktop y móvil comparten render; dark = overrides en dark-theme.css junto a las demás reglas `parking-*`, los `color-mix ... #FFFFFF` NO voltean solos). El select "¿Trae vehículo?" de reservaciones/crear.php se RETIRÓ el jul-29 a pedido del owner (redundante con los vehículos de la ficha del huésped): la columna `reservaciones.vehiculos_estimados`, el contrato del controller (ausente=NULL, ~línea 4700) y la regla del servicio siguen VIVOS — sin el campo, toda alta nueva guarda NULL y la proyección cae a `huesped_vehiculos`; los valores históricos se respetan. No reintroducir el select sin pasar por el owner. FIX de paso: el "ocupado" de HOY de la tarjeta cuenta checked_in que CUBREN hoy (antes exigía entrada=hoy y un hospedado multi-noche desaparecía del conteo al segundo día).
- **Matriz de permisos: el "control total" del área es un interruptor, no una casilla más** (jul-23, `roles/form.php` + `roles/permisos_persona.php`): el permiso `tipo=wildcard` de cada grupo (`<modulo>.all`) se saca de la lista con el helper compartido `rf_separar_total()` y se pinta arriba como bloque propio (`.rf-total`/`.pp-total` + separador "o elige punto por punto" / "Todo esto ya va incluido"); puesto, los hijos van `checked disabled` al 55% y el botón "Todos" se esconde. **Deshabilitado = NO viaja en el POST**, a propósito: el rol se guarda con la clave total sola en vez de repetir hijos. Dos contratos que eso obliga: (a) `Rol::derivarAjustes` usa `permission_in_list` para QUITAR (el total cubre a sus hijos, o poner el interruptor los mandaría a `quitados` y la persona perdería el módulo) e `in_array` literal para DAR (así `extra` guarda solo `<modulo>.all`); (b) `RolController::actualizarAction` reinyecta los permisos válidos del rol que el catálogo no dibuja — sin eso, guardar Recepcionista desde la UI le borraba `llaves.control` en silencio (bug viejo, ya corregido y verificado con round-trip idéntico de los roles 15 y 29). El botón "Todos" del grupo ahora solo alcanza `.rf-perms`/`.pp-perms`, nunca el interruptor. **Contrato: TODA área tiene encabezado** — o su comodín, o (si tiene UNA sola opción) esa opción promovida al mismo bloque (`promovido` de `rf_separar_total`, sin `data-total`: es ascenso VISUAL, no manda sobre nadie ni bloquea nada, y sin separador ni botón "Todos"). Un área con 2+ permisos y sin comodín se quedaría sin encabezado: lo vigila `RolPermisosPersonaTest` (junto con que todo comodín se llame `<modulo>.all`, lo único que `permission_in_list` entiende). Jul-23 se agregaron al catálogo `caja.all`, `tarifas.all`, `usuarios.all`, `configuracion.all` (solo catálogo, NINGÚN preset cambió → sin migración; los roles existentes siguen con sus permisos sueltos y el interruptor sale apagado). GOTCHA de dos sesiones: `rf_separar_total()` está DUPLICADO en las dos vistas bajo `function_exists` — si las definiciones se separan, gana la de la vista que cargue primero; al tocar una, tocar la otra.
- **Permisos por PERSONA dentro de su rol** (jul-23): `/configuracion/roles` lista la gente de cada rol (bloque `.rv-people`, tope 5 + "Ver las N restantes"; franja `.rv-orphans` para los `hotel_usuarios` sin `role_id`) y cada persona enlaza a `/configuracion/roles/{id}/persona/{usuarioid}` (GET+POST+`/restablecer`, vista `roles/permisos_persona.php`, tokens `--pp-*`). Se guarda la DIFERENCIA contra el rol en `hotel_usuarios.permisos_json` = `{"extra":[],"quitados":[]}` (la columna ya existía desde la migración base, sin migración nueva); así editar el rol después sigue alcanzando a la persona ajustada. Resolución en `can()`: quitados → extra → rol (helpers `normalizar_ajustes_permisos` / `current_hotel_permission_ajustes` / `permisos_con_ajustes_personales` en auth.php; consulta 1 vez por request y NO se cachea entre requests a propósito — quitar un permiso pega en el siguiente clic). El delta lo deriva `Rol::derivarAjustes(permisosRol, marcados, ambito)` y **el `ambito` es el catálogo de `config/permisos.php`**: un permiso sin casilla (hoy `llaves.control`) jamás cae en `quitados`. Candados: roles de acceso total no se ajustan por persona, rol/persona cruzados o de otro hotel rebotan, y sobre uno mismo no se puede quitar `roles.manage` ni darse lo que su rol no trae (a OTROS sí: editar el rol ya lo permitía). Suite `RolPermisosPersonaTest.php` (53). GOTCHA de paso: `old()` pasa el valor por `htmlspecialchars()` → con `old('permisos')` (arreglo) es TypeError fatal en PHP 8; leer `$_SESSION['old_input'][...]` crudo (ya corregido también en `roles/form.php`).
- **Centro documental → baja = retención, no borrado inmediato** (jul-27): `DocumentoController::eliminarAction` solo cambiaba `estado='eliminado'` en BD; el archivo NUNCA se borraba físicamente (bug real detectado en auditoría; OJO: la cuota de 2GB/hotel + aviso 80% se construyó EN PARALELO por la otra rama y quedó fusionada el 28-jul — hoy conviven cuota Y retención/purga). Fix: `actualizarEstado()` marca `eliminado_en=NOW()` al dar de baja (abre ventana de `Documento::RETENCION_ELIMINADOS_DIAS`=30 días, archivo recuperable en disco); `tools/cron_purga_documentos.php` (patrón cron_mantenimiento_preventivo) purga lo vencido vía `Documento::purgarElegibles()`→`purgarDocumento()` (1 transacción `FOR UPDATE` por documento, unlink real + `purgado_en`, idempotente, jamás re-purga). `resumenPorHotel()` excluye `eliminado` de `bytes_total` desde el momento de la baja (la cuota se libera de inmediato aunque el archivo siga en disco durante la retención) y expone `bytes_pendientes_purga` aparte para no perder visibilidad operativa de lo que aún ocupa disco. Suite `DocumentoPurgaTest.php` (20 asserts). Migración `20260727_001_documentos_purga_retencion.sql`. La cuota 2GB (`Documento::CUOTA_HOTEL_BYTES`, `validarCuotaHotel`, aviso 80% en documentos/index) YA existe desde la fusión del 28-jul: ambas features conviven en `Documento.php` — la cuota cuenta activos+archivados e ignora eliminados, la purga borra del disco al vencer la retención.
- **Multi-dueño (propietarios por habitación) = APAGADO de fábrica (jul-30)**: `hotel_owner_distribution_default()` devolvía a "Manolo" y "Elia" cableados (los socios del hotel original) y la pantalla de configuración pre-llena el form con ese default → **todo hotel que alguna vez guardó configuración terminó con esos dos socios escritos** en `hotel_configuracion.propietarios.distribucion` (aparecían en cortes, PDFs de reporte y en el chip de cada tarjeta de habitación); encima el normalizador reinyectaba la semilla al recibir lista vacía y el validador exigía ≥1 propietario, así que era IMPOSIBLE borrarlos. Hoy: default `propietarios: []` / `propietario_default: ''`, `hotel_owner_distribution_normalize` ya NO rellena, `..._normalize_payload` acepta cero dueños (y al quedar sin dueños ACTIVOS tira también reglas y asignaciones, si no una regla huérfana bloqueaba la baja; el predeterminado vacío se deriva del primer activo en vez de ser error) y `hotel_owner_distribution_enabled($hotelId)` responde si el hotel reparte. En el servicio: `configuracionLegacy()` → `configuracionVacia()`, `normalizarPropietarios` filtra a ACTIVOS sin fallback, y `distribuirPorPrecio`/`aplicarMovimientoCaja`/`aplicarMovimientoReporte`/`propietarioParaHabitacion` cortan en seco con cero dueños (sin el corte, `$resultado['']` = cubeta fantasma con warnings). Consumidores en sintonía: `HabitacionController::anexarPropietariosHabitaciones` ni siquiera estampa los campos (así `habitaciones/index` esconde `.rc-owner` y el reverso, y `ver.php` la fila "Propietario" — se acabó el "Sin propietario" en 49 tarjetas), y el enlace "Configurar propietario" de ver.php pasó a `config_hotel_url('#hc-owners')` (era `url('configuracion')` fijo = rebote para el hotelero desde el jul-28). UI en `#hc-owners`: banner `[data-owner-off]` cuando está apagado (oculta la vista previa), botón por fila `[data-owner-remove]` (la ÚLTIMA fila de cada lista NO se borra, se VACÍA: es la plantilla que clona `[data-owner-add]`) y `[data-owner-clear-all]` con msConfirm. Migración `20260730_002` borra las filas que son la semilla INTACTA (candado JSON: exactamente manolo+elia al 100%, sin asignaciones y máx. la regla "manolo") → en prod limpió Hotel base y Hotel Demo y **conservó Los Cedros (hotel 2), que sí reparte de verdad con sus 9 habitaciones asignadas a Manolo**. Suite `PropietariosMultiDuenoTest.php` (38). Los bloques hardcodeados MANOLO/ELIA de los PDFs se borraron (`includes/ReporteCortePDF.php`, su copia muerta en `app/includes/`, y el método muerto `generarSeccionPropiedadesLegacyPDF` de ReportesController — ese además leía `$propiedades['manolo']` a pelo). NO se tocaron los tipos de habitación legacy `sencilla_manolo`/`doble_manolo`: son DATOS reales de Los Cedros (`habitaciones.tipo`), no un default. **EN PRODUCCIÓN desde 2026-07-31** (10 archivos + migración; respaldo `backups/pre_deploy_duenos_<ts>.tar`).
- **Configuración del hotel = pantalla de MEDISOFT, no del hotel (jul-28)**: el hotelero ya NO entra a `/configuracion` (gate en `ConfiguracionController::puedeGestionarConfiguracionHotel` = `isSaasAdmin() && can('configuracion.view')`, entrada de sidebar con `isSaasAdmin()`, catálogo `config/navegacion.php` con el flag `solo_medisoft` que resuelve `nav_pantalla_visible` — el gate y el flag YA existían en la rama desde el 24-jul: esta tanda solo agregó DÓNDE ejercer la configuración). Se opera desde `/admin/saas/hoteles/{id}/configuracion` (`SaasAdminController::configuracionHotel`/`guardarConfiguracionHotel`, rutas GET+POST) que **reusa la MISMA vista** `configuracion/index.php` apuntada a otro hotel — jamás duplicar esas 8,600 líneas. Cómo funciona: `activarContextoHotel()` hace `TenantContext::setHotel($hotel)` (los helpers `hotel_*` lo consultan ANTES que `$_SESSION`, así que `hotel_setting()` y compañía leen el hotel objetivo) y **no toca `$_SESSION`** (el admin conserva su sesión); el guardado vive en `services/HotelConfiguracionService.php` (`normalizarPayload`/`guardar($valores,$hotelId)`/`datosDeVista`/`fondoGuardado`), y `ConfiguracionController::actualizarAction` sigue con su camino legacy. La vista se parametriza con `$configContextoSaas`, `$configFormAction`, `$configCancelUrl` y el helper local `$configSeccionVisible()`; en modo SaaS se OCULTAN 3 secciones: `hc-appearance` (tema/vibración = del dispositivo de quien mira), `hc-brand` (editor único = pestaña Marca del panel; el nav deja un `.hc-nav-link-out` que enlaza allá) y `hc-system` (sesión/respaldos = globales de la instalación) — junto con el bloque `.hc-hidden-compat`, a propósito: **la tabla legacy `configuracion` NO tiene `hotel_id`** (UNIQUE por `clave` a secas), así que escribirla desde el panel pisaría a todos los hoteles; por eso el modo SaaS no la lee ni la escribe (deuda viva: hoy un gerente que guarda desde `/configuracion` sí la pisa). El botón "Revocar" de dispositivos push se oculta en modo SaaS (su ruta `/configuracion/pwa-push/...` es del hotel de la sesión). Enlaces del hotel que apuntaban a los ajustes (dashboard estacionamiento ×7, `habitaciones/crear`, `ia/vigilancia_financiera`, `roles/index`) pasan por el helper `config_hotel_url($fragmento)` de `helpers/navegacion.php`: devuelve `null` para quien no es SaaS admin y la vista degrada a texto/`<span>` en vez de mandar a un rebote. Suite `SaasConfiguracionHotelTest.php` (24; el assert que importa es que guardar el hotel B no escriba filas del A ni de `configuracion`). GOTCHA de test: `hotel_config_normalize_editable_payload` valida TODOS los obligatorios juntos, así que un payload parcial devuelve 13 errores — armar el POST desde `hotel_config_editable_definitions()` con sus defaults. `tests/bootstrap.php` ahora carga `navegacion.php`+`footer_nav.php` (espejo de index.php). **EN PRODUCCIÓN desde 2026-07-29** (commit `3093949`, 14 archivos; respaldo `backups/pre_deploy_saascfg_20260729_015702.tar` = rollback de 1 `tar -x`).
- **`/configuracion` y los bloques no contratados: AVISAR, no ocultar (tanda 4, jul-29)**: esta pantalla la opera Medisoft, así que un ajuste de un bloque que el hotel no tiene se marca con un chip (`.hc-chip.is-locked`, "Sin el bloque «X»"), NUNCA se esconde ni se deshabilita. Razón dura: `hotel_config_normalize_editable_payload` recorre TODO el catálogo, así que un campo ausente del POST se guarda vacío (`''` en texto, **`'0'` en casilla** — de ahí el `<input type="hidden" value="0">` que precede a cada switch—, `visible=false` en política de huésped) y los 14 obligatorios abortan el guardado completo con `redirect` sin `old_input`; y **un `disabled` tampoco viaja en el POST** (bug con precedente en esta misma vista). Se conecta desde 6 puntos y cubre 19 ajustes + 10 campos de huésped + 2 catálogos: los closures `$configBloqueActivo`/`$configAvisoBloque` (junto a `$configSeccionVisible`), los mapas `$configBloquePorAjuste` (solo las 3 reglas de notificación con bloque real; `regla_habitaciones_limpieza` y `regla_mantenimiento_activo` NO, su consumidor gatea por `habitaciones` = base) y `$configBloquePorCampoHuesped` (**7** campos `vehiculo_*` + `rfc` + `requiere_factura` + `identificacion_archivo`), los dos renderizadores centralizados (`$configRenderSettingField` = único emisor de `hotel_config[...]`, `$configRenderGuestFieldPolicy`), la llave `'modulo'` en `$configHotelSettingGroupMeta` (grupos `reportes`→`reportes_distribucion` y `copiloto`→`copiloto_briefing`, avisados en el encabezado para no repetir 8 chips) y los 2 `hc-catalog-head` (Estacionamientos con `hotel_parking_visible`, Unidades con `inventario`; **Zonas no lleva chip**: no tiene módulo ni consumidor). **GOTCHA que invalida la tanda si se omite**: el predicado va SIEMPRE con guard `$hotelId > 0` porque `hotel_has_module` hace `$hotelId ?: current_hotel_id()` y `?:` trata 0 como falsy ⇒ un 0 desde el panel responde por el hotel del ADMIN, que es exactamente la fuga que esto cierra (`hotel_parking_visible` tiene el mismo agujero: solo comprueba `!== null`). Fail-OPEN a propósito: catálogo ilegible ⇒ no se avisa de más. Verificado en navegador con round-trip real desde el panel: hotel 2 → 60 filas suyas actualizadas + 8 creadas, **0 filas del hotel 1**, tabla legacy `configuracion` intacta y 0 inputs deshabilitados con los 69 `hotel_config[...]` presentes. Suite `SaasConfiguracionHotelTest` (43).
- **Panel SaaS → pestaña Marca = taller visual (jul-28)**: `admin/saas/hotel_detalle.php` §branding se reescribió (clases `sb-*`, autocontenido con su `<style>`+`<script>`) porque el formulario plano perdía contra el `hc-brand` viejo. Trae vista previa EN VIVO con 3 escenas (Sistema / Acceso / App instalable) movida por variables CSS `--sb-primary/-secondary/-accent/-bg/-on-primary` sobre `[data-sb-preview]`, aviso de **contraste** por color (ratio contra blanco; <4.5 avisa "el texto blanco encima se lee mal" — mismo error que se pagó en los PDFs de cotización), 6 paletas listas, **colores tomados del logo** por canvas, validación de peso/medidas antes de enviar (los íconos PWA exigen 192/512 exactos), semáforo de completitud 6/6 con anillo, y contador de cambios sin guardar + Restablecer. Recupera dos cosas que el panel NO tenía y solo vivían en `/configuracion`: el **tema** (`tema` agregado a `datosBrandingDesdePost`; vacío = conserva el actual por `COALESCE` del modelo) y el **fondo del sistema** (`apariencia.fondo_sistema`, que NO vive en `hotel_branding` sino en `hotel_configuracion` → lo guarda `guardarFondoSistema()` tras el branding). GOTCHAS pagados: (a) las rutas guardadas son relativas (`uploads/branding/...`) y desde `/admin/saas/hoteles/4` el navegador las resuelve contra esa carpeta → TODAS las miniaturas rotas; el JS las ancla con `resolverUrl()` + `data-sb-base="<?= url('') ?>"` (el HTML inicial estaba bien: lo rompía el repintado JS); (b) si la ruta apunta a un archivo borrado, `img.onerror` vuelve a la inicial; (c) el extractor de color debe descartar el lienzo (max>232, o gris claro con max-min<26) o un logo sobre papel devuelve tres tonos casi blancos. QA: el pane no compositaba (screenshot colgado) → todo verificado con asserts JS + round-trip real contra la BD.
- **Clasificación comercial del catálogo (jul-24)**: `modulos.tipo_comercial` (base|opcional|interno, VARCHAR validado en PHP) + `activo_global` + `motivo_bloqueo`. Paquete base (**10** desde jul-25, es_core=1 precio 0): dashboard/habitaciones/reservaciones/huespedes/caja/usuarios/roles_avanzados ("Roles y permisos", clave técnica intacta)/mantenimiento/**notificaciones**/**pwa** (migración `20260725_001`, decisión del owner: van en el base "de ley"). Al promover un opcional a base NO basta el UPDATE de catálogo, hay 3 efectos: (a) `$BASE` del verificador es una lista EXACTA (`===`) y alimenta 2 asserts → sale exit 1 si no se actualiza; (b) los crons que filtran con `INNER JOIN hotel_modulos` dejan fuera a TODOS los hoteles (es_core no crea filas) — caso real `cron_resumen_ia.php`; (c) el módulo deja de ser el candado de facto, así que la pantalla necesita su permiso explícito en navegacion.php + `require_permission_or_403` en el `before()` (se hizo con `notificaciones.view`). Internos jamás cobrables ni togglables: configuracion y anticipos (ambos es_core=1 — los gates de anticipos en Reservaciones ya no dependen de hotel_modulos), reportes (contenedor), tareas y reportes_distribucion (re-registrados bloqueados, trazabilidad). **A la venta (8, jul-25, migración `20260725_002`)**: inventario $299, facturacion $249, compras $199, documentos $149, reputacion $149, descuentos $99, tarifas_dinamicas $149, lealtad $179 — lista blindada por el assert `$OPCIONALES_VENTA` del verificador (exacta, como `$BASE`). **+ los 5 `reporte_*` a $49 c/u (jul-28, migración `20260728_001`, decisión del owner)**: el bloque 5 del verificador quedó INVERTIDO (hasta jul-28 exigía por contrato bloqueados/precio 0/con motivo; ahora opcional+es_core=0+`activo_global=1`+precio>0+sin motivo) y `$OPCIONALES_VENTA` NO los cubre porque su query excluye `clave NOT LIKE 'reporte\_%'` — por eso siguen siendo 8 ahí y el assert no se tocó. Desbloquearlos NO movió el cobro de NINGÚN hotel (cero filas de `hotel_modulos` apuntaban a un reporte_*: nacieron bloqueados en 20260724_001 y jamás fueron contratables) ⇒ el GOTCHA de dinero de aquí abajo aplica SOLO a bloques que ya fueron vendibles alguna vez; comprobarlo con un `COUNT(*) FROM hotel_modulos hm JOIN modulos m … WHERE m.clave …` ANTES de asustarse por el estimado. Quedan disponibles, no contratados: para que un hotel los use hay que activárselos en `/admin/saas/hoteles/{id}`. El panel `admin/saas/modulos.php` no necesitó cambios (agrupa por `activo_global` + prefijo `reporte_` y ya traía el caso vacío "los desbloqueados aparecen arriba, entre los disponibles"). El resto sigue `activo_global=0` con motivo. GOTCHA de dinero al DESBLOQUEAR: `hotel_modulos` nunca se borró (se congela como historial), así que abrir un bloque REACTIVA el cobro de todo hotel que lo tuviera contratado — Los Cedros pasó de $1,298 a $2,471/mes de un golpe. Revisar el cobro estimado de cada hotel tras cada apertura y apagar desde el panel lo que no deba renovarse. `documentos` se abrió con la política de retención/borrado de PII aún PENDIENTE (decisión explícita del owner jul-25, consta en la cabecera de la migración). **Auditoría de gates de los 8 (jul-25, 18 agentes)**: `compras`/`reputacion`/`lealtad` = candado sólido; `facturacion`/`documentos`/`inventario` = candado sólido con fugas SATÉLITE (la pantalla da 403 pero otras pantallas filtran metadata: el check-in del base crea filas en `solicitudes_factura` sin gate, CxC pinta deep-links a `/facturacion/ver/{id}`, el KPI "Facturas pendientes" del gerencial-diario solo pide `tablero_ejecutivo`) — deuda anotada, no bloquea la venta; `tarifas_dinamicas` y `descuentos` estaban **SIN candado alguno** (caso `pwa` repetido) y se les escribió uno: `TarifasController::before()` ahora exige `require_hotel_module('tarifas_dinamicas')` ANTES del permiso, y `descuentos` —que no tiene ruta propia, es feature-flag dentro del paquete base— se gatea en los PUNTOS DE ENTRADA (`claseTarifaPermitida()` degrada clase 'descuento'→'incremento' en crear/editar tarifa; `puedeAplicarDescuentos()` ignora `descuento_aplicado` del POST en los 2 puntos de ReservacionController), JAMÁS con 403 (tumbaría Reservaciones). Los descuentos ya aplicados se conservan (historial congelado). Suite `GatesBloquesVentaTest` (16) — bloquear global corta rutas al instante pero `hotel_modulos` se CONGELA como historial (contrato: `actualizarModulosHotel` salta internos/bloqueados; sin ese candado el checkbox disabled, que NO viaja en POST, borraba la contratación al guardar). Cobro (`resumenCobroMensual`) exige `tipo='opcional' AND activo_global=1`. Centro de Reportes: acceso = ≥1 pantalla permitida vía mapa `hotel_report_screen_modules()` (helpers/modulos.php, pantalla→módulos any-of); gates `require_hotel_report()` por acción + datos-grafica/exportar-pdf mapean `tipo`→pantalla (exportar exige además `exportaciones`); ranking-estados ⊂ reporte_procedencia; mantenimiento (base) regala sus 2 reportes a todo hotel ⇒ el centro siempre existe. /configuracion (index/update/backup) = SOLO `saas_admins` (`isSaasAdmin()` en before + flag `solo_medisoft` en navegacion.php + gate en sidebar); OJO: /configuracion/tarifas y /configuracion/roles son controllers PROPIOS (Tarifas, Rol) y siguen siendo del hotel. Planes: Pro/Premium `activo=0` conservados (actualizarPlanHotelAction ya rechaza planes inactivos); preset Básico = paquete base exacto (reportes quedó `incluido=0`). Verificador PASS/FAIL del catálogo real: `tools/saas/verificar_clasificacion_comercial.php`. Suites `ClasificacionComercialModulosTest` + `ReportesIndividualesGatesTest`. Respaldo pre-migración: `backups/respaldo_catalogo_modulos_20260724.sql`.
- **Fugas satélite de bloques opcionales — tanda 1 CERRADA (jul-29)**: contrato general — en un flujo del PAQUETE BASE el gate de un bloque opcional JAMÁS es 403 (tumbaría Reservaciones): se apaga el productor y la UI no pinta la pregunta (mismo criterio que `descuentos`). `facturacion`: gate `ReservacionController::puedeRegistrarFacturas()` (vía `current_hotel_has_module`) con early-return en `procesarSolicitudFactura` (cubre sus 4 llamadas de un golpe) + el `requiere_factura` del anticipo derivado CON el gate y guardado como 'no' en el abono (si no, una reversión futura intenta ajustar una factura que nunca existió); la UI la apagan `$rvModuloFacturacion` en `reservaciones/ver.php` (5 bloques: anticipos, resumen del anticipo, check-in, cambio de pago, pago tardío) y `$resModuloFacturacion` en `reservaciones/index.php` — **ese listado es el 5º productor y fue el hallazgo de la revisión adversarial: su modal de check-in postea a la MISMA acción y su pregunta era obligatoria**. Los 4 validadores JS deben tratar la pregunta ausente como "no aplica" (`if (!facturaSi || !facturaNo) return true`) o el cobro queda bloqueado por un campo que ya no se pinta, y `confirmarCambioPago` leía `.checked.value` sobre null. Las solicitudes YA creadas se conservan (historial congelado) y el ajuste negativo por reversión sigue condicionado al ABONO, no al módulo. `inventario`: early-return `hotel_has_module('inventario', $hotel_id)` al inicio de `procesarDescuentoInventario` — la devolución vive en CANCELACIÓN (`Reservacion::devolverInventarioCancelacion`, NO en checkout) y no pasa por gate, así que el consumo histórico sigue reversible con el bloque apagado. Footer (`helpers/footer_nav.php`): Documentos y Compras seguían proxies ajenos (`huespedes`/`reservaciones` son base ⇒ atajo visible para TODOS y 403 al entrar) → ahora `['documentos']` y `['compras']`. **El nombre del paso 3 lo decide el JS, no el marcado (jul-29, reportado desde prod con captura)**: `setCheckInWizardStep` PISA la etiqueta en cada cambio de etapa (`facturaStepLabel.textContent = sinCobroNuevo ? 'Sin cobro nuevo' : …`), así que un ternario de PHP en el `<strong>` se pinta y a los 200 ms el JS lo borra — el paso seguía diciendo "Factura" en un hotel sin el bloque con el fix ya desplegado. Hay **DOS** steppers con paso de factura: el del wizard de check-in (`data-step-indicator="3"`) y el del wizard de anticipos (`data-rda-ind="2"`, cuya etapa 2 sin el bloque queda solo con el "Resumen del anticipo"); ambos renombran a "Confirmar". Corolario de QA: para verificar una etiqueta de wizard hay que leerla **después de abrir el modal** y ya en la etapa, no en el HTML inicial ni por `strpos` del fuente — así se colaron los dos casos. Suite `GatesFacturacionInventarioTest.php` (31). Trabajo hecho en un worktree sobre `origin`, no sobre el árbol local: el plan que originó la tanda se escribió sobre un checkout 18 commits atrás y ~60% ya vivía en prod (clasificación comercial + candados c74b407 + reportes individuales) — antes de auditar o ejecutar un plan ajeno, `git fetch` + `git status -sb`. **Las 5 tandas están EN PRODUCCIÓN desde 2026-07-29** (7 commits `a2f0669..6ec7d36`, 23 archivos de `src/`, **cero migraciones**): tanda 1 facturación/inventario/footer, tanda 2 estacionamiento, tanda 3 motor de tarifas, tanda 4 barra inferior del panel, tanda 5 PDF gerencial. Respaldo de rollback en `backups/pre_deploy_fugas_20260729_183429.tar` (20 archivos, `tar -x` y listo). Verificación: prod coincidía byte a byte con el `origin` previo antes de pisar nada (md5 normalizados), `php -l` 23/23 en el contenedor, manifiesto revalidado ya instalado, smoke funcional de 17 asserts sobre el código desplegado y `/health` 200. Efectos visibles del deploy: **San Nicolás** (no tiene `facturacion`) pierde la pregunta "¿requiere factura?" y **Hotel Demo** (no tiene `vehiculos`) pierde la tarjeta de estacionamiento; Los Cedros sin cambios. Lo que queda son decisiones del owner, en la memoria [[fugas-modulos-saas-pendientes]].
- **`/configuracion` es la pantalla del VENDEDOR: no se gatea por módulo, se avisa (tanda 4, jul-29)**: la conclusión al auditarla (6 agentes) fue que ocultarle a Medisoft la configuración de un bloque que el hotel no contrató **invierte el flujo comercial** (configurar→demostrar→activar→cobrar) y lo obliga a activar el bloque —y arrancar el cobro— solo para poder configurarlo; el estado real de bloques ya vive en la pestaña hermana "Bloques y cobro" del mismo panel. Además **ocultar parcialmente ROMPE o VACÍA el POST**: 14 claves `required` de `hotel_config_editable_definitions`, un booleano ausente se guarda `'0'` (`hotel_config.php:650-654`), la política de huésped ausente cae a `visible=false` (`hotel_guest_field_policy_normalize_payload` recorre el catálogo COMPLETO), `amenities` ausente se guarda `[]`, y los 3 catálogos generales exigen ≥1 fila (`hotel_general_catalog_normalize_rows`: el error "Debes conservar al menos un registro en X" es INCONDICIONAL, `requireActive` solo gobierna el segundo). Si algún día se oculta algo ahí: **enlace del nav + panel + tarjeta de `hc-config-map` con el MISMO predicado**, porque el nav es un TABLIST con JS (`role=tab`+`aria-controls`, controlador ~§8484) y un enlace cuyo panel no existe queda como BOTÓN MUERTO silencioso (`activateConfigSection` hace `return` tras el `preventDefault`); y **jamás `disabled`** para "avisar" (un input disabled no viaja y se guarda vacío — ya pasa con frecuencia/retención de respaldos), `readonly` sí postea. Lo ÚNICO que sí se arregló aquí porque es un bug de verdad: **la barra inferior en el panel SaaS** — `hotel_footer_nav_available_catalog()` no aceptaba hotel y filtraba con `hotel_menu_module_enabled`, que devuelve `true` bajo `/admin/saas` y resuelve por `current_hotel_id()` (`$_SESSION`) ⇒ el panel ofrecía **y guardaba** (el sanitizador valida contra el catálogo completo) atajos hacia pantallas que el hotel cliente rebota con 403; ahora los 3 helpers aceptan `$hotelId` y el filtro explícito es **espejo exacto** de la semántica de menú (catálogo `null`⇒no esconde nada, catálogo vacío⇒sobrevive `dashboard`) porque `hotel_has_module` a secas es fail-CLOSED y en un despliegue fresco (tabla `modulos` vacía) dejaría el panel sin un solo atajo. Mitigación obligatoria del mismo cambio: el relleno de respaldo de `hotel_footer_nav_items` ahora completa hasta el **mínimo** (antes solo actuaba si quedaba VACÍA) — sin eso, un hotel cuyos atajos guardados caigan a 1 tras filtrar aborta TODO el guardado de la pantalla con "necesita al menos 2 atajos". Asserts en `SaasConfiguracionHotelTest` (29). Pendiente de decisión del owner (no es técnico): retirar de verdad las 16 definiciones de `reportes`+`copiloto` si esas funciones están retiradas, y qué hacer con **5 ajustes + 1 catálogo MUERTOS** (`documentos.texto_cotizacion/_ticket/_footer/mostrar_logo`, `reservaciones.mensaje_confirmacion_whatsapp`, `catalogos.hotel_zonas`: cero consumidores en `src/` — 15% de la superficie editable es teatro).
- **El MOTOR de tarifas respeta la contratación POR CLASE (tanda 3, jul-29)**: candado en `IncrementoTarifa::getIncrementosAplicables` — el ÚNICO punto por el que pasan las dos calculadoras (`calcularPrecioConIncremento` y `calcularDescuentoPorTipo`), así que cubre de un golpe sus ~20 consumidores (modelo `Reservacion` ×6, las 2 cotizaciones PDF, `MotorReservaOnlineService`, Copiloto IA, `HabitacionController` ×3, `ApiController` ×3, `TarifasController`). La llave es la CLASE de la regla: `incremento`→`tarifas_dinamicas` ($149), `descuento`→`descuentos` ($99) — contratar uno NO arrastra al otro (caso real: Los Cedros tiene tarifas_dinamicas y NO descuentos). c74b407 solo había blindado las PANTALLAS y el override manual del operador; las reglas guardadas seguían cobrándose, y el hotel que cancelaba no podía apagarlas porque `/tarifas` le da 403. Sin bloque el motor devuelve CERO reglas aplicables (precio vuelve al base, `descuento_total` 0), jamás un error: los consumidores no distinguen "no contratado" de "sin reglas vigentes". Lo ya cobrado queda congelado (`precio_total`/`descuento_total` viven en la reservación) y las reglas NO se borran: al recontratar reviven. `sin contexto de hotel resoluble → true` a propósito (CLI/cron; `getActivosParaFecha` ya devuelve vacío ahí). El endpoint `incrementosTarifaActivos` está mapeado a `reservaciones` (**base**, lo consume el alta) así que NO se 403ea: filtra las filas por clase con el bloque de cada una. Suite `GatesMotorTarifasTest.php` (17). **Consecuencia obligatoria**: `TarifaImpactoTest` tuvo que CONTRATAR `tarifas_dinamicas` para su hotel sembrado o sus 48 asserts miden precios base — cualquier caso nuevo que siembre reglas en `incrementos_tarifas` necesita la fila de `hotel_modulos` (28 FAIL instantáneos si se olvida).
- **Estacionamiento = bloque `vehiculos`, un solo predicado (tanda 2, jul-29)**: `hotel_parking_visible($hotelId = null)` en `helpers/modulos.php` es el punto ÚNICO — con `$hotelId` consulta ese hotel (`hotel_has_module`), sin él usa la semántica de menú (`hotel_menu_module_enabled`: fail-open y `true` bajo `/admin/saas`, por eso en el panel SaaS SIEMPRE se pasa el hotel objetivo). Antes el bloque gateaba solo MEDIA función (los 3 endpoints AJAX de `HuespedController` 629/990/1061 y `vehiculosHuesped` en el mapa de ApiController) y la otra mitad seguía encendida para cualquier hotel: la proyección estaba mapeada a `dashboard` (**paquete base**, ya corregida a `vehiculos`), la tarjeta del tablero, el POST del alta de huésped, el PDF de cotización, los chips del listado y las fichas contador. **Se GATEA, jamás 403**: vive dentro de pantallas del paquete base (mismo criterio que descuentos/facturación); los 3 AJAX sí cortan porque responden JSON. 15 consumidores: tablero (productor + tarjeta escritorio + bloque móvil — **los dos bloques se apagan JUNTOS o la tira móvil queda vacía sin error: el JS pinta todos los `[data-pf-strip]` pero su ancla `#parkingForecast` vive en el de escritorio**), `ApiController` (mapa), `ReservacionController` (`cotizacionPdfVehiculos` = 1 punto para las 2 acciones de cotización porque la caja del huésped deriva su alto de `count()`; carga de la ficha; **exports PDF y Excel, que SÍ imprimen marca/modelo/color en `$res['vehiculo']` — no son inocuos**), `HuespedController` (`verAction` + `guardarAction`, el 4º punto de escritura y el único que estaba sin candado), `huespedes/index.php` (chips ×2, y de paso no instancia el modelo = muere el N+1), `huespedes/ver.php` (2 fichas contador + tarjeta "Vehículo anterior", que lee las columnas LEGACY `huespedes.vehiculo_*` y sobrevivía a todos los gates), `habitaciones/ver.php` (tile) e `historial.php` (chip). **BUG VIVO cerrado en el paso 0**: el `.catch` de `cargarVehiculos()` (reservaciones/ver.php) reemplazaba `#listaVehiculos` con "Error al cargar vehículos", borrando la lista que el servidor ya había pintado — un 403 del bloque o una red intermitente y el hotelero perdía los vehículos en pantalla. Se conserva el `fetch` a propósito: su render es MÁS rico que el del servidor (agrega Editar/Eliminar), así que quitarlo —como sugería el análisis— habría borrado esas acciones. Suite `GatesEstacionamientoTest.php` (34). GOTCHA de QA local: el catálogo de la BD de dev tiene `vehiculos` con `activo_global=0` y **prod lo tiene abierto** (ver [[catalogo-modulos-prod-vs-local]]) → en local la función se ve apagada aunque el código esté bien; abrir el bloque localmente antes de concluir que algo se rompió.
- **Funciones con nombre propio que NO son módulos del catálogo** (auditoría jul-25 — no las busques en `modulos`): **Guardián financiero** vive DENTRO de `ia_ejecutiva` (`VigilanciaFinancieraController:27` = `require_hotel_module('ia_ejecutiva')` + `can('guardian.view')`), con modelos/servicios/permiso/entrada de navegación propios pero sin fila que contratar ni cobrar. **Proyección de estacionamiento** ~~está mapeada al módulo `dashboard`~~ CORREGIDO jul-29: toda la función (proyección incluida) cuelga del bloque `vehiculos` vía `hotel_parking_visible()` — ver la entrada de estacionamiento de arriba. **Llaves y controles** no tiene pantalla ni controlador propio: son 6 acciones POST incrustadas en `ReservacionController` (3166-3560) y sus tablas `control_llaves`/`control_remotos`/`historial_*` NO TIENEN columna `hotel_id` — la pertenencia se valida por código (`habitacionesPertenecenAlHotel()`), no por esquema.
- **Los tests de RETIRO comercial verifican TEXTO FUENTE, no comportamiento**: `TareasInfraestructuraTest` (21/21) y `RetiroDistribucionReportesTest` (10/10) son 100% `strpos()` sobre archivos; `MantenimientoReporteNivelesTest` y `ReportesIndividualesGatesTest` casi. Consecuencia real medida: Tareas pasa en VERDE mientras `/tareas/reporte`, `/tareas/agenda`, `/tareas/crear`, `/tareas/{id}` y `/tareas/{id}/editar` siguen registradas y vivas (routes.php:474-488; solo `GET /tareas` redirige). Los de CÁLCULO sí son conductuales y buenos (`RentabilidadHabitacionesTest` siembra BD y asserta cifras). Al retirar una sección, el test debe preguntarle al ROUTER qué sigue alcanzable, no al archivo si contiene una cadena.
- **Captura offline de ESCRITURAS: APAGADA (jul-26)**. `window.MEDISOFT_OFFLINE_ESCRITURAS = false` (header.php) + candado duro en `OfflineData.encolarOperacion` (offline-data.js) que lanza `OFFLINE_ESCRITURAS_DESHABILITADAS` — punto ÚNICO, cubre a los 6 llamadores actuales y a cualquiera futuro, no moverlo a los interceptores. Motivo: `/api/sync` responde 423 desde hace meses (`ApiController::syncAction`, con test que lo fija) y los interceptores SÍ encolaban dinero real (`caja-offline` movimientos y pre-corte, `habitaciones-offline` check-in/out/liberar, `huespedes-offline` alta, `reservaciones-offline` check-in/out): la app decía "se enviará al volver internet" y NUNCA llegaba. Cada interceptor tiene además un guard temprano con aviso honesto ("no quedó guardado") porque el diálogo de confirmación prometía el envío ANTES de encolar, y habitaciones-offline ni siquiera atrapaba el rechazo. Lo que sigue vivo: caché de LECTURA, app instalable y la pantalla `/offline/pendientes` (la cola vieja en dispositivos de producción es la ÚNICA copia de esos datos: al recibir 423 se avisa que se capturen a mano y JAMÁS se borra). Reactivar = reabrir /api/sync auditando idempotencia y cortes de caja, y poner el flag en true.
- **Reputación NO filtra por calificación (jul-26)**: la invitación a dejar reseña en Google se ofrece a TODO el que responde (`ReputacionService::puedeInvitarResena` solo mira que haya URL configurada). Antes exigía `>= 4` = review gating, prohibido por la política de contenido de Google Maps. El gating vivía en DOS puntos y ambos hay que respetar: el servicio Y `views/encuesta/formulario.php` (`$mostrarGoogle` gateaba por su cuenta; tras el POST la vista relee la encuesta, así que arreglar solo el servicio NO quita el filtro). El umbral de ALERTA INTERNA (`reputacion.umbral_alerta`, default 3) es independiente y sí puede discriminar: es privado. Suite `ReputacionTest.php` (28). GOTCHA de venta: el correo de la encuesta NO funciona (no hay MTA en la imagen) → se comparte copiando el link.
- **Centro documental con cuota (jul-26)**: `Documento::CUOTA_HOTEL_BYTES` = 2 GB por hotel (decisión del owner), validada en `validarCuotaHotel()` dentro de `crearDesdeUpload`; `espacioUsado()` suma activos + archivados e IGNORA los `eliminado` (válvula del hotelero para liberar). `resumenAlmacenamiento()` alimenta el indicador y el aviso al 80% en documentos/index. OJO: la baja es LÓGICA, el archivo sigue en disco → no se puede prometer borrado definitivo hasta que exista la purga física (deuda anotada en `docs/politica-centro-documental.md`). `formatBytes` ahora corta en GB (antes imprimía "2048.00 MB").
- **Offline de LECTURA: el arranque de la PWA lo hacía inalcanzable (arreglado jul-30, SW v25)**. Síntoma que reportó el owner desde su iPhone: sin internet la app SIEMPRE mostraba el muro "Sin conexión", con el caché de pantallas intacto del otro lado. Causa: el manifest por hotel declaraba `start_url = /h/{slug}/login` y `OFFLINE_PAGE_PATHS` solo cachea `dashboard|habitaciones|reservaciones|huespedes|caja|offline/pendientes` → el arranque caía en `networkOnlyPage` → `offline.html`, y en iOS la PWA se descarta de memoria (arranca en start_url casi siempre), así que el offline que sí funciona NUNCA se veía. Arreglo en 3 piezas: (a) `start_url` → `url('dashboard')` en `PwaController::manifestAction` — el `id` sigue siendo `/h/{slug}` (cambiar start_url NO reinstala la app) y sin sesión `require_auth`→`login_path_for_current_context` devuelve al login de ESE hotel por la cookie `MEDISOFT_HOTEL_LOGIN_CONTEXT_COOKIE`, así que no se pierde marca ni scope; (b) `OFFLINE_ARRANQUE_PATHS` + `arranquePage()` en el SW: login por slug, login genérico y raíz sirven la mejor pantalla guardada (`OFFLINE_ENTRY_PAGES`, dashboard primero) — **imprescindible y NO redundante con (a): una PWA ya instalada conserva su start_url viejo hasta reinstalarse**, sin esto los dispositivos existentes seguirían rotos; (c) `offline.html` dejó de ser callejón sin salida (antes su único botón hacía `location.reload()` de la MISMA url que acababa de fallar): enlaza lo que sí está guardado leyendo `caches.open('loscedros-pages').keys()` y cuenta la cola vía `localStorage['loscedros_offline_db_name']`→store `operaciones_offline`. El caché de páginas pasó a `loscedros-pages` **SIN sufijo de versión** a propósito: el `activate` borra todo `loscedros-*` ajeno a `CACHE`, así que antes CADA deploy del SW borraba la memoria offline del hotelero; sigue limpiándose por logout/cambio de hotel con `CLEAR_PAGES_CACHE`, que es lo que aísla de verdad (hay assert de eso). **El copy de cualquier UI offline nueva NO puede prometer envío** — es solo lectura, ver la entrada de captura apagada de arriba; el lenguaje correcto es "regístralo a mano". `camarista` está en las rutas cacheables Y **segundo en `OFFLINE_ENTRY_PAGES`, después de dashboard**: un rol acotado a Limpieza nunca tiene copia del dashboard (`DashboardController::before` lo rebota a su home) y su arranque offline caía al muro; el orden no le quita su pantalla a nadie porque cada rol solo cachea lo que puede abrir. Al sumar una pantalla operativa nueva: va en los DOS sitios del SW (`OFFLINE_PAGE_PATHS` para que se guarde, `OFFLINE_ENTRY_PAGES` si sirve de puerta de entrada) y en `PANTALLAS` de `offline.html` con el nombre del MENÚ, no el de la ruta ("Limpieza", no "camarista"). Deuda deliberada que queda: rutas no cacheables (reportes, configuración) siguen dando el muro a propósito — suplantarlas mentiría sobre la URL. **GOTCHA que costó el arreglo entero y NO se ve sin navegador — `Vary` mata a `cache.match` por URL**: Apache manda `Vary: Accept-Encoding,User-Agent` en todo HTML (`Header append Vary User-Agent` del `.htaccess`, sección de deflate). Las pantallas se guardan con la Request de NAVEGACIÓN real (que lleva User-Agent), pero buscarlas con `cache.match('https://host/dashboard')` arma una Request SIN ese header (User-Agent es forbidden en JS: no se puede poner) → el Vary no casa y **el match devuelve undefined SIEMPRE, aunque `cache.keys()` muestre la entrada ahí**. Toda lectura del caché por URL string necesita `{ ignoreVary: true }` (ya puesto en `mejorPantallaGuardada` y en el fallback de `networkFirstPage`). Dos corolarios: (1) `offline.html` nunca sufrió esto porque el shell se precachea con `cache.add(string)` — ninguno de los dos lados lleva UA — lo que hacía el bug aún más difícil de ver, porque el muro sí funcionaba; (2) sin `ignoreVary` en `networkFirstPage`, **una actualización de Safari cambia el User-Agent e invalida en silencio TODA la memoria offline del hotelero** aunque siga guardada en disco. Síntoma para reconocerlo rápido: `cache.keys()` lista la URL pero `cache.match(url)` da undefined. Verificación sin navegador (el pane no corta la red ni reproduce el arranque instalado): arneses `tools/tests_js/` — el `FakeCache` **simula el Vary a propósito** (guardado por Request ⇒ con UA, por string ⇒ sin UA); sin esa regla el arnés daba verde a un fallback que en el navegador real no encontraba nada. Receta en `docs/QA-RECETAS.md` §PWA/Offline.
- **Estado de red: `PWA.isOnline()` es una FOTO, no una medición (bug real jul-30, corregido)**. El hotelero no podía registrar huéspedes: "Sin conexión… no quedó guardado" **con internet perfecto**. Causa: `_onlineConfirmado` (pwa.js) solo se recalculaba al cargar la página, en los eventos `online`/`offline` del navegador o cuando el SW avisa al navegar — y el caso más común en un hotel no dispara NINGUNO de los tres: **el wifi sigue conectado y es el servidor el que no responde un instante** (se reinicia, cambio de antena, señal que parpadea). El ping fallaba, la foto quedaba en `false` y **nadie la volvía a mirar**: el usuario capturaba dos minutos y al guardar lo rechazaba un dato viejo. Se curaba recargando, que es justo lo que nadie hace a media captura. Arreglo en dos capas: (a) **reverificación periódica** en `detectarConexionReal` — mientras el estado sea offline, se re-mide cada 15 s (`programarReverificacionRed`) y al recuperar red se dispara `handleNetworkChange`, que repinta banners y emite `loscedros:network-change`; más `visibilitychange` (volver a la pestaña re-mide). Esto cura SOLO a los 4 consumidores de una vez; (b) `PWA.hayConexionAhora()` — medición real, asíncrona, que los interceptores consultan **antes de bloquear al usuario**: camino normal sin latencia (si la foto dice que sí, devuelve true de inmediato) y ping fresco solo cuando la foto dice que no. Contrato para código nuevo: **`isOnline()` para pintar UI, `hayConexionAhora()` antes de cualquier decisión que le cueste trabajo al usuario** (guardar, cobrar). Interceptores corregidos: `huespedes-offline` (alta), `caja-offline` (ingreso, gasto y botón Hacer Corte) y `reservaciones-offline` (que además ignoraba el regreso de la red: su listener de `network-change` solo actuaba con `online === false`, así que el banner "sin conexión" se quedaba pegado). `habitaciones-offline` NUNCA tuvo el bug porque consulta solo `navigator.onLine` — el criterio más simple resultó el más robusto. Al reenviar tras confirmar red se usa `form.submit()` **nativo** a propósito: no vuelve a disparar el handler (sin bucle) y esquiva el gotcha de `requestSubmit()` con los guards de app.js. De paso, el SW dejó de responder "la acción se ejecutará cuando vuelva internet" a un POST sin red (v26): es la misma promesa falsa que se quitó de los interceptores el 26-jul y sobrevivió ahí porque nadie la buscó en el service worker; hay caso de regresión en el arnés.
- **Offline vendible: qué se endureció y qué NO se tocó (auditoría + tanda jul-30)**. Auditoría de 6 ángulos sobre las ~6,700 líneas del subsistema: 92 hallazgos, 24 confirmados tras verificación adversarial. Veredicto: **se vende como CONSULTA sin internet, nunca como "sigue operando"** — límites redactados para ventas en `docs/ruta_maestra_estrategia_comercial.md` §Modo sin internet. Lo implementado: (a) **la app mentía en producción** — el banner de Caja prometía que los cobros sin internet "se guardan y se aplican al volver", con las escrituras apagadas desde el 26-jul; el guard de `encolarOperacion` impedía la pérdida, pero el cartel inducía a cobrar en efectivo confiado. Ese texto y otros 3 (toast de `pwa.js`, banner de `header.php`, el catch de `crear.php` que culpaba al usuario con "Revisa los datos") ahora dependen de `OfflineData.escriturasHabilitadas()`; y `_preCorteOffline` ganó el guard que le faltaba: hacía **contar el efectivo billete por billete y fallaba después**. (b) **Cinta de frescura en el SW** (`conCintaOffline`, v27): toda pantalla servida del caché se ve idéntica a la de internet, y los 4 avisos que existían en las vistas estaban MUERTOS (un `return;` al inicio de cada uno) — ahora el propio SW inyecta una cinta con la edad real del dato (del header `Date` de la copia, cero costo) y avisa cuando sirvió otra query que la pedida (`/huespedes?buscar=X` devolvía la lista completa como si fuera el resultado). Se hace en el SW y no en cada vista a propósito: cubre cualquier pantalla, presente y futura. (c) **Hueco legal cerrado**: `clearLocalPrivateData` preservaba IndexedDB ENTERA "para no perder pendientes", dejando ~1,000 huéspedes con teléfono y correo en cualquier equipo; ahora borra los datos del hotel y conserva solo `operaciones_offline`/`offline_queue`/`meta` (la cola vieja es la única copia de lo capturado antes del apagado). Además el logout se detecta por la ACCIÓN del form y no por `id="logout-form"`: hay DOS formularios de logout y el del **menú móvil no tenía id**, así que en celular —donde trabaja recepción— no se borraba nada; y se limpia también al caducar la sesión (evento `loscedros:sesion-expirada` desde el 401). (d) **`_txReemplazar`**: los snapshots hacían `_txClear` + N `_txPut` con transacción POR REGISTRO (~7,200 aperturas por ciclo) dejando media copia si el móvil suspendía la pestaña — ahora es una transacción atómica (o entra entera o se conserva la anterior) y las reservaciones capturadas en el equipo van en el MISMO reemplazo. (e) Costo sobre el VPS: `_ultimaPruebaConexion` arranca en `Date.now()` para que el `handleNetworkChange` del arranque caiga en el throttle — evitaba un ping a `/api/buscar` (2 JOIN + 3 GROUP_CONCAT) en CADA carga de página y de TODO usuario; guard de re-entrancia en `capturarSnapshots`; y `dashboard.js` dejó de pollear 3 endpoints cada 30 s contra anclajes que su vista ya no tiene (autolimitante: revive solo si el DOM vuelve). (f) La **segunda cola** de `pwa.js` (`data-offline-queue`, sin un solo consumidor) quedó alineada con el candado: era una mina documentada como API. **NO se tocó la infraestructura de escritura dormida** y es deliberado: la auditoría halló que `Sync.php:460` escribe `estado='completada'`, valor que NO existe en el enum de la tabla (check-out roto al 100%), que los INSERT de dinero no verifican su retorno (un fallo se reporta como éxito y la idempotencia impide reintentarlo) y que el CSRF global —posterior al último cierre— rechazaría `/api/sync` con 403 sin rama que lo maneje en el cliente (cola en bucle infinito). Reabrir exige olas empezando por lo que no toca dinero; requisitos completos en el resultado de la auditoría y resumidos en `docs/QA-RECETAS.md`.
- **Captura offline OLA 1: `crear_huesped` reabierto (jul-30)**. `/api/sync` estuvo en 423 desde mayo; se reabre **solo alta de huésped**, la única de las 8 operaciones que ya comprobaba su INSERT, validaba hotel y no duplicaba. Contratos: (a) **el permiso es POR OPERACIÓN, no un booleano** — `Sync::OPERACIONES_HABILITADAS` es la fuente única, el layout la publica con `json_encode` en `window.MEDISOFT_OFFLINE_OPERACIONES` (cliente y servidor no pueden desincronizarse) y `escriturasHabilitadas(tipo)` **falla CERRADO sin tipo**; los 6 llamadores declaran el suyo. Un booleano global habría reactivado de un tirón los interceptores de caja, que es el desastre del 26-jul. (b) **Lista BLANCA, no negra**: la versión de julio rechazaba los tipos de dinero conocidos y dejaba pasar el resto — una operación nueva quedaba habilitada por omisión. (c) El candado se repite en `Sync::despachar` (defensa en profundidad: aunque un controlador nuevo acepte otro tipo, ahí muere). (d) **403 del CSRF**: el token vive en el `<meta>` y caduca a las 4 h, así que un equipo que estuvo horas sin red lo manda vencido; sin rama para 403 la cola **reintentaba para siempre sin drenar** — ahora avisa y espera a la recarga, que trae meta fresco y dispara el sync del arranque. (e) Se envía por **tandas de 200** (`MAX_OPERACIONES_POR_LOTE`) y el exceso vuelve en `restantes`: antes un lote grande recibía 400, que el cliente trata como error genérico → un equipo con 201 pendientes no sincronizaba nunca. (f) **El dedupe por teléfono ya no descarta en silencio**: si el teléfono existe con OTRO nombre, la operación se aplica (reutiliza el huésped) pero viaja un `avisos[]` que el cliente muestra — antes el recepcionista creía haber registrado a alguien que no existe (dos personas comparten teléfono más seguido de lo que parece). **Los 5 candados que fijaban el 423 se levantaron A LA VEZ** o el propio equipo revierte creyendo que hay regresión: `SyncBloqueadoTest` (reescrito: ahora vigila que la lista siga mínima y sin dinero, no que rechace todo), `ClasificacionComercialModulosTest`, `health_check_fase_1a.php` y los 2 preflights de nómina. Verificado E2E contra BD real: capturado sin red → subido al reconectar → fila con `hotel_id` correcto; `pago_caja` inyectado a la fuerza en la cola → rechazado con mensaje; teléfono repetido → aviso al operador. **Ampliar la lista sin arreglar lo suyo reintroduce el problema**: `checkout` escribe `estado='completada'` (ausente del enum), y las de caja no comprueban su INSERT ni bloquean el corte con FOR UPDATE.
- **Señal débil: el sistema dejó de esperar a una red que no responde (jul-30, SW v28)**. Queja del owner: cuando el internet del hotel se pone lento "todo el sistema se siente lentísimo" — y no era el sistema. Un `fetch` con mala señal **no falla, se cuelga**: la antena sigue viva, así que el navegador espera su propio límite (30 s, a veces más de un minuto) antes de rechazar, y hasta ese momento el SW ni se entera de que hay problema. La pantalla anterior queda congelada con la copia guardada lista al lado, el hotelero le pica otra vez y concluye que el software no sirve. **Todo el offline anterior (v25-v27) solo se activaba con la red CAÍDA, que es el caso raro; el común es la señal mala.** Arreglo: plazos por estrategia en la tabla `ESPERA` del SW (`conCopia` 3 s · `sinCopia` 9 s · `arranque` 4 s · `api` 7 s · `asset` 2.5 s), medidos contra **lo que podemos dar a cambio** — con copia guardada esperar más no compra nada, sin nada que servir sí vale aguantar. Contratos: (a) **las escrituras JAMÁS llevan plazo** — abortar un POST que quizá ya llegó al servidor duplica un cobro o inventa un check-in; el corte es solo para GET idempotentes, y el POST conserva su rama de siempre; (b) el plazo **no cancela** la petición de páginas/assets (`conLimite` + `mantenerVivo`): la respuesta que llega tarde refresca el caché y la siguiente navegación ya sale fresca — solo las de API se abortan (son GET que las vistas repiten solas y una fila de colgadas se estorba entre sí); (c) el camino feliz **no paga nada**: el caché se consulta al vencer el plazo, no antes (assert de <300 ms con red buena); (d) **"lento" y "sin conexión" son mensajes DISTINTOS y no son intercambiables** — decirle "sin conexión" a alguien cuyo internet sí funciona lo manda a reiniciar el módem y a culpar al sistema; el SW propaga `lenta` en la cinta (`data-motivo`, fondo azul, "Tu internet está muy lento"), en el 503 de API, en el muro (bandera `MEDISOFT_RED_LENTA` inyectada antes de su script) y en el postMessage `OFFLINE` que pwa.js adopta (`adoptarEstadoSinConexion`, etiqueta "Conexión lenta"); (e) el ping de `detectarConexionReal` ahora lleva `AbortController` a 4 s **porque es el que corre antes de guardar un huésped o cobrar** (`hayConexionAhora`): sin corte, "Guardando…" se quedaba medio minuto; (f) la **precarga especulativa** de instant-nav (`Sec-Purpose: prefetch`) se desvía a `soloRed`: si recibiera la copia guardada, el navegador la archiva en su caché de especulación y el clic abriría una pantalla vieja con cinta aunque la red ya esté bien — que la precarga falle es lo correcto (sigue alimentando la memoria offline cuando sí llega). De paso se corrigió un `ignoreVary` que faltaba en el respaldo de assets de `cacheFirst` (mismo gotcha de `Vary` de v25: sin él, un update de Safari dejaba ese respaldo sin encontrar nada — lo destapó el arnés, no se veía en el navegador). Verificación: `node tools/tests_js/sw_lentitud.test.js` (14, tarda ~40 s a propósito: mide los plazos REALES del archivo) + los 3 arneses en verde (20/14/14).
- **Precalentado de pantallas + pantalla sin conexión en escritorio (jul-30, SW v29)**. Hasta v28 solo se guardaba **lo visitado**, así que al caerse el internet faltaba justo lo que no se había tocado esa sesión — el owner lo vio en su PC: el muro le ofrecía Inicio, Habitaciones y Caja, **sin Reservaciones** (la ruta SÍ estaba en `PANTALLAS` de `offline.html`; lo que faltaba era la copia). Ahora `pwa.js` decide CUÁNDO (`programarPrecalentado`: `requestIdleCallback` con la página ya quieta, una vez cada 6 h por `localStorage['loscedros_precalentado_at']`, y solo si `conexionBuenaParaPrecalentar()` — respeta `saveData` y descarta 2g) y el SW hace el trabajo (`precalentarPantallas`, mensaje `PRECALENTAR_PANTALLAS`). Contratos: (a) las rutas se validan contra `OFFLINE_PAGE_PATHS` **dentro del SW** — un mensaje manipulado no puede hacerlo recorrer el sitio; (b) se salta lo que ya tiene copia, así que en régimen normal no pide nada; (c) de una en una, espaciadas 2.5 s, nunca en ráfaga (VPS de 2 vCPU con dos hoteles); (d) las rutas salen de `rutasOperativasVisibles()`, que lee los enlaces que el servidor YA pintó — así una recepcionista no gasta peticiones pidiendo Caja para recibir un 403; (e) se guarda con `new Request(url)` (sin User-Agent) y se lee con `ignoreVary`, que es lo único que hace que la copia se encuentre después — ver el gotcha del `Vary`. **La pantalla `offline.html` dejó de ser una tarjeta de móvil estirada**: desde 900px va a dos columnas (identidad del hotel a la izquierda, qué puede hacer a la derecha, accesos en rejilla de 2 y botón que ya no ocupa todo el ancho); verificado a 1280px (tarjeta 920px, `lado_a_lado`) y a 390px (apilada, sin scroll horizontal). GOTCHA al probar el precalentado en navegador: corre tan pronto hay idle que **para cuando mides ya terminó**, y parece que no hizo nada; la prueba determinista es el arnés (`dispararMensaje` sobre `listeners.message`). Y NO navegues dentro de esa prueba: un fetch con red refresca el caché por su cuenta y acabas midiendo eso.
- `dist/` = builds de deploy (Hostinger): **nunca editar ahí**, solo `src/`.

## Comandos

- **Radiografía (correr AL ARRANCAR y tras merges)**: `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tools/radiografia.php` — migraciones pendientes, tenancy, frescura CSS, errores en logs, tests. GOTCHA Git Bash: sin `MSYS_NO_PATHCONV=1`, MSYS rompe las rutas `/var/...` de todo `docker exec`.
- CSS: `npm run build:css` tras tocar clases Tailwind (o `watch:css`). GOTCHA: los globs de `tailwind.config` jamás deben incluir helpers/tcpdf/fonts (líneas de 1.1M chars → build de 5 min). GOTCHA jul-26: el JIT escanea los PHP como texto plano, así que una palabra suelta en un COMENTARIO en español que coincida con una utilidad ("invisible", "hidden", "block") genera su regla — un rebuild puede aparecer con clases que nadie usó. `tailwind.css` es una sola línea minificada: para saber si un rebuild cambió algo real, comparar SELECTORES (`sed 's/}/}\n/g' | grep -oE '^[^{]*' | sort -u` viejo vs nuevo), no el diff.
- Tests: `php src/tests/run.php` (casos en `src/tests/casos/`, concurrencia en `src/tests/concurrencia/`). **Todo caso DEBE terminar con `t_fin()`** (sin él los FAIL no tumban el runner y reporta verde); si un caso necesita un helper nuevo, cargarlo en `tests/bootstrap.php` espejo de `index.php`.
- Linter tenancy: `php src/tools/lint_tenancy.php` (baseline en `lint_tenancy_baseline.json`; no subir el conteo).
- Migraciones: `php src/tools/migrate.php` (SQL en `migrations/` de la RAÍZ del repo, ~100 archivos; `src/database/migrations/` es un directorio muerto). **Cómo correrlo aquí** (no hay PHP en el host): `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tools/migrate.php up` — el compose monta `./migrations` en `/var/www/migrations`, justo donde migrate.php lo busca (`dirname($src)`); para un WORKTREE (que no está montado) sigue haciendo falta el `docker run --rm -v <wt>:/repo -w /repo … php src/tools/migrate.php up`. **Sin `up` solo reporta** (el default es `status`, dry-run: se ve "PENDIENTE" y parece que corrió); subcomandos: `status`, `up`, `baseline` — `--aplicar` NO existe. GOTCHA: tras cualquier migración que cambie esquema, regenerar `src/database/schema.sql` (los tests recrean `medisoft_test` desde ahí; si queda viejo, la suite llena el log de `Unknown column`): `docker exec medisoft_hoteles_db mysqldump --no-data --skip-comments --ignore-table=medisoft_hoteles_import.vista_caja_actual -umedisoft_user -pmedisoft_pass medisoft_hoteles_import | sed 's/ AUTO_INCREMENT=[0-9]*//' > src/database/schema.sql`. **GOTCHA jul-28 — DOS tablas de control, no una**: el runner lee y marca `schema_migrations` (columna `archivo`), mientras cada .sql se auto-registra en `migrations` (columna `nombre`) desde su propio INSERT final; aplicar un .sql a mano llena SOLO la segunda y `migrate.php status` lo sigue reportando PENDIENTE. Y `up` corre TODAS las pendientes de golpe (no hay flag por archivo: `--una` aplica la SIGUIENTE en orden, que puede no ser la tuya) → con una migración AJENA pendiente (otra sesión o un rebase a medias) aplicar solo la propia con `docker exec -i medisoft_hoteles_db mysql -umedisoft_user -pmedisoft_pass medisoft_hoteles_import < migrations/X.sql` + `INSERT IGNORE INTO schema_migrations (archivo) VALUES ('X.sql')`. OJO al leer el resultado: `mysql … | grep -v 'Using a password'; echo "EXIT=$?"` reporta el código del GREP (1 = no hubo líneas), no el de mysql — verificar con un SELECT al estado real, no con ese exit.
- Tests en paralelo NO: dos corridas simultáneas se pisan (`run.php` hace DROP/CREATE de `medisoft_test` → deadlocks, `1146 table doesn't exist`, duplicados de semilla fantasma). Si fallan raro, revisar `SHOW PROCESSLIST` por otra conexión a `medisoft_test` antes de depurar. Con OTRA sesión Claude activa en la misma PC (casos reales jul-22 y jul-24: `1146 medisoft_test.X doesn't exist` a mitad de suite + corrida lenta = otra corrida hizo DROP/CREATE), aislarse con `MS_TEST_DB=medisoft_test_wt` (crear la BD+GRANT una vez con root; el runner y bootstrap la aceptan si contiene 'test'; en la Surface ya existe desde jul-24). GOTCHA jul-25: `medisoft_test_wt` ya NO es refugio seguro — la otra sesión también la usa y chocan (`1007 Can't create database ...; database exists` = la otra corrida la tiene tomada; confirmarlo con `SHOW PROCESSLIST | grep medisoft_test`). Cada sesión necesita SU BD: `medisoft_user` no puede crear bases nuevas (`1044 Access denied`), así que se crea con root DESDE DENTRO del contenedor sin imprimir el secreto — `docker exec medisoft_hoteles_db sh -c 'mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -e "CREATE DATABASE IF NOT EXISTS medisoft_test_X ...; GRANT ALL ON medisoft_test_X.* TO \"medisoft_user\"@\"%\";"'` (la contraseña root del compose es `DB_ROOT_PASS`, default `root_pass`; en la Surface existe `medisoft_test_sfc` desde jul-25). Una corrida completa tarda ~5-8 min: lanzarla con `run_in_background`, nunca esperar 120s.
- **QA de un WORKTREE en navegador**: el compose sirve el checkout PRINCIPAL, no el worktree → levantar contenedor hermano: `docker run -d --name tmp_smoke --network medisoft-hoteles_default -p 127.0.0.1:8090:80 -e APP_ENV=local -e DB_HOST=db -e DB_NAME=medisoft_test_wt -e DB_USER=medisoft_user -e DB_PASS=medisoft_pass -v "<worktree>\src:/var/www/html" medisoft-hoteles-app` (sembrar la BD corriendo la suite del worktree con `MS_TEST_DB`; borrarlo al final). La BD recreada de schema.sql tiene catálogo `modulos` VACÍO → TODO 403 (gotcha deploy fresco): sembrar `mysqldump --no-create-info medisoft_hoteles_import modulos planes plan_modulos | mysql <bd_test>`. GOTCHA jul-29: ese seeding truena con `ERROR 1136 Column count doesn't match value count` si tu BD de dev está ATRÁS en migraciones respecto al `schema.sql` del worktree (el dump trae menos columnas de las que la tabla nueva espera) — no pelear con el INSERT: `php src/tools/migrate.php up` contra la BD de dev primero (**`up`, no a secas**: sin argumento el comando es `status` y solo LISTA las pendientes, parece que no hizo nada). Otra salida cuando la BD de dev sirve: apuntar el contenedor hermano directo a `medisoft_hoteles_import` (datos reales de Los Cedros, catálogo ya sembrado) en vez de a la BD de prueba. GOTCHA sesión QA + RBAC: `can()` sin role_id cae a `can_legacy` que lee `usuarios.rol` GLOBAL (no el de `hotel_usuarios` ni el de sesión) Y `current_user()` cachea la fila en `$_SESSION['user']` → para probar como gerente: `UPDATE usuarios SET rol='gerente'` + el endpoint temporal debe hacer `unset($_SESSION['user'])`; el preset administrador NO trae `tarifas.*` (403 esperado). Correr tests del worktree sin tocar el contenedor: mismo `docker run --rm` con `-e DB_HOST=db -w /repo -v <worktree>:/repo … php src/tests/run.php Caso`.
- **Centinela de errores**: `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tools/centinela.php` — agrupa errores de logs por firma con estado persistente (NUEVA/REGRESIÓN gritan, conocidas callan; exit 1 = actuar). `resolver <firma> "nota"` al arreglar un bug, `ignorar <firma>` para ruido; `--json` = contrato para autocorrección futura con IA. Canal cli suele ser tests, no la BD real.
- **Observatorio del Copiloto**: `MSYS_NO_PATHCONV=1 docker exec medisoft_hoteles_app php /var/www/html/tools/observatorio_copiloto.php` — salud reglas/ia/fallback + preguntas caídas en fallback agrupadas por tema (firma de tokens sin orden/muletillas); `atendida <firma> "nota"` al enseñarle algo, `ignorar` para saludos/ruido; REGRESIÓN = se le enseñó y sigue cayendo; `--json` = contrato de automatización. GOTCHA: al fechar estado usar reloj de la BD, no PHP (relojes difieren 6h).
- Crons de referencia en `src/tools/cron_*.php` (night audit, ical, copiloto, cobros saas…).
- **Cuenta DEMO para prospectos**: `tools/saas/sembrar_hotel_demo.php` (`docker exec … php /var/www/html/tools/saas/sembrar_hotel_demo.php`, `--limpiar` para borrarla, `--confirmo-no-produccion` si `APP_ENV != local`; aborta duro si APP_ENV es production/prod). Crea el hotel `hotel-demo-medisoft` (usuario `demo_medisoft` / `DemoMedisoft2026*`, gerente) con 12 cuartos, 10 huéspedes, 13 reservaciones repartidas por estado, corte abierto con 9 movimientos, inventario con 2 productos bajo mínimo, 3 solicitudes de factura, 3 encuestas de reputación (5/3/2) y los 8 bloques a la venta contratados ⇒ cobro estimado $2,471/mes (base $999 del plan Básico + $1,472 de los 8). Siembra además lo que exigen los guiones de `docs/estrategia_comercial/20_guiones_demostracion.md` §3/§5/§6: 3 PDF REALES en `storage/documentos/hotel_<id>/<Y>/<m>/` (2 activos + 1 archivado para enseñar la recuperación, vinculados a huésped/reservación vía `documento_entidades`), una regla `incrementos_tarifas` global +25% vigente hoy→+7d, y `huespedes.descuento_tipo/valor` en un cliente frecuente. Los precios NO son constantes: un closure aplica el incremento cuando `fecha_entrada` cae en la ventana (mismo criterio que `calcularPrecioConIncremento`, que se evalúa con la fecha de ENTRADA y no noche a noche) y luego el descuento del perfil, así el invariante `precio_total = Σ detalle − descuento_total` se cumple solo; los 2 movimientos de caja ligados a reserva llevan `monto = null` = "lo que pagó esa reserva" (hardcodear el importe se desincroniza en cuanto una regla de tarifa cambia el total — pasó). Contratos del guion: TODO va en UNA transacción sobre la conexión del singleton `Database` (así `Rol::sembrarPresetsParaHotel`, `Caja::ensureDefaultCajaForHotel` y `Modulo::actualizarModulosHotel` ven `enTransaccion()=true` y participan en vez de abrir la suya) + `putenv('DB_STRICT_ERRORS=true')` al arranque, o `Database::query()` devuelve `false` en silencio y el rollback no protege de nada; re-ejecutar PURGA sus propios datos y vuelve a sembrar (idempotente, conserva hotel_id y el usuario). Las 5 tablas sin `hotel_id` que igual hay que limpiar se acotan por su padre: `control_llaves`/`historial_llaves`/`control_remotos`/`historial_remotos` por `habitacion_id IN (SELECT … WHERE hotel_id=?)` y `denominaciones_efectivo` por `corte_id`. **Una lista explícita de tablas NUNCA basta para borrar un hotel**: lo que se SIEMBRA no es lo que se LLENA usando la app — un solo recorrido por el navegador dejó 9 filas en `usuario_preferencias_nav` + `logs_acceso` que ninguna lista preveía, y como esas tablas no tienen FK a `hoteles`, borrar la fila del hotel las deja HUÉRFANAS apuntando a un id inexistente (no truena, no se ve). Patrón: lista explícita para el orden de FKs + **barrido dinámico** sobre `information_schema` de toda tabla con columna `hotel_id`, en pasadas con `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` (un 1451 reintenta en la siguiente pasada, cuando el hijo ya está vacío) y `throw` si una pasada no avanza. Verificar con `SELECT COUNT(*) … WHERE hotel_id NOT IN (SELECT id FROM hoteles)` sobre todas ellas: debe dar 0. GOTCHA de smoke por curl: el form de login postea a **`/login/authenticate`** (no `/login`) y el campo es **`nombre_usuario`** (no `usuario`) — con el endpoint o el nombre equivocado el 303 vuelve a /login y parece contraseña mala; y la ficha de reservación es `/reservaciones/ver/{id}`, `/reservaciones/{id}` es 404.
- **Un script CLI que escribe en `storage/` corre como ROOT y el sitio como `www-data`** (jul-26, mordió al sembrar el Centro documental): `docker exec` entra como uid 0, así que un archivo creado con 0640 y un directorio con 0750 quedan `root:root` y Apache **no puede leerlos** → `Documento::resolverRutaPrivada` lanza y la descarga contesta 303 "El archivo privado no está disponible" con el archivo perfectamente presente en disco (el 303 va a `/documentos/{id}`; si fuera a `/documentos` el problema sería la fila, no el archivo — el destino del redirect distingue las dos causas). Peor que el síntoma: si el script es el PRIMERO en crear la raíz compartida `storage/documentos/`, la deja root:root y rompe la subida REAL de todos los hoteles, no solo la demo. Regla: tras crear cualquier cosa ahí, `chown`+`chgrp` a `www-data` (uid/gid 33, vía `posix_getpwnam`) en CADA nivel incluida la raíz, conservando 0750/0640; abrir permisos (0755/0644) solo como último recurso sin posix. Verificarlo de verdad = pedir el archivo por HTTP y comprobar `content_type: application/pdf` + `size_download` igual a `size_bytes`, no `ls`.
- **Suite completa = ~2h con la máquina ocupada** (cada caso hace DROP/CREATE del esquema). Para una auditoría o un cambio acotado, correr el SUBCONJUNTO dirigido (`run.php <Caso>` uno por uno, ~2 min c/u) en vez de la corrida entera; los 5 casos comerciales dan 162 asserts en minutos. GOTCHA de diagnóstico: `docker exec … run.php | tail -N` BUFFEREA toda la salida hasta terminar, así que un archivo de salida en 0 bytes NO significa que esté colgada — comprobar vida con `SHOW PROCESSLIST` o `ps aux | grep tests/`.
- **Aislamiento real entre dos sesiones Claude** (verificado jul-25): `MS_TEST_DB=medisoft_test_wt` corrió en paralelo con otra sesión sobre `medisoft_test` sin un solo deadlock ni `1146`. Confirmar con `SHOW PROCESSLIST` que cada corrida está en su BD antes de culpar a la concurrencia.
- **Cap de subagentes en la Surface = 2** (4 núcleos → `min(16, cores-2)`): un fan-out de 8 agentes se serializa en 4 oleadas. Preferir POCOS agentes profundos a muchos superficiales, y no lanzar 2 workflows a la vez (satura y ralentiza todo, incluidos los tests). Si un agente devuelve mucho texto estructurado, acotar el schema con `maxItems`/`maxLength`: dos bloques de auditoría murieron por exceder el máximo de 32k tokens de salida.

## Infra de producción (Fase 2 estabilización, jul-17)

- **Proxy confiable**: `helpers/proxy_confiable.php` (funciones puras + `ms_proxy_aplicar()` llamado en index.php ANTES de las cookies). Solo honra `X-Forwarded-For/-Proto` si `REMOTE_ADDR` ∈ `TRUSTED_PROXIES` (.env, IPs/CIDRs). Detrás de Caddy: cookies `Secure`+HSTS y `login_intentos` registra la IP real (verificado E2E). Dev sin la var = comportamiento histórico. Suite `ProxyConfiableTest.php`. GOTCHA: `.env` entra por `env_file` del compose → var nueva exige `docker compose up -d app`; el loader de index.php lee `ROOT_PATH/.env` que NO existe en el contenedor.
- **Puertos dev solo loopback**: compose publica app/MySQL/phpMyAdmin en `127.0.0.1` (`DEV_BIND` en .env para LAN deliberada).
- **RBAC legacy**: `can_legacy` resuelve el rol-string contra el PRESET homónimo de `config/permisos.php` (`legacy_preset_permissions()`) → los 3 hotel_usuarios sin role_id ya conocen `reservaciones.*`/`documentos.*`/`tareas.*` con paridad; el gotcha can_legacy queda resuelto. Suite `LegacyRbacCompatTest.php`. Gates de enforcement (pasos 2-6 del plan) PENDIENTES de aprobación en `docs/PLAN-AUTORIZACION-OPERATIVA.md`.
- **PHPStan**: cubre `core`+`services`+`models` (nivel 0, baseline). Helpers globales via `scanFiles` (JAMÁS `scanDirectories` sobre helpers/ completo: tcpdf). Controllers = siguiente lote. Correr: `docker run --rm -v <repo>:/repo -w /repo medisoft-hoteles-app php vendor/bin/phpstan analyse --no-progress` (no hay PHP en el host).
- **VPS producción (jul-20, LIVE)**: Vultr CDMX (216.238.90.237), Ubuntu 24.04, `/opt/medisoft`, `docker-compose.prod.yml` (Caddy HTTPS auto + app + MySQL). Dominio **medisoft-hoteles.com** (Cloudflare, DNS-only). SSH `~/.ssh/medisoft_vps`. Detalle+secretos: [[deploy-vps-produccion]]. **GOTCHA deploy fresco (crítico)**: `schema.sql` es `--no-data` → importarlo + `migrate.php baseline` deja los CATÁLOGOS VACÍOS (`modulos`/`planes`/`plan_modulos`), porque baseline marca las 102 migraciones como aplicadas SIN ejecutar sus INSERTs. Sin catálogo `modulos`, `listarActivosDeHotel` no devuelve ni los `es_core` → TODO gateado con 403 (dashboard incluido). Fix: sembrar catálogo desde la BD de dev (`mysqldump --no-create-info medisoft_hoteles_import modulos planes plan_modulos` → cargar en prod); los 7 core (dashboard/habitaciones/reservaciones/huespedes/caja/configuracion/usuarios) quedan activos para cualquier hotel sin fila en `hotel_modulos`. **GOTCHA login sin hotel**: un usuario sin fila en `hotel_usuarios`→hotel activo NO puede entrar por `/login` (BUG-001, `resolve_default_hotel_context_for_user`); el owner SaaS necesita ≥1 hotel activo asignado (gerente) para autenticarse y de ahí acceder a `/admin/saas/hoteles` (gateado por `saas_admins`). `promover_saas_admin.php` solo corre en `APP_ENV=local`; en prod se inserta `saas_admins` a mano.
- **Staging bajo demanda + vigilante (jul-27)**: `docker-compose.staging.yml` + `tools/staging.sh` (`up`/`restaurar`/`estado`/`down`/`destruir`) levantan una copia desechable en `/opt/medisoft-staging` — directorio, `.env` y volumen PROPIOS (jamás comparte `src/` con prod: si lo compartiera, editar staging editaría producción), reusando la imagen ya construida `medisoft-app:latest`, escuchando SOLO en `127.0.0.1:8090` (se entra por túnel SSH) y con `restart:"no"` a propósito (comparte los 3.9 GB con prod: ~450 MB mientras corre, apagarlo al terminar). CONTRATO: el `.env` de staging se DERIVA del de prod **neutralizando** WhatsApp/Anthropic/VAPID/pasarela/HEALTH_TOKEN/llaves de B2 (`neutralizar_env`) — un staging con el token real le manda mensajes a huéspedes REALES y escribe sobre los respaldos; por eso nunca se levanta con `docker compose` a mano. Sirve para drill de restauración, QA de deploys y migraciones; **NO para k6** (comparte los 2 vCPU con prod → número contaminado y golpe al hotel). Drill jul-27: restaurar el dump real = **5 s, 126 tablas**. · `tools/watchdog_infra.sh` (cron 7:45, log `/var/log/medisoft_watchdog.log`) cubre lo que un monitor HTTP no ve: contenedores, `/health`, disco ≥85%, RAM disponible <300 MB, backup local >30 h, rastro de offsite y caducidad del cert TLS; sale 1 si algo falla. NO sustituye al monitor externo (un vigilante dentro del VPS no puede avisar que el VPS murió) y NO usa WhatsApp como canal (`WHATSAPP_SEND_ENABLED=false` + token pendiente de rotar): hay hook `WATCHDOG_NOTIFY_CMD` para cuando haya canal.
- **GOTCHA de sesiones concurrentes en prod (jul-27, caso real)**: `docker ps` mostró `app  Up 10 minutes` sin que esta sesión lo reiniciara. Diagnóstico correcto ANTES de asustarse: `docker inspect -f 'RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'` — `RestartCount=0` + `ExitCode=0` + sin OOM = reinicio DELIBERADO (un `docker restart` manual no incrementa RestartCount), no un crash loop; `docker events --since 2h --filter container=<n>` muestra quién hizo qué (ahí se vio el `php -l` del deploy de la OTRA sesión). Corolario útil: `docker restart` CONSERVA el sistema de archivos del contenedor (un archivo puesto con `docker cp` sobrevive); solo `compose up -d` que RECREA el contenedor lo pierde — por eso el bind mount del compose es la red de seguridad del `docker cp`.
- **Backup/restore**: `tools/backup_db.sh` (dump BD+uploads a `backups/`) y `tools/restore_db.sh --verificar`; restauración COMPROBADA jul-17 (120 tablas, conteos idénticos). **Offsite (jul-27)**: §4 del script sube dump+uploads+`.env` cifrado a Backblaze B2 y RE-LEE el destino para verificar que el archivo llegó (que rclone no truene ≠ que subió); rclone ya instalado en el VPS. El remote se arma con `RCLONE_CONFIG_OFFSITE_*` desde `BACKUP_B2_ACCOUNT/_KEY/_BUCKET` del `.env` — sin `rclone config`, todos los secretos en un solo lugar, y `RCLONE_CONFIG=/dev/null` SOLO en esa rama (silencia el NOTICE diario; un remote hecho a mano sí debe leer su .conf). `BACKUP_ENV_PASSPHRASE` cifra el `.env` con openssl AES-256 (sin él un restore no arranca; la passphrase va al gestor de contraseñas del owner, no solo al `.env`). Alta del bucket y receta de restore desde B2: `docs/RUNBOOK-PRODUCCION.md` §Copia offsite. GOTCHA que costó encontrar: `env_val()` termina en `|| true` porque con `set -euo pipefail` una llave AUSENTE en .env hace fallar el grep, pipefail propaga y la asignación **aborta el backup a media corrida en silencio** — toda llave opcional nueva depende de ese `|| true`.
- **Resiliencia de prod — estado VERIFICADO jul-27** (no re-auditar por SSH sin necesidad; radiografía con `ssh -i ~/.ssh/medisoft_vps root@216.238.90.237 'crontab -l; ls -lht /opt/medisoft/backups/db | head; df -h /; docker ps'`). Automático: los 3 servicios traen `restart: unless-stopped` + `docker` `enabled` al boot → crash de contenedor y reboot del VPS se recuperan solos (segundos / 1-2 min), y la rotación de logs (10m×3) tapa la causa #1 de VPS llenos. Crons instalados en prod (deploy 2026-07-20, NO viven en el repo): ical `*/30`, **backup 3:30am**, night audit 4am, resumen IA 7am, cobros SaaS 8am, truncate del slow.log el día 1, y un `restore_db.sh --verificar` del último dump el día **2 de cada mes** (instalado jul-20 → su PRIMERA corrida real es el 2026-08-02, aún sin evidencia en `/var/log/medisoft_backup.log`). Huecos reales, por urgencia: (1) ~~backups solo en el VPS~~ **CERRADO jul-27** con el offsite a B2 (código y rclone listos; queda que el owner cree bucket+llave y pegue `BACKUP_B2_*` en el `.env` de prod — hasta entonces el log avisa `NO hay copia fuera del VPS` a diario); (2) sin snapshot de Vultr → reconstruir es `provision_vps.sh`+deploy+restore a mano; (3) `/health` funciona pero NADIE lo consulta → una caída se entera por queja del hotelero; (4) ~~el `.env` no se respalda~~ CERRADO: viaja cifrado al offsite si hay `BACKUP_ENV_PASSPHRASE`. Durante una caída recepción NO opera (la PWA cachea el shell, pero cobrar/check-in/caja exigen servidor) → papel.
- **Capacidad del VPS y tuning — MEDIDO jul-27** (2 vCPU / 3.9 GB; 156ms mediana desde fuera con la máquina ociosa). Huella real por hotel: Los Cedros (49 cuartos, 2029 reservas, 1606 huéspedes, 2 años de historial migrado) pesa **<10 MB**; BD entera 16.8 MB con 57 GB libres → los datos NO son el límite, ni de lejos. El techo REAL es el disco por DOCUMENTOS: a la cuota planeada de 2 GB/hotel caben ~25 hoteles (y esa cuota + aviso 80% + bloqueo siguen SIN existir en el código). Cómodo hoy 20-30 hoteles; de 30 en adelante, subir RAM. El baseline de `docs/prueba-carga-baseline.md` (100 usuarios, p95 162ms) se corrió en la PC de dev, **nunca contra el VPS** — sirve para decir "no hay cuello de botella de código", no como capacidad de esta máquina; y correr `tools/k6/carga.js` contra PROD **bloquearía la cuenta real** (LoginRateLimiter 15/15min vs ~979 logins del script): esa prueba exige staging.
- **Tuning aplicado EN CALIENTE jul-27 (cero downtime, con el hotel operando)**: (a) Apache prefork venía con el default `MaxRequestWorkers 150` y cada worker mide ~35 MB → 5.2 GB, MÁS RAM de la que tiene la máquina: un pico de tráfico habría hecho que el OOM killer matara a MySQL (mina enterrada, no problema visible: en reposo solo viven 5 workers). Ahora 40 + `MaxConnectionsPerChild 1000` (los hijos de prefork NO se reciclan por default → toda fuga de PHP se acumula por días) vía `docker/php/apache-prod.conf`, montado en el compose de prod como `conf-enabled/zz-mpm-prod.conf`. (b) buffer pool 2G→1G. Presupuesto resultante: Apache 1.4G + MySQL 1.3G + SO/Caddy 0.4G = 3.1 de 3.9 GB. GOTCHAS que costaron tiempo: (1) el buffer pool solo acepta múltiplos de `chunk_size × instances` (defaults 128M × 8 = **mínimo real 1G**) y MySQL **redondea hacia arriba EN SILENCIO** — poner 512M en el .env da 1G igual; verificar siempre con `SELECT @@innodb_buffer_pool_size, @@innodb_buffer_pool_instances`, y es DINÁMICO en MySQL 8 (`SET GLOBAL`, sin restart). (2) Recargar Apache sin cortar conexiones: `docker cp` del .conf + `apachectl -t` + `apachectl graceful` (el maestro conserva su PID, solo rotan los hijos) — evita recrear contenedores en horario de operación. (3) `conf-enabled/` carga DESPUÉS de `mods-enabled/` (líneas 222 vs 147 de `apache2ctl -t -D DUMP_INCLUDES`) → por eso el archivo pisa a mpm_prefork.conf sin editarlo; **`-D DUMP_RUN_CFG` NO lista directivas de MPM** (no sirve para verificar MaxRequestWorkers). Para PROBAR que Apache lee un .conf: meterle una directiva inventada y ver que `apachectl -t` truena señalando ESE archivo y línea. (4) **`docker exec -i` dentro de un `ssh 'bash -s' <<EOF` se COME el resto del script** (ambos compiten por el mismo stdin y el heredoc ES el stdin): dentro de heredocs usar `docker exec` SIN `-i`; el `-i` solo con `< archivo`.
- **Migración legacy Los Cedros → hotel 2 prod**: `tools/migracion_loscedros/` (migrar.sql re-ejecutable + verificar.sql 26 checks + README runbook día D). Ensayo 26/26 OK en prod 2026-07-21; detalle vivo en memoria [[migracion-legacy-loscedros]]. GOTCHA MySQL: una variable de usuario DEGRADA un valor JSON a string — al re-embeberlo en JSON_OBJECT va `CAST(@var AS JSON)` o queda doble-codificado (bug real del primer ensayo); y las columnas GENERATED (denominaciones_efectivo.subtotal) no se insertan.
- **Migración legacy San Nicolás → hotel 3 prod (EJECUTADA jul-29, 40 checks 0 FAIL)**: `tools/migracion_sannicolas/` — `dia_d.sh` la corre COMPLETA en un comando (respaldo→staging→migrar→verificar, se corta al primer error) desde **Git Bash** (desde CMD el `\$` no se expande y MySQL responde "Access denied" con la contraseña literal); migrar.sql re-ejecutable con mapas old→new y foto de tenancy `_tenancy_pre` + verificar.sql 40 checks + seed_clon_local.sql + README con el estado en prod. Ensayo local 0 FAIL + QA de app en contenedor hermano contra `medisoft_prod_clone`; auditoría adversarial (3 agentes) sin severidad alta — 4 decisiones del owner pendientes antes del día D (README §Hallazgos: 20 confirmadas descuadradas $18,850, cuenta admin fusionada, "Baño de burbujas" sin descuento auto, 117 confirmadas con $0 pagado porque el legado dejó de registrar pagos en abril). Esta vez los `.sql` SÍ viajan por git (excepción en .gitignore; los de Los Cedros nunca viajaron y hubo que reconstruir de cero en la otra PC). Gotchas de dump MariaDB (vistas rotas → cargar staging con `--force`, enum `''` → normalizar) documentados en el README; staging `sannicolas_legacy` + clon viven en el MySQL local de la PC principal.
- **Deploy de código a prod (verificado jul-22)**: `/opt/medisoft` NO es checkout git — se despliega por `scp` de archivos sueltos desde la PC (`scp -i ~/.ssh/medisoft_vps <archivos> root@216.238.90.237:/opt/medisoft/src/...`) + `chown www-data:www-data` + `cd /opt/medisoft && docker compose -f docker-compose.prod.yml restart app` (opcache) + smoke curl (home 200, ruta nueva 303→/login). ANTES de pisar un archivo: comparar contra la versión git esperada con `diff --strip-trailing-cr` (los md5 SIEMPRE difieren por CRLF de Windows en scp previos — no es divergencia real). Si el cambio trae migración, correrla en prod aparte. **Receta de deploy con red (jul-26, 22 archivos)**: en vez de scp suelto, `tar` del lote → subir a `/tmp` → respaldar lo vivo (`tar --ignore-failed-read -cf backups/pre_deploy_<ts>.tar -T <lista>`; SIN ese flag un archivo NUEVO —que en prod no existe— aborta todo el script con `set -e`) → extraer a `/tmp/stage_<ts>` → verificar contra un manifiesto de md5 NORMALIZADOS (`tr -d '\r' < f | md5sum`, que sí compara de verdad pese al CRLF) → `cp -a stage/src/. /opt/medisoft/src/` → chown → verificar el manifiesto OTRA VEZ ya en su lugar → restart → borrar staging y dejar el tar de respaldo (rollback de 1 comando). Smoke honesto SIN inventar endpoints de sesión en prod (jamás): los 303 solo prueban el router, así que además `php -l` de cada archivo desplegado + `docker exec … php -r 'require "…/app/services/X.php"; …'` ejercitando las funciones PURAS nuevas contra el código YA desplegado (sin BD ni efectos). Comparar prod contra la baseline git del deploy anterior (no solo contra HEAD) delata ediciones a mano antes de pisarlas. **La verificación prod↔origin es BIDIRECCIONAL (jul-29, segunda mitad del gotcha)**: no basta comprobar que prod no esté ADELANTADO por ediciones a mano; también hay que ver que el REPO no esté atrás por trabajo desplegado sin commitear. Auditoría completa de los **1213 archivos** de `src/` (`git -c core.quotepath=false ls-tree -r --name-only origin/<rama> -- src/` + md5 normalizados a los dos lados) encontró exactamente 2 derivas, **una en cada dirección**: (a) `public_html/css/custom.css` tenía en PROD 33 líneas que el repo NO tenía —el apagado de selección táctil del 27-jul, desplegado a mano desde el árbol sucio y jamás commiteado (`git log -S` en todas las ramas: cero)— así que "igualar prod con origin" lo habría BORRADO en todos los celulares, en silencio; se rescató al repo (commit `a715c9a`) sin desplegar nada; (b) una vista huérfana de caja donde prod estaba atrás por un texto. **GOTCHA de medición que esconde derivas**: sin `core.quotepath=false`, `git ls-tree` devuelve los nombres con acentos ENTRECOMILLADOS con escapes octales (`"src/…Configuraci\303\263n…"`), el `[ -f "$f" ]` falla en LOS DOS lados y la diferencia real se disfraza de "falta en ambos" → la segunda deriva fue invisible en la primera pasada. Correr esta auditoría completa antes de un deploy grande cuesta ~3 min y es la única forma de ver las dos direcciones. **GOTCHA jul-30 — el smoke que calla en silencio y te deja creer que verificaste**: en un script con `set -euo pipefail`, la forma `echo "$SW" | grep -q PATRON && echo "OK: ..."` da FALSO NEGATIVO **intermitente**: `grep -q` sale al primer match y cierra el pipe, el `echo` que aún escribe recibe SIGPIPE (141) y **`pipefail` marca el pipeline entero como fallido aunque el patrón SÍ estaba**; y como es una lista `A && B`, `set -e` NO aborta (POSIX ignora el fallo de cualquier comando de una lista AND-OR que no sea el último) → la línea simplemente no se imprime y el deploy termina "en verde" con un check menos. Pasó en el deploy del arranque offline: el check del fallback no salió pese a que el archivo estaba bien desplegado y su md5 coincidía extremo a extremo — se detectó porque FALTABA una línea en la salida, no porque algo gritara. Usar `grep -c PATRON` (lee todo el stream, no cierra temprano) y comparar el número, o `if grep -q …; then …; else exit 1; fi`. Regla general: **en un smoke, un check que no puede fallar ruidosamente no es un check**.
**GOTCHA jul-29 (casi se revierte medio producto)**: prod corre `origin`, NO tu HEAD local — con la rama local 13 commits atras, copiar los archivos de la feature habria BORRADO en el hotel vivo la cuota de documentos, la clasificacion comercial del catalogo y el retiro de Cuentas por cobrar (94 archivos de diferencia, ~7.3k lineas), sin ningun sintoma hasta que el hotelero abriera esas pantallas. Antes de armar el lote: `git fetch` + `git status -sb` (¿behind?) y md5 normalizados de prod vs **`origin`** archivo por archivo — que prod coincida con origin en TODO el lote es la luz verde. Si la rama esta atras, NO rebasees el arbol sucio: `git worktree add --detach <tmp> origin/<rama>` y porta la feature con `git merge-file <origin> <base=tu HEAD> <local>` archivo por archivo (los conflictos que salgan son trabajo REAL duplicado entre las dos lineas: aqui el mismo "Configuracion solo Medisoft" estaba hecho dos veces con flags distintos — gano el que ya vivia en prod). Cuidado con un test que quede describiendo el flag descartado: la suite lo caza. **GOTCHA jul-27 — commitear con OTRA sesión escribiendo en el mismo árbol**: `git add CLAUDE.md` (o cualquier doc vivo) se lleva las líneas de la otra sesión, y al revés: un `git commit` amplio de la otra sesión SE LLEVÓ mi línea de CLAUDE.md a medio trabajo (caso real: `090fb15` "fix(cotizaciones)" incluyó la entrada de MODO FECHA). Antes de commitear: `git status --short` y `git diff -U0 <doc> | grep '^@@'` para contar hunks; **GOTCHA jul-30 al medir el tamaño real de tu cambio**: con `core.autocrlf` activo, `git diff --stat` reporta archivos ENTEROS reescritos (un borrado de 118 líneas salió como "1100 líneas cambiadas" en `ReporteCortePDF.php`, y da un susto de "sed me destrozó el archivo") — usar `git diff --ignore-cr-at-eol --numstat` para ver el diff verdadero antes de asumir daño; si hay ajenos, NO uses `git add <doc>` — extrae solo tus hunks a un patch y stagéalo con `git apply --cached` (`git add -p` es interactivo y NO está disponible aquí; tampoco hay Python en el host, usar awk: bufferear por `^@@` y emitir solo el hunk que contenga tu marcador). `MM <archivo>` en `git status` = quedó bien (tu parte staged, la ajena en el árbol). Ojo al verificar el patch: las viñetas markdown hacen que las líneas del diff empiecen con `+-`/`--`, así que `grep -E '^[+-][^+-]'` da CERO falsamente — usar `grep -E '^[+-]' | grep -vE '^(\+\+\+|---)'`. Con DOS sesiones activas (PC principal + otra), fetch justo antes del push: el remoto se mueve (caso real jul-22: rebase limpio porque los cambios eran aditivos en routes.php/CLAUDE.md). Si además el árbol local está SUCIO con trabajo ajeno que se SOLAPA con lo que avanzó el remoto (caso real jul-25: 25 archivos), NO rebasear con autostash (el pop conflictúa sobre trabajo que no es tuyo): `git worktree add --detach <tmp> origin/<rama>` + cherry-pick del commit propio + push desde ahí + `git worktree remove`; la rama local queda ahead-1 y se auto-sana al siguiente `pull --rebase` (patch-id idéntico se descarta solo). GOTCHA jul-27 (deploy de UN archivo): para un solo archivo el tar es exceso — `scp` a `/tmp/stage_<ts>.php` + `cp -a` del vivo a `backups/<archivo>.pre_deploy_<ts>` basta y el rollback sigue siendo 1 `cp`. DOS trampas de escapado: (a) `php -l /tmp/x.php` vía `docker exec` NO ve el `/tmp` del HOST (filesystems distintos) → `docker cp` al contenedor primero, o linteas un archivo inexistente y el `|| php -l` de respaldo truena con `php: command not found` (el VPS no tiene PHP fuera de Docker); (b) verificar contenido con `docker exec … php -r '…'` a través de ssh mete TRES capas de comillas y los `\$` se comen solos — un `strpos` da falso negativo aunque el código SÍ esté (pasó: reportó "fix presente: NO" con el fix desplegado). Para asertar sobre el archivo desplegado usar `ssh '…'` con comilla SIMPLE + `grep -c` directo sobre `/opt/medisoft/src/...`, no PHP anidado.
- **BAJAR lo de la otra PC sin subir lo propio (verificado jul-26, 13 commits / 16 conflictos)**: `git fetch` + `git rebase origin/<rama>` con el árbol sucio guardado en un stash NOMBRADO (`git stash push -m … <archivo>`, no autostash: hay otro stash viejo y el índice se corre); tag de respaldo antes de empezar (`git tag respaldo-…`) y NUNCA `push` si lo pedido es solo integrar. Un commit propio que la otra PC ya pusheó se descarta SOLO (patch-id idéntico: comprobarlo antes con `git show <sha> | git patch-id --stable` en los dos, ahorra revisar un conflicto entero). Resolver "quedarme con el lado del remoto" hunk por hunk, NO con `git checkout --ours <archivo>` (eso descarta también lo que ya se auto-fusionó bien en ese archivo): `sed -i -e '/^<<<<<<< /d' -e '/^=======$/,/^>>>>>>> /d' <archivo>` = keep-ours quirúrgico, y con los rangos al revés (`/^<<<<<<< /,/^=======$/d` + `/^>>>>>>> /d`) = keep-theirs. Cerrar SIEMPRE con `php -l` de cada archivo tocado (en el contenedor: no hay PHP en el host) + `grep -rn '^<<<<<<<\|^>>>>>>>'` en todo el árbol. **TRAMPA que costó rehacer los commits: `git add -A` a mitad de un rebase absorbe los UNTRACKED al commit que se está aplicando** (21 .md de estrategia comercial acabaron dentro de un commit de código): stagear por ruta explícita, y si ya pasó, arreglarlo SIN rebase interactivo (prohibido en este entorno) = `git checkout --detach <base>` + cherry-pick del commit + `git rm -r --cached <rutas>` + `git commit --amend --no-edit` + cherry-pick del resto + `git branch -f <rama> HEAD`. Tras integrar: `migrate.php status`+`up` (cada PC tiene su BD), radiografía, verificador comercial y el SUBCONJUNTO de tests que cubre lo resuelto a mano — la radiografía marca "build:css pendiente" por MTIME después de cualquier merge (falso positivo: si `git diff HEAD~N HEAD -- src/public_html/css/tailwind.css` sale vacío, los commits propios no necesitaron clases nuevas; en la Surface no hay `node_modules`, `npm run build:css` truena con "tailwindcss no se reconoce"). **GOTCHA jul-28 — con un rebase AJENO en curso, los documentos vivos son minas**: la regla de alimentación garantiza que casi todo commit toque CLAUDE.md/CEMENTERIO.md/docs/QA-RECETAS.md, así que dejarlos sucios aborta el `rebase --continue` de la otra sesión con "local changes would be overwritten by merge". Antes de alimentarlos, comprobar `ls .git/rebase-merge` (existe = rebase vivo; `MERGE_HEAD` no aparece, un rebase NO es un merge) y listar qué reescriben los commits que faltan: `cat .git/rebase-merge/git-rebase-todo` + `git show --name-only --format="" <sha> | grep -x CLAUDE.md`. Si sale, ESPERAR a que termine y alimentar después; mientras tanto se puede trabajar sin miedo en archivos que esos commits no tocan (comprobado con el mismo `git show --name-only`). Corolario: nunca commitear con un rebase ajeno vivo — el commit aterriza dentro de la reproducción de otro.
- **Rebase PROPIO atorado por git concurrente de la otra sesión (jul-28)**: `git rebase --continue` puede negarse gritando "must edit all merge conflicts" con `git ls-files -u` VACÍO (sequencer desincronizado). Salida sin perder nada: commitear lo staged a mano (`git commit --no-edit -C <sha-original>`), `git rebase --quit` (JAMÁS `--abort`: quit conserva árbol e WIP ajeno; abort hace reset duro) y terminar con `git cherry-pick` de los commits restantes + `git branch -f <rama> HEAD` + checkout. El WIP ajeno que aparezca a MITAD del rebase (archivos modificados/untracked que tú no hiciste) se respalda a scratchpad y NUNCA se stagea (add por ruta explícita). GOTCHA tar de Git Bash: `tar -cf C:/ruta.tar` interpreta `C:` como host remoto ("Cannot connect to C") → usar `/c/...` o `--force-local`.
- **Dos ramas que tocan el MISMO gate llegan con dos helpers de nombre distinto** (jul-26, caso documentos): el remoto traía `puede_ver_documentos_vinculados()` (fail-OPEN, vía `hotel_menu_module_enabled`) y la rama local `documentos_entidad_visible()` (fail-CLOSED) para exactamente el mismo candado, y el auto-merge dejó **las dos vivas** en `helpers/modulos.php` + variables de AMBOS lados usadas en la misma condición del cuerpo del archivo (`documentos_entidad.php`: `$documentosEntidadPermiteVincular` del remoto Y `$documentosEntidadPuedeGestionar` del local) ⇒ resolver el conflicto de cabecera a un solo lado deja el cuerpo con variables indefinidas. Patrón: unificar haciendo que el nombre viejo DELEGUE en el fail-closed (para PII la degradación correcta es ocultar) y conservar las dos variables. Mismo cuidado al revés: el remoto BORRÓ `$sidebarEsGestion` de `sidebar.php`, así que el lado local del conflicto la referenciaba sin definición — al quedarse con un lado, `grep` que sus variables sigan existiendo en el archivo fusionado.
- **Consultar BD de prod (read-only) desde cualquier PC sin imprimir secretos**: `ssh -i ~/.ssh/medisoft_vps root@216.238.90.237 "docker exec -i medisoft_hoteles_db sh -c 'mysql -uroot -p\"\$MYSQL_ROOT_PASSWORD\" --table medisoft_hoteles' 2>&1 | grep -av 'Using a password'" < consulta.sql` (BD prod = `medisoft_hoteles`; SQL por stdin desde archivo — el quoting inline se enreda). GOTCHA: sin `-a` grep declara "binary file matches" y TRAGA el resto del output en cuanto un valor JSON de `hotel_configuracion` trae chars no-ASCII; con columnas dudosas usar `--force` para que un 1054 no aborte el resto del archivo. GOTCHA jul-30: un ALIAS con acento/ñ (`AS dueños`) en ese SQL por stdin vuelve del servidor como **1064 syntax error** señalando basura mojibake — el pipe no viaja en UTF-8; alias siempre en ASCII (`AS duenos`). Truco de oro antes de un DELETE de datos: correr el MISMO predicado como `SELECT ... CASE WHEN <cond> THEN 'SE BORRA' ELSE 'se conserva' END` contra local **y contra prod**; así se vio que el candado de la migración de propietarios limpiaba Hotel base/Demo y respetaba Los Cedros, sin tocar prod.

## Invariantes — NO ROMPER

- **Tenancy**: toda query nueva filtra por `hotel_id`; el linter vigila.
- **Caja**: 3 candados de concurrencia de cortes (un corte abierto por caja, movimiento no cae en corte cerrado, cierre atómico) — patrón `FOR UPDATE` + `enTransaccion` (commit 875c51bd).
- **Nómina v2**: crédito = bruto − ledger absorbido + candado anti doble pago en anular/reabrir (b275390d). La vista vieja trabajadores/nomina sigue siendo la pantalla de PAGO — no borrar; `/nomina` nueva es fachada sin pago.
- **Anticipos**: ligados a caja vía `reservacion_abonos` + AnticipoService; check-in es saldo-aware (sin doble cobro).
- **Descuentos**: motor reusa `IncrementoTarifa(clase)`; el form activo de reservación es `crear.php` (jQuery) — `crear.js` está muerto.
- Acciones del Copiloto por chat: propuesta determinista → confirmación humana → motor de pantalla estándar (nunca SQL directo desde IA).
- Alta de hotel (SaaS): `guardarHotelAction` siembra roles (`Rol::sembrarPresetsParaHotel`) Y caja principal (`Caja::ensureDefaultCajaForHotel`, idempotente FOR UPDATE) en try/catch no bloqueantes — un hotel nuevo ya viene con caja aunque el módulo Caja no se active. `obtenerCajaPrincipal` exige `activa=1` o `/caja` grita "No hay ninguna caja configurada".
- **huespedes/ver — renglones que se RECORTAN, no que se encogen (jul-31, EN PRODUCCIÓN)**: `.guest-panel` lleva `overflow:hidden`, así que cualquier hijo que desborde se pierde SIN scroll ni pista visual. Pasó con `.guest-reservation-card`, que era grid de 5 columnas con mínimos que suman ~816px mientras la columna central mide ~700px (el `minmax(0,1fr) minmax(280px,350px)` de `.guest-layout` se lleva 350+16 y la sidebar ~373): el chip de estado (última columna) salía 101–448px fuera del panel y quedaba INVISIBLE — el hotelero no podía ver si la reserva estaba confirmada o cancelada — y como eran 6 hijos en 5 columnas, "Ver" caía a un segundo renglón. Ahora es **flex con `wrap`** (`main` `flex:1 1 210px; min-width:0`, `.guest-reservation-state` con `margin-left:auto`, link `flex:none`): envuelve en vez de recortar y en pantalla ancha la tarjeta hasta baja de 141px a 93px de alto. Al agregar una columna a esa tarjeta NO volver a grid de anchos fijos. Hermano del mismo día: en `.guest-vehicle-title` el `<div>` interno no tenía `min-width:0`, así que unas placas SIN espacios (el token interno `SINPLACA<hex>`) no podían partirse y se desbordaban POR DEBAJO de los botones editar/eliminar (invasión medida de hasta 168px) → `min-width:0` en el div + `overflow-wrap:anywhere` en el span. **Deuda viva descubierta ahí**: ese `SINPLACA<hex>` que genera `HuespedController::generarPlacasInternasVehiculo` (para no romper el índice único cuando el hotel no exige placas) es INTERNO y hoy se pinta crudo en la ficha del huésped, en el modal de editar, en el aviso de eliminar, en `habitaciones/ver`, en el dashboard de estacionamiento y en el PDF de cotización — debería leerse "Sin placas" en TODA superficie visible.
- **habitaciones/index fichas de estado (contrato jul-22)**: las 5 fichas son PARTICIÓN EXCLUYENTE que suma el total e IGUALA al grid y a cada filtro — "Libres hoy" = disponibles físicas SIN llegada hoy (vendible a walk-ins), "Por llegar" = SOLO llegadas con cuarto ya disponible (las moradas), las llegadas con cuarto en limpieza se quedan en la ficha Limpieza (sub-etiqueta "N para llegadas de hoy"); el TOTAL de cuartos que llegan hoy + reservas + salidas viaja en la franja `.hb-today-strip` (dimensión aparte). Derivación = helper PURO `HabitacionController::derivarEstadisticasLlegadas` (suite `HabitacionLlegadasStatsTest`); consumidores a mantener en sintonía: fichas hb-stat, `$hbMobileSegments`, `$hbChips`, `$quickLegend` (4 lugares en index.php). JAMÁS volver a mostrar `disponibles` físicas crudas en una ficha: 18 de 25 podían estar comprometidas = sobreventa de walk-ins (queja real del owner jul-22). En /reservaciones el metric "Llegadas hoy" aclara "· N cuartos" cuando N > reservas (grupales).
- **habitaciones/index en MODO FECHA (contrato jul-27)**: elegir fecha en el filtro manda `mostrar_disponibilidad=1` (hidden) y DESVÍA a `mostrarDisponibilidadPorFecha()` — otra semántica con la MISMA vista; los números de ahí son PROYECCIÓN, no estado físico. Contratos: (a) elegir **hoy** ya NO desvía (`indexAction` apaga `mostrar_disponibilidad` si `fecha_consulta === date('Y-m-d')`, conservando `fecha_consulta` para que el input siga marcado) — antes el MISMO día daba dos números distintos según cómo llegaras a la pantalla; (b) la reservación que **entra** el día consultado se pinta `por_llegar` (violeta) y solo la estancia que viene de antes queda `ocupada_fecha` — helper puro `estadoDisplayCompromisoEnFecha(fecha_entrada, fecha)`; se REUSA el estado `por_llegar` a propósito: inventar un `por_llegar_fecha` obligaría a duplicar ~12 grupos de CSS `.estado-*` (§5824-5840, §7241, §8480, §8710-8788, §13788) + `hbFilterEstado` + `$stateAccentColors` + `$colorRapido`; (c) las fichas salen de `derivarEstadisticasParaFecha(string[] estadosDisplay)` (partición excluyente, suma = total; `por_llegar` ya NO va forzado a 0 y `limpieza` es 0 porque la limpieza es estado de AHORA, no proyectable; un estado desconocido cuenta como OCUPADA para nunca inflar lo vendible); (d) en la vista la reserva que cubre la fecha viaja en `info_ocupacion` para AMBOS estados → los 5 sitios que la pintan (id de detalle, faceGuest/faceMeta, reverso, y los 2 botones "Ver Reservación") usan `$tieneCompromisoEnFecha`/`$esLlegadaEnFecha`, JAMÁS `== 'ocupada_fecha'` a secas; (e) los textos con "hoy" salen de `$hbLabelLibres`/`$hbFechaCorta` (definidos al INICIO de index.php, antes de los 4 consumidores) y los paneles de movimientos de `$hb_mov_prefijo`. Suite `HabitacionFechaStatsTest.php` (19). **Check-outs en modo fecha (Ola 3, jul-27)**: ya NO se suprimen — HOY conserva su criterio EXACTO (`estado='checked_in'`, la cola de trabajo real y la única con botón de acción) y en otra fecha se proyecta con el mismo juego de estados que la consulta de ocupadas (`confirmada|checked_in|checked_out`; los `checked_out` son los que hacen útil una fecha PASADA). El botón "Check-out" se OCULTA al proyectar (`!$es_filtro_fecha && ...`) — si no, cerrabas HOY una salida de otro día — y en su lugar va un chip de estado humano (Aún no llega / Hospedado / Ya salió), nunca el enum crudo. Efecto colateral BUSCADO: `$habitaciones_doble_movimiento` ya se puebla en modo fecha, así que la rotación "sale uno y entra otro el mismo día" (clase `estado-doble`) también se ve al proyectar; el `data-estado` de la tarjeta sigue siendo el estado real (`por_llegar`), así que el chip y el conteo NO se desincronizan. La hora del panel salió del hardcode "Hasta 12:00 PM" a `operacion.checkout_hora` (`$hb_hotel_checkout_label`, misma forma que `$hb_hotel_checkin_hora`). Ficha Mantenimiento al proyectar avisa "estado de hoy, no proyectado". **Alertas "vencidas" en modo fecha (jul-27)**: `checkin/checkout vencido` se calculan contra `CURDATE()` (`$sql_checkins_vencidos`, `DATEDIFF(CURDATE(),…)`) y son hechos de HOY. Se separaron en dos capas: (1) las TARJETAS ya no se repintan al proyectar — los dos `isset($habitaciones_con_check*_vencido[...])` llevan `!$esta_consultando_disponibilidad_fecha` delante; sin ese candado un cuarto con check-in atrasado HOY salía rojo "Sin check-in · N días" en la vista de septiembre y, peor, ganaba a `$tieneCompromisoEnFecha` en la cadena de `faceGuest` y mostraba al huésped atrasado en vez de quien ocupa el cuarto ESA fecha (las reglas CSS `.has-checkin-vencido … .estado-*_fecha` sugieren que el repintado era intencional, no lo es: son de cuando el modo fecha no tenía llegadas); (2) el panel `.hb-alerts` SÍ se conserva al proyectar (un check-out vencido es urgente aunque estés planeando septiembre) pero pasa a variante ESTÁTICA (`.hb-alerts.is-static`): cabecera `<div>` en vez de `<button>`, sin `hbToggleAlertas`, sin chevron y **sin renderizar el desplegable** — conserva la señal (chips + `<small>` "De hoy, no del dd/mm") y quita la ACCIÓN, mismo criterio que el botón de check-out; la salida es un enlace `.hb-alerts-goto-hoy` a `/habitaciones`. Re-anclar estas alertas a la fecha consultada NO es implementable: las 3 se calculan contra `CURDATE()` y no existe un "check-out vencido del 17 de septiembre". Las otras 2 ramas del bloque (`$tiene_checkout_hoy`, `$es_llegada_tardia`) ya eran inertes en modo fecha porque dependen de `ocupacion_actual`/`reservacion_pendiente`, que esa rama no carga. **TERCER tiempo verbal — consulta de PASADO (jul-27)**: la vista tenía solo hoy y futuro, y el pasado heredaba el vocabulario del futuro ("Por llegar" sobre una reserva que ya entró y salió, "vendibles el 25/07" sobre una noche que ya no se vende). Todo lo que cambia de tiempo cuelga de `$hbFechaEsPasada` (definida arriba junto a `$hbFechaProyectada`; antes vivía suelta dentro del bloque de la barra de contexto): labels `$hbLabelLibres` ("Libres hoy"/"Libres"/**"Sin ocupar"**) y `$hbLabelPorLlegar` ("Por llegar"/**"Sin check-in"**) y `$hbLabelOcupada` ("Ocupada"/**"Se rentaron"**) — los 4 consumidores del contrato de sintonía leen de ahí—, subs de las 5 fichas, `faceMeta`/reverso de la tarjeta ("Entró ese día"), y en el controller los `$estados['disponible_fecha'|'por_llegar']` ('Sin ocupar'/'Sin check-in'/'Se rentó'). **El botón Reservar era el hallazgo grave**: pasaba la fecha consultada a `crearReservacionConFecha`, que arma entrada/salida en el PASADO y abre el alta con eso (se podía crear una reservación hacia atrás); ahora en pasado se manda `''` y cae en `crearReservacionRapida`→`obtenerDatosReservaDefault` (hoy→mañana), en futuro se conserva la precarga. **El panel de check-ins mentía distinto**: filtraba `estado='confirmada'`, así que en otra fecha SOLO listaba las que nunca hicieron check-in —pintadas como llegadas normales— y escondía a las que sí llegaron; ahora usa el mismo juego de estados que los check-outs y cada renglón lleva chip (Sin check-in / Aún no llega / Hospedado / Ya salió). HOY conserva su criterio exacto en ambos paneles. **El EJE de la partición cambia en pasado (jul-28, rediseño tras "aún es medio revuelto")**: en hoy/futuro las fichas particionan por ESTADO OPERATIVO (5 cubetas); hacia atrás particionan por RESULTADO DE LA NOCHE (3: se rentó / sin check-in / sin ocupar) y **las fichas Limpieza y Mantenimiento DESAPARECEN** (no hay histórico por día y el mantenimiento es el estado de HOY; un indicador que necesita disculpa no debe existir, y encima filtraban a un grid vacío) — se van también de `$hbChips` (`unset`), `$hbMobileSegments` y `$quickLegend`, la rejilla pasa a `.hb-stats--pasado` con 4 columnas y `order` para que se lea "de 49, se rentaron N". Sigue siendo partición excluyente que suma el total, sobre los MISMOS 3 `estado_display` de siempre (cero CSS nuevo, `hbFilterEstado` intacto). Helper puro `estadoDisplayCompromisoPasado(estadoReservacion)`: `checked_in|checked_out`→`ocupada_fecha`, `confirmada`→`por_llegar`, desconocido→`ocupada_fecha` (no inflar "sin ocupar"). **DOS BUGS que esto destapó**: (a) una reserva pasada en `confirmada` es un NO-SHOW y se pintaba "Entró/Entraron" mientras el panel de abajo decía "Sin check-in" — la pantalla se contradecía sola; por eso la cubeta se llama "Sin check-in", NUNCA "No llegó" (el sistema sabe que no se registró, no que la persona no llegó, y este hotel tiene alertas de check-in vencido justo por eso); (b) el `if ($habitacion['estado']=='mantenimiento')` ganaba ANTES de mirar `$ocupadas`, así que un cuarto rentado el 25 y hoy en mantenimiento se contaba como Mantenimiento y **subvaluaba la venta de esa noche** — en pasado la ocupación va primero. Se retiran además el precio `/noche` (el `precio_base` es el de HOY), el "Hasta HH:MM" de salidas y el toast. `es_cortesia` (existe en `reservacion_habitaciones`) alimenta el sub "N% de ocupación · M de cortesía" solo si M>0: una cortesía se ocupa pero no se renta.
- **habitaciones/index barra de filtros en UNA fila (jul-27)**: `.hb-filterbar` tenía `flex-wrap:wrap` + `.hb-filter-right{margin-left:auto}` → cuando no cabía, el grupo fecha/Hoy/Limpiar saltaba a un segundo renglón y el `auto` lo dejaba flotando a la derecha (se veía como barra partida; queja del owner). Fix en `@media (min-width:768px)`: `nowrap` + chips elásticos con `overflow-x:auto`. GOTCHA de presupuesto: **la barra NO mide lo que el viewport** — la sidebar se come ~370px, así que a 1440px hay 1067px reales y el contenido pedía 1241; se recuperan con botones Hoy/Limpiar en solo-ícono <1600px (ya tienen `title`, mismo recurso que el móvil con `.hidden sm:inline`) + padding de chip 13→9 y gap 7→5. GOTCHA de flex que costó dos vueltas: el reparto de shrink es **base × factor**, y la base de los chips (~685px) aplasta a la del buscador (200px) → con factores normales los chips se comen el recorte y SIEMPRE queda uno cortado; la solución es `flex-shrink:100` en el buscador (cede primero, es lo menos denso), NO bajar el de los chips: con `flex-shrink:0.02` en chips se ve perfecto a 1440 y a **1024 empuja el botón Limpiar 111px FUERA de la tarjeta** (medido) porque el sobrante ya no lo absorbe nadie. Al medir, `resize_window` del pane sí aplica aquí, pero recargar con `?nc=N` o el CSS viejo sigue en pie.
- habitaciones/index estados vacíos (3 distintos, no confundir): `#hbNoResults` = miss del filtro CLIENT-side, solo se renderiza si `!empty($habitaciones)` (con cero cuartos se apilaba con el estado vacío = doble mensaje, ya arreglado); `.hb-empty-first` = onboarding "Añadir primera habitación" cuando el hotel NO tiene ninguna (`$estadisticas['total']===0`, señal SIN filtro); `.hb-empty-filter` = filtro/búsqueda servidor sin match (`total>0`). Estilos con tokens `--brand-*` (dark solo).
- **habitaciones/index — color de un ESTADO (jul-25)**: la fuente viva son los tokens `--c-<estado>`/`--bg-<estado>` de `.habitaciones-view` (§5663: available/occupied/arriving/cleaning/maint/critical); de ahí derivan badge, chips, fichas, leyenda móvil y los fondos de Cupertino (`color-mix` 8% claro / 20% dark). Cambiar SOLO ese token deja el color desparejo: hay 4 puntos más que repiten el valor — (a) un 2º bloque `.habitaciones-view{}` (§8361) que RE-DECLARA los `--bg-*` y GANA en Deleite, (b) `$stateAccentColors` PHP (§3539, hex crudo) que pinta la cinta `.rc-stripe` y el gradiente de la hoja del reverso (`--sheet-c`), (c) `--qv-purple`/`--qv-blue`… del modal Vista rápida (§4287), (d) el partial `reservaciones_alertas_pendientes.php` (§32) que reclara los MISMOS tokens para las alertas de llegadas. Los bloques legacy `.estado-*` (§568/§1571/§2244, incluido un `#c084fc` "forzar morado en móvil") están MUERTOS: `.habitaciones-view .estado-x` les gana por especificidad — no perder tiempo ahí. GOTCHA de tema: en **Cupertino la card es BLANCA** (`.flip-card-front[class*="estado-"]{background:#FFF!important}`, `#1C1C1E` en dark) → tocar `--bg-*` NO se ve, el estado lo cuentan solo cinta + badge. Caso real: "por llegar" era `#5A57D2` (hue 241°) y se confundía con limpieza `#2F77E0` (hue 214°) → `#8039D0` (hue 268°); regla útil = el lila debe tener R>G y el azul G>R, es lo que separa los fondos pálidos de un vistazo.
- Formularios de alta con distintivos Obligatorio/Opcional + leyenda + línea de tiempo de recompensa (progreso por campo/subcampos, bonus dorado para opcionales, celebración sutil): implementado en `huespedes/crear.php` (clases `.gc-*`, obligatorias suben con CSS `order`+`:has(.gc-badge-req)`) y `reservaciones/crear.php` (clases `.rwp-*`/`.res-*`, tema verde/oro `--lc-*`; como la selección de habitaciones NO es input normal, el tracker usa `!#btnGuardar.disabled` —la validación jQuery existente— como señal de "listo", vía MutationObserver del `disabled`). Patrón en playbook §3 ítem 23. GOTCHA reservaciones/crear: la sección de "habitación de cortesía" fue aplanada a superficie neutra por `.vista-reservacion .seccion-cortesias{...!important}` (§1192, restyle boutique) — para destacarla hay que ganar con `!important` propio más abajo en el cascade.
- **Centro Documental — metadata en vistas host (contrato jul-25)**: el bloque documental de las 7 áreas host (proveedores/compras/cuentas_por_pagar/huespedes/reservaciones/trabajadores/tareas) SE OCULTA y NO SE CARGA cuando `documentos` no está contratado o falta `documentos.view` — gate único `documentos_entidad_visible()` (`helpers/modulos.php` = `current_hotel_has_module('documentos') && can('documentos.view')`, espeja el `before()` del módulo) usado en el partial `documentos_entidad.php` (return temprano = garantía de render) y en los 7 controllers (no cargar). Una 8ª área que muestre docs = repetir el gate o filtra metadata PII (nombre de archivo tipo `INE_Juan.pdf`, quién/cuándo subió). Subir/vincular exige `documentos.all` (POST `guardar` + GET `subir` form + CTAs ocultos en partial e index), no basta `.view` (era hueco RBAC de escritura). Suite `DocumentoTest.php` (aislamiento/traversal/blocklist/permisos/gates de subida). El módulo sigue `activo_global=0`: pendiente la política de borrado/retención de PII antes de vender (punto 4 del owner).

## Gotchas conocidos

- **Auditar un análisis/plan escrito por otra sesión u otra herramienta (jul-29)**: ANTES de calificarlo o ejecutarlo, `git fetch` + `git status -sb` — el plan de "coherencia de módulos SaaS" se escribió sobre un checkout 18 commits atrás y ~60% ya vivía en origin (clasificación comercial 20260724/25_001 + candados c74b407 + reportes individuales 20260728_001, todo en prod desde 2026-07-28); ejecutar su paso de "normalizar catálogo" habría sido regresión con impacto en cobros (abrir un bloque REACTIVA el cobro histórico de `hotel_modulos`, y `activo_global`+`precio_mensual` van en la MISMA transacción o SaasCobroService congela precios en `desglose_json`). Las fugas que SÍ siguen vigentes en origin (facturación lado productor en ver.php/procesarSolicitudFactura, `procesarDescuentoInventario` sin gate, motor IncrementoTarifa/descuentos sin gate en modelo+API+motor público, interruptor de estacionamiento pendiente ~6 sitios, footer documentos/compras con `modules_any` equivocado, secciones de /configuracion sin gate por módulo, `current_hotel_id()` lee solo sesión → `hotel_has_module()` en panel SaaS exige `$hotelId` explícito) están con archivo:línea en la memoria [[fugas-modulos-saas-pendientes]].

- **`activo_global=0` NO significa "pendiente de precio": significa APAGADO PARA TODOS** (auditoría maestra jul-25). `listarActivosDeHotel`/`hotelTieneModulo` exigen `activo_global=1`, así que bloquear un opcional apaga su función en TODOS los hoteles aunque la tengan contratada en `hotel_modulos`. Efectos colaterales reales medidos: con `exportaciones` bloqueado NINGÚN hotel descarga nada (13 `require_hotel_module('exportaciones')` en Caja/Inventario/Nómina/Reportes/Reservaciones/Trabajadores + 17 condicionales de UI — incluidos los 8 reportes laborales de Personal, que así queda comprado pero sin entregable); con `auditoria` bloqueado la BITÁCORA DEJA DE ESCRIBIR en silencio (`core/Controller.php:76` corta el INSERT); con los 5 `reporte_*` + `tablero_ejecutivo` + `camarista|limpieza` bloqueados quedan 9 de las 11 pantallas del Centro de Reportes muertas (sobreviven solo las 2 de mantenimiento, que es base). Y no hay vuelta atrás desde la UI: `Modulo::setActivoGlobalPorClave` no tiene ruta ni llamador, se desbloquea con SQL a mano.
- **Un módulo a la venta SIN `require_hotel_module` = se regala** (jul-25): `TarifasController::before()` solo exige `require_permission_or_403('tarifas.view')`; con `tarifas_dinamicas` ya abierto a $149, cualquier hotel cuyo gerente tenga `tarifas.view` (el preset lo trae) usa la pantalla sin contratarla. Al abrir un bloque a la venta, verificar que su controller tenga el gate de MÓDULO además del de permiso — el menú no es enforcement.
- **El catálogo `modulos` NO modela dependencias entre bloques** (jul-26): no hay columna `depende_de` ni validación en `actualizarModulosHotel` → vender "X requiere Y" es texto de marketing, no candado; desde el panel se puede contratar X sin Y y el hotel aterriza en una pantalla rota. Caso vivo: **`compras` depende DURO de `inventario`** — `compra_detalles.producto_id` es NOT NULL con FK a `inventario_productos`, `catalogosBorrador()` llena el combo desde ahí (hotel sin inventario = combo vacío = ni el borrador guarda) y `recibirCompra()` mueve `stock_actual`+`movimientos_inventario`. PERO el bloque `compras` son TRES cosas y sólo una depende: `ProveedorController` y `CuentaPorPagarController` tienen CERO referencias a inventario/producto/stock y `cuentas_por_pagar.compra_id` es NULLABLE (una CxP vive sin compra) — ambos gateados con `require_hotel_module('compras')`. ⇒ el corte comercial natural es partir Proveedores+CxP a su propio bloque, NO desacoplar compras de inventario (eso exigiría `producto_id` nullable + concepto libre + 6 INNER JOIN→LEFT JOIN + `recibirCompra` bifurcado, todo sobre ruta de dinero). Al jul-26 los 4 hoteles con `compras` tienen también `inventario`, así que nadie está roto todavía.
- **PRODUCCIÓN YA CORRE LA CLASIFICACIÓN COMERCIAL, CON CANDADO DE LEGADO (deploy 2026-07-28, baseline git `a193a2f`, 120 archivos + 7 migraciones)**: regla pedida por el owner = la clasificación solo restringe a hoteles FUTUROS; los 3 hoteles vivos (1 Hotel base, 2 Los Cedros, 3 **San Nicolás — creado 28-jul**) conservan TODO. Cómo quedó: 31 módulos re-abiertos con `motivo_bloqueo` `'Legado 2026-07-28…'` (activo_global=1, tipo opcional — el motivo es marcador informativo, NO bloquea ni se "arregla"), los 5 `reporte_*` sembrados a los 3 hoteles vía `hotel_modulos.fuente='legado'` + `precio_override=0` (gratis para existentes; futuros a precio de lista), push restaurado a '1' en hoteles 1-2 (el 2 tiene suscriptor real; el 3 queda opt-in '0'), y los únicos bloqueados globales son tareas + reportes_distribucion (internos, sin gate de pantalla). Diff pre/post por hotel: solo salieron tareas/reportes_distribucion/mantenimiento_plus (retiros sin pérdida funcional; Plus vive en Mantenimiento base) y entraron los 5 reportes; `descuentos` de Los Cedros NO se reactivó (el owner lo apagó a mano el 23-jul, `hm.disabled_at`; el gate nuevo ahora sí lo hace valer — si lo quieren, se prende del panel). El verificador comercial en prod da **6 FAIL esperadas** (asserta el contrato para-futuros; el legado diverge a propósito). OJO dinero: `saas_cobros` trae 2 cobros 2026-07 VENCIDOS con catálogo viejo ($9,187 h1 / $8,909 h2, desglose congelado) y el cron generará 2026-08 con el legado a precio de catálogo (~$999 base + $6,737 h1/h3 y $6,131 h2) — si el owner no quiere cobrarse los bloques de legado: `precio_override=0` desde el panel o cancelar/regenerar. Respaldos: `/opt/medisoft/backups/pre_clasificacion_20260728.sql.gz` (BD completa), `pre_deploy_20260728_clasificacion.tar` (archivos) y `pre_activos_20260728.txt` (módulos activos por hotel ANTES). Cron nuevo instalado: purga documental 4:15 (tras el backup de 3:30 → lo purgado sobrevive en el respaldo de esa noche).
- **Gates fail-open sin contexto de hotel**: `require_hotel_module` y `require_hotel_module_api` (helpers/modulos.php) devuelven `true` si `!has_hotel_context()`; `hotel_menu_module_enabled` devuelve `true` si la consulta de módulos falla. Degradan a pantallas vacías (las queries filtran por `current_hotel_id()` null), no a fuga, pero un gate comercial que falla abierto no es candado.
- **El gateo de módulo suele vivir en el CONTROLLER, no en la vista**: contar condicionales en la vista SUBESTIMA la protección (dashboard/index.php tiene 1 sola en 6087 líneas y aun así no filtra metadata, porque `DashboardController:128` no carga el dato si falta el módulo). Antes de reportar "fuga de UI", revisar si el controller ya evitó cargar.
- **`Reporte.php` mezcla DOS bases de ingreso incompatibles**: Procedencia/RankingEstados usan `r.precio_total` (NETO) y Rentabilidad/OcupacionPorTipo/IngresoPromedio/OcupacionPorDiaSemana usan `rh.precio` (BRUTO, sin restar `descuento_total`). Verificado en datos: `precio_total = Σ rh.precio − descuento_total`. ⇒ "Habitaciones más rentables" sobrestima el ingreso por el descuento completo y NO reconcilia con Procedencia. Al tocar cifras de reportes, elegir base y documentarla.
- **Ocupación vs Rentabilidad no comparan lo mismo**: `obtenerNochesOcupadasEnPeriodo` cuenta `estado <> 'cancelada'` (incluye `confirmada` futura) y `obtenerRentabilidadHabitaciones` solo `checked_out`; además `obtenerOcupacionDiaria` OMITE los días con cero ocupación (sesga cualquier promedio sobre sus filas) y el denominador `totalHabitacionesActivasHotel` es `activa=1`, o sea incluye cuartos en mantenimiento. `obtenerEstadisticasOcupacion` sí calcula bien (capacidad = cuartos × días del periodo, independiente de qué días tuvieron huéspedes).
- **`saas_cobros` congela `desglose_json` al generar y NUNCA lo re-deriva** (`SaasCobroService`): tras una reclasificación del catálogo, las facturas abiertas siguen cobrando los precios viejos (los cobros de 2026-07 traen anticipos/reportes/mantenimiento/roles_avanzados/tareas/reportes_distribucion, hoy base o internos a precio 0). Al reclasificar: cancelar (`cancelar()`, ruta POST ya existe) y regenerar los cobros no pagados del periodo en curso.
- **Cuentas por cobrar** (`/cuentas-por-cobrar`): la pestaña principal NO lee la tabla `cuentas_por_cobrar` — DERIVA en vivo de reservaciones−pagos−abonos (`CuentaPorCobrar::listarDerivadasPorHotel`, único consumidor); la tabla solo guarda las "operativas" generadas a mano. Filtro `vigencia` default `vigentes` (`fecha_salida >= CURDATE()`, pedido del owner jul-22: el histórico migrado saturaba la vista) — `pasadas`/`todas` por query param; "borrar una CxC pasada" = filtrarla, no hay fila que borrar.

- Disponibilidad de habitaciones: RESUELTO jul-17 el candado de pendientes ("salida efectiva"). Los 4 predicados de solapamiento (`Habitacion::disponiblesEntreFechas`, `Reservacion::verificarDisponibilidadMultiple`+`Excluyendo`, `ApiController::verificarDisponibilidadAction`+`todasConOcupacionAction`) extienden la salida con `CASE WHEN r.fecha_salida < CURDATE() THEN CURDATE()+1` → checkout vencido o confirmada pasada sin resolver BLOQUEAN hoy (nunca fechas futuras); resolver libera solo. Fuente única de alertas/pendientes en el modelo: `Reservacion::alertasPendientesOperativas()` (consumida por ambos index) y `pendientesPorHabitacion()` (mapa por habitación, checkout_vencido pisa a checkin_pendiente). UI: picker de crear.php pinta la tarjeta "Check-out vencido · Resolver primero" (viaja `info_ocupacion.pendiente` en todas-con-ocupacion), chip+msConfirm "En limpieza ahora" si entrada=HOY (aviso, no candado; `FECHA_HOY` + `data-limpieza-hoy`), y ver.php confirma check-in si la habitación sigue en limpieza/mantenimiento (`CHECKIN_HABS_NO_LISTAS`, `getHabitaciones` ya trae `h.estado`). Suite `DisponibilidadPendientesTest.php`. GOTCHA QA: `CURDATE()` es del timezone de SESIÓN — la app corre en hora local pero un PDO crudo va en UTC (de noche difieren 1 día) → sembrar fechas QA relativas al reloj de la APP y en tests leer `CURDATE()` de la misma conexión. GOTCHA API: `/api/*` exige header `X-Requested-With: XMLHttpRequest` o responde "Acceso no autorizado" (fetch manual desde consola debe agregarlo).
- Cache/navegación: el freno era `.htaccess` (Apache pisa headers de PHP), no PHP — revisar ahí primero ante temas de cache.
- **bfcache (regresar con flechas) y flotantes (jul-22)**: instant-nav habilita el back-forward cache a propósito → TODO widget/overlay con estado JS transitorio (candados `ocupado`, esqueletos "pensando", overlays full-screen, dictado activo) debe limpiarse en `pagehide` (la foto del historial se toma DESPUÉS de pagehide → se guarda limpia) + `pageshow persisted` de respaldo; patrón en copiloto_widget (`soltarCandados`) y confirm.php (`limpiarEstadoPagina` — los botones href de msPageState navegan sin close() a propósito). Caso real: copiloto revivía atorado tras "atrás" (Enviar muerto, chips ignorados; en móvil un `body.cop-abierto` huérfano esconde el FAB). GOTCHA QA: ni el Browser pane ni claude-in-chrome reproducen bfcache (CDP lo desactiva; back = recarga real) — simular con `window.dispatchEvent(new PageTransitionEvent('pagehide'/'pageshow', {persisted:true}))` y verificar por clases/DOM.
- Validador de mantenimiento: campos `*_costo` tienen trampa de validación; `habitacion_id` es NULLABLE (usar LEFT JOIN).
- Headers glass Cupertino: `padding-inline` de la vista pisa al header → usar `!important`; si la vista fija geometría con `!important`, agregar regla propia en §48c de cupertino.css.
- Sesiones concurrentes de edición se pisan sin commit (mantenimiento) — avisar al usuario si aplica.
- `navegacion.php` tiene un OR de permisos traicionero (caso Guardián).
- **Menú/nav filtrado por permiso de rol (jul-18)**: la sidebar (`views/layout/sidebar.php`) y el catálogo `config/navegacion.php` (buscador/accesos/favoritos vía `nav_pantalla_visible`) filtran cada pantalla por `módulo activo Y can(permiso)`; antes casi todo era SOLO-módulo → un rol acotado veía toda la operación. Contrato al agregar pantalla operativa: fijar su `permiso` en AMBOS lados — en la sidebar con el helper local `$puede('x.view', ...)` (any-of) y en la entrada del catálogo (`permiso` acepta string o lista any-of; lo resuelve `can_any()`). Extras de gerencia SIN permiso propio (operacion/diaria, forecast, night audit, motor-reservas, canales, whatsapp, IA, copiloto, lealtad) van por `$sidebarEsGestion` (rol-string gerente/administrador), NUNCA por `is_gerente()/is_admin()` (leen `usuarios.rol` GLOBAL, ajeno al hotel → fuga real: un camarista cuyo usuario tenga rol global 'gerente' veía Tarifas). GOTCHA: un rol PERSONALIZADO guarda `hotel_usuarios.rol='recepcionista'` (UsuarioController::resolverAsignacionRol), así que los gates por rol-string SOLO distinguen gerente/administrador reales; todo lo acotable va por permiso con `roles=>null` en el catálogo. Aterrizaje por rol: `home_route_for_current_user()` (auth.php) manda a los roles acotados a su pantalla de trabajo (`primary_landing_route()`, p.ej. camarista→/camarista), `nav_puede_ver_dashboard()` decide si ven el dashboard, y `DashboardController::before()` rebota si su home != dashboard. Permiso nuevo `camarista.view` (catálogo de `config/permisos.php`) = crea un rol "solo Limpieza" (la pantalla Limpieza es `can_any('camarista.view','tareas.view')`, sin migración: los roles sembrados la conservan por tareas.view). Es visibilidad de MENÚ, NO enforcement de servidor (eso sigue en `docs/PLAN-AUTORIZACION-OPERATIVA.md`, pendiente de aprobación): las URLs siguen cargando, hay que sumar gates de servidor aparte para blindar.
- MySQL enum: insertar valor inválido (ej. 'egreso') no truena, duplica conceptos — validar enums en PHP.
- **Auditoría seguridad fable jul-19 (CORREGIDA y verificada E2E; Maximiliano aprobó autorización monetaria)**: gates de servidor ahora ENFORCED (ya no solo menú). Contratos nuevos: (a) escrituras de `ReservacionController` gateadas por acción con `require_permission_or_403` (checkin/checkout→`habitaciones.checkin/checkout`, anticipos/cambiar-metodo→`caja.cobros`, guardar→`reservaciones.create`, modificar-dias→`reservaciones.edit`) o `can_any(['reservaciones.cancelar','reservaciones.edit'])+deny_access_403` (cancelar/noShow; recepción trae `.edit` no `.cancelar`); llaves→any-of `llaves.control`∨`habitaciones.checkin/checkout`. `ReservacionGatesRbacTest` caracteriza cada gate. (b) `is_gerente()/is_admin()` (rol GLOBAL) → `can()`/`require_permission_or_403` en Usuarios/Config/Tarifas + gate base en Huespedes/Documentos/Proveedores/Areas/Habitaciones. OJO: **administrador de hotel PIERDE /configuracion y /tarifas** (su preset no los trae; ampliar preset si se requiere). (c) `metodo_pago` con whitelist enum en `cambiarMetodoPagoAction`+`checkInConPagosMixtos`+`checkInCheckOutExpress`. (d) `cancelar()` con `FOR UPDATE`+`WHERE estado<>'cancelada'`; check-in/cancelación revalidan el corte con `SELECT...FOR UPDATE` antes de insertar. (e) SSRF iCal: `IcalCanalesService` resuelve+valida IP pública (rechaza privadas/169.254/loopback), `FOLLOWLOCATION=false` con re-validación por salto. (f) open-redirect por Referer cerrado en `core/Controller`+`core/Router` (`refererInternoSeguro`, valida host por parse_url, no substring). (g) login: rate-limit por CUENTA (`LoginRateLimiter`, 15/15min→30min, indep. de IP) + `password_verify` dummy anti-enumeración. PENDIENTE del usuario (no es código): rotar token WhatsApp + purgar historial git. GOTCHA verificación RBAC en el gotcha de QA-RECETAS y [[auditoria-seguridad-fable-julio2026]].
- `can_legacy` RESUELTO jul-17: ahora resuelve contra el preset homónimo de config/permisos.php, así que los roles legacy SÍ conocen `reservaciones.*`/`documentos.*`/`tareas.*`. Sigue vigente la regla: un enlace/acción jamás exige más permiso que la pantalla a la que apunta.
- **Auto-edición de usuario con roles configurables (FIX jul-20)**: en `usuarios/editar.php`, cuando te editas a TI MISMO el selector de rol va `disabled` ("No puedes cambiar tu propio rol") — pero `UsuarioController::actualizar` con `$usaRolesConfigurables` EXIGE un `role_id` válido en el POST (línea ~272) o corta con "Selecciona un rol válido", bloqueando guardar hasta la contraseña. La rama self-edit debe reenviar el `role_id` ACTUAL como `<input hidden name="role_id">` (además del `rol` legacy hidden que ya tenía) o el auto-guardado es imposible en cualquier hotel con roles sembrados. Sin pantalla de "mi perfil": el único cambio de contraseña propio es por esta pantalla (o SQL directo `password_hash`).
- Los modales de habitaciones/index (limpieza, vista rápida…) viven DENTRO de `.container` (`position:relative; z-index:1`): su z-index alto (900/10040) queda ATRAPADO en ese contexto y cualquier elemento externo con z>1 (ej. header sticky z40) les pinta encima. Arreglo canónico: bajar el elemento invasor con `body.ms-modal-abierto` (clase que publica modal-sidebar-fix.js), patrón sidebar — no mover el modal ni ocultar con display.
- Widgets fijos (copiloto, FAB): las reglas `body.has-hotel-bottom-nav #id` de ≤1024px pisan por especificidad a las de `#id` en ≤640px — todo bloque móvil nuevo debe repetir esa selectora o el elemento hereda bottom/max-height ajenos (caso panel copiloto diminuto, jul-2026).
- **JSON por POST**: `post()`/`getPost()` pasan TODO por `htmlspecialchars(ENT_QUOTES)` → un JSON llega como `{&quot;...}` y `json_decode` falla EN SILENCIO. Revertir con `html_entity_decode(..., ENT_QUOTES, 'UTF-8')` solo para params que alimentan json_decode (caso copiloto historial/flujo). Los tests por servicio NO lo detectan: probar el cable real.
- GOTCHA QA navegador: variables `const`/`let` declaradas top-level en un `<script>` inline (ej. `HB_LIMPIEZA_PERSONAL`/`HB_LIMPIEZA_ASIGNADAS` en habitaciones/index.php) NO cuelgan de `window` (a diferencia de `var`) — probar con `window.X` desde `javascript_tool` da `undefined` aunque el dato SÍ esté en el HTML; leer el identificador a secas (`X`), no `window.X`.
- Paletas hardcodeadas fuera de `--brand-*` (quejas "muy verde/azul" del owner): varias vistas fijan colores propios en vez de derivar del brand — reservaciones/crear.php (verde sage `#EFF4EC`), habitaciones/editar.php (indigo `--s-*`), inventario/index.php (verde `--lc-*`). Fix canónico: retintar el cromado neutro al par `#F5F5F7`/`#FAFAFC` y rutear el acento a `var(--brand-primary/-soft/-accent, …)`, conservando semánticos (éxito/aviso/error/estado). GOTCHA inventario/index.php: tiene DOS bloques `<style>` — el viejo `--lc-*` sage y `#inventory-boutique` que lo pisa TODO con `!important` vía tokens `--inv-*` derivados del brand… salvo el `background` de `.inv-page`, que seguía verde (bloque 2 define `--inv-ivory/-2` pero no los aplicaba al canvas) → apuntar el fondo a `--inv-ivory`.
- Topbar global `ms-vtb` (view_topbar.php): `.is-stuck` (al pegarse en scroll) SOLO pisa el padding HORIZONTAL (bleed del vidrio via `--vtb-pad-*`), no el vertical → para darle altura a la barra sin salto al fijarse, el `padding-block` va en la base `.ms-vtb` (queda igual en reposo y pegada); la flecha se recentra sola (nav con `align-items:center`).
- Limpieza de habitaciones: hay DOS selectores de personal distintos y ambos persisten bien en `tareas_operativas`+`tarea_trabajadores` (verificado por PDO) — `CheckoutLimpieza` (checkout-limpieza.js, opcional, al hacer check-out, "¿quién SE ENCARGARÁ?") vs `LimpiezaPersonal` (mismo archivo, obligatorio, al marcar limpia, "¿quién LA HIZO?"). `liberarHabitacion()` en habitaciones/index.php ya trae `HB_LIMPIEZA_ASIGNADAS[id]` como preselección; si viene no-vacía usa confirmación de 1 clic ("¿Confirmas que Fulano hizo la limpieza?" con opción "Cambiar personal") en vez de reabrir el picker completo — no volver al picker obligatorio por defecto cuando ya hay responsable asignado, es la causa típica de la queja "doble trabajo". Pendiente (no implementado): badge de "asignada a" en el card y precarga per-cuarto en el modal masivo `.lm-staff` (que solo permite 1 persona para todo el lote).

## Componentes reutilizables (no reinventar)

- Toast/confirm: `msToast` + `msConfirm` (modal PC / sheet móvil) + `msPageState` · Modales: `window.msModal` (animación+skeleton, footer.php).
- **Select con buscador `ms-combo`** (jul-22, global en header/footer): `<select data-ms-combo="placeholder">` → botón + panel filtrable (panel en `body`, `position:fixed`, z 10060; hoja inferior en ≤640px). El `<select>` sigue en el form INVISIBLE PERO RENDERIZADO (`.ms-combo__native`, opacity 0 + `tabindex=-1`) o la validación nativa no puede anclarse; al elegir dispara `input`+`change` (borrador de app.js y previews de la vista siguen vivos) y en `invalid` lleva el foco al trigger. Búsqueda sin acentos y ranking prefijo>palabra>medio; `data-ms-combo-free` deja guardar lo escrito; por opción: `data-iso` (bandera), `data-hint` (texto secundario buscable), `data-search` (alias), `data-label`. Trigger HEREDA las clases del select (gc-control/ge-control) → se ve nativo en cada vista; el CSS usa `--brand-text/-muted/-border/-surface` (dark automático, `-soft` es negro en dark: no usarlo de fondo de panel). Montado hoy en huespedes crear/editar (Estado y País). GOTCHA QA: el clic físico del pane no siempre dispara, usar `dispatchEvent(new MouseEvent('click'))` y teclear seteando `input.value` + evento `input`.
- **Catálogo de países** `helpers/paises.php` (`ms_paises`, `ms_paises_frecuentes`, `ms_pais_bandera` desde ISO2, `ms_pais_resolver` que entiende nombre/gentilicio/ISO, `ms_select_paises()` que arma el select listo para ms-combo). Suite `PaisesCatalogoTest.php`. El campo `nacionalidad` guarda ahora el NOMBRE del país ("Estados Unidos"); un valor legacy tipo "Estadounidense" se resuelve al país y uno desconocido se conserva como opción `data-libre`. Banderas = emoji derivado del ISO2 con detección por ancho en canvas → Windows (sin fuente de banderas) cae al chip "US".
- Validación de forms (app.js): todo POST guardado recibe el recuadro genérico `.ms-form-error-summary` + anti-doble-submit + borrador. Opt-outs por atributo: `data-form-guard="off"` (apaga TODO), `data-ms-no-summary="1"` (solo suprime el recuadro, útil si el form ya avisa por su cuenta — ej. wizard de check-in que usa `mostrarMensaje`→`msToast`), `data-no-unsaved-warning`, `data-no-draft`.
- Inputs de dinero: `data-money-format="true"` (MedisoftMoneyInput ya vive en app.js ~2204-2335).
- Filtros de listados: partial `filtros.php` (.msf-*) · Badges de novedad sidebar: helper `sidebar_novedades()`.
- Push PWA: cadena completa con ruteo por rol (ver PwaPushController); iOS requiere instalación. **Activación AUTOMÁTICA por dispositivo** (jul-25, pwa.js §8b): al abrir la app instalada (o `appinstalled`) pide permiso y suscribe solo; con permiso ya dado suscribe en silencio en cualquier navegador; iOS/Safari exige gesto → la solicitud se dispara con el primer `pointerup`. Marcadores localStorage: `loscedros_push_optout` (apagado manual del panel = la automática NO insiste) y `loscedros_push_endpoint_registrado` (= `endpoint|usuario_id`, evita re-POST por carga y re-registra al cambiar de usuario); QA en pane: stubear `window.Notification` + `ServiceWorkerRegistration.prototype.pushManager` y llamar `window.PWA.autoActivarPush('arranque'|'appinstalled')`. OJO catálogo (jul-25): `notificaciones` y `pwa` son **paquete base** (es_core=1) → ya NO hay 403 y la cadena opera en todo hotel; el gate de la pantalla es ahora el permiso `notificaciones.view`, no el módulo. El push quedó **opt-in** en 3 puntos (los 3 hacen falta): (1) fallback `false` en `PwaPushService::estaActivoParaHotel` ← **este es el que manda**, porque `hotel_config_get($clave,$default,$hotelId)` devuelve el `$default` DEL LLAMADOR y nunca lee el `'default'` del catálogo; (2) `'default' => false` en `helpers/hotel_config.php` (solo pinta el form de /configuracion); (3) la migración apaga la clave en **TODOS** los hoteles sin excepción (decisión del owner: "que lo enciendan a propósito"; el `1` que traían los hoteles 1/2/4 era herencia del default `true` — bastaba guardar /configuracion para persistirlo — así que no probaba una elección). Encenderlo = editar `notificaciones.pwa_push_activo` en `/configuracion`, que es SOLO `saas_admins`: si un hotel pide push, lo prende Medisoft. El gate corta ANTES del prompt del navegador: `estadoCliente` → `/api/pwa-push/public-key` devuelve `enabled:false` y `pwa.js` aborta, así que nadie ve la petición de permiso; también rechaza `guardarSuscripcion`. Esa clave solo se edita en `/configuracion`, que es SOLO `saas_admins`: si un hotel pide push, lo enciende Medisoft. `pwa` NUNCA tuvo gate de servidor (cero `require_hotel_module('pwa')` en el repo: manifest/SW/offline/instalar ya funcionaban para todos) → moverlo a base fue inocuo en código; si algún día se revende, hay que ESCRIBIR los gates, no solo la fila del catálogo.

## UI / temas

- White-label: TODO el cromado deriva de `--brand-*`; semánticos (éxito/error/limpieza) reservados a su significado. Skill `deleite-sereno` para pantallas nuevas "premium".
- Temas: base Deleite (editorial) + Cupertino (liquid glass, cupertino.css con secciones §N) + modo oscuro (dark-theme.css, remapeo de tokens). Modo oscuro se activa con `html[data-theme="dark"]` (persistido en `localStorage['medisoft:theme']` = light|auto|dark, script inline en header.php). GOTCHA superficies dark: `--brand-surface`=#1C1C1E (tarjeta ELEVADA) y `--brand-surface-soft`=#000 (hundida/negro) en Cupertino oscuro (en Deleite oscuro son #201F19/#26241D) → para tarjetas usar SIEMPRE `--brand-surface`, `-soft` deja negro puro. GOTCHA vistas con tokens propios (`--user-*` en usuarios, `--qv-*`, etc.): si están hardcodeados claros NO se voltean solos en dark aunque cupertino.css oscurezca los paneles base; y un `!important` de la vista sobre esos tokens le gana a las reglas de cupertino sin `!important` (caso `.act-btn` blanco). Fix canónico: bloque `html[data-theme="dark"] .vista { --token: var(--brand-*) }` que remapea los neutros de la vista de un golpe; semánticos (rol/estado) se tintan oscuro + texto claro CONSERVANDO matiz.
- Tipografía: font-weight máx 700 en sans, nunca 800/900. Mobile-first ≤768px compacto.
- Íconos: el bundle es **Font Awesome 6.4 free** (`public_html/vendor/fontawesome/css/all.min.css`) — los nombres Pro NO fallan ruidosamente, se pintan VACÍOS (había un `fa-sparkles` invisible en el título "Amenidades especiales" de /configuracion, ya cambiado a `fa-wand-magic-sparkles`). Antes de usar un ícono nuevo: `grep -q "\.fa-<nombre>:before" all.min.css`. Los alias FA5 (`fa-swimming-pool`, `fa-hot-tub`, `fa-tshirt`, `fa-check-circle`) siguen vivos en el 6 free.
- **Selección de texto accidental en táctil = apagada GLOBALMENTE** (jul-27, `css/custom.css` al final): `body{user-select:none;-webkit-touch-callout:none}` dentro de `@media (hover:none) and (pointer:coarse)` — el ratón conserva la selección normal, así que el Browser pane (pointer fino) NO reproduce el fix: verificarlo parseando el CSS a un `document.implementation.createHTMLDocument` y leyendo las `cssRules` del media, no midiendo computados. Antes había ~30 parches por vista (dashboard, mensajes, reservaciones/ver, habitaciones/editar…) y el resaltado azul con manijas reaparecía en cualquier vista nueva. OBLIGATORIO el bloque que reactiva `input,textarea,select,[contenteditable]` (sin él iOS bloquea la EDICIÓN de los campos, no solo la selección); para dejar copiar un dato puntual (folio, teléfono) usar la clase `.ms-copiable` o `[data-copiable]`. `asset_version()` es `filemtime` → el celular recibe el CSS nuevo solo, sin purgar nada.
- Fondo ivory/beige ("marfil cálido" de Deleite Sereno): jul-2026 se ELIMINÓ por completo, en TODOS los temas (no solo Cupertino) y en TODAS las vistas (no solo el canvas de página). Dos capas distintas, ambas tocadas: (1) el fondo raíz por-vista — `background: radial-gradient(...), linear-gradient(180deg, var(--xx-ivory-2), var(--xx-ivory))` en ~55 wrappers, con variantes `!important`/stops extra/sufijo `-strong` — se borró la declaración entera; (2) los TOKENS beige mismos (`--xx-ivory`, `--xx-ivory-2`, `--xx-surface-warm` y sus bases en `color-mix(...)`) que seguían pintando cards/paneles/hovers/empty-states aun sin el canvas — se retintaron in-place (hex #F6F2EA/#FBF8F2/#FCFAF5/#F8F5ED/#FBF9F4/#FFFCF7/#F7F4EC/#F8F4EC → #F5F5F7/#FAFAFC, el mismo par neutro que cupertino.css ya usaba para remapear). La fuente MÁS central del beige no eran las vistas sino el shell: `body.hotel-layout-scope` en `hotel-layout-shell.css` (`--hotel-bg`/`--hotel-panel`/`--hotel-brand-soft`, fallback cuando el hotel no define `--brand-surface-soft`) + `sidebar-rail.css` (`--sr-ivory`) + `pwa-launch-splash.css` — esas 3 pintan TODA la app sin importar la vista, arréglalas primero si el beige reaparece. Si algo nuevo necesita "calidez", no usar estos hex — usar `--brand-*` o quedarse en `#F5F5F7`/blanco.

## Estilo de trabajo del usuario (Maximiliano)

- **Auditar antes de implementar**; propuestas concisas; economía de tokens (sin .md innecesarios, lecturas quirúrgicas).
- Toda mejora verificada del giro SaaS se vuelca a `docs/PLAYBOOK-SAAS-VERTICALES.md` en la misma sesión (§3/§4/§5/§7a según tipo).
- Commits en español con prefijo tipo `feat(modulo):` / `fix:` / `chore:`.
